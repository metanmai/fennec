/**
 * @fennec/daemon — LaunchDaemon-hosted capture process. Phase 1 Plan 01-01
 * (Wave 0) ships only this placeholder; the adapter registry, JSONL queue,
 * redactor, and sync loop arrive in plans 01-03 .. 01-06.
 */
export const PLACEHOLDER_VERSION = "0.1.0";
