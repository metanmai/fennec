{
  "name": "@fennec/daemon",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc --build && node scripts/copy-assets.mjs",
    "build:gitleaks-rules": "node scripts/build-gitleaks-rules.mjs",
    "test": "vitest run"
  },
  "dependencies": {
    "@clack/prompts": "1.5.0",
    "@fennec/shared": "*"
  }
}
