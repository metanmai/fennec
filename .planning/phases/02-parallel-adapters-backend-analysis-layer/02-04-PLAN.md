---
phase: 02-parallel-adapters-backend-analysis-layer
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260531000008_partition_window_2026_07_08.sql
  - supabase/migrations/20260531000009_prompt_outcomes.sql
  - supabase/migrations/20260531000010_model_fit_scores.sql
  - supabase/migrations/20260531000011_model_pricing.sql
  - supabase/migrations/20260531000012_daily_rollups_by_user.sql
  - supabase/migrations/20260531000013_daily_rollups_by_project.sql
  - supabase/migrations/20260531000014_ai_events_correlation_columns.sql
  - supabase/migrations/20260531000015_git_events_idempotency_key.sql
  - supabase/migrations/20260531000016_seed_model_pricing.sql
  - supabase/migrations/README.md
autonomous: false
requirements: [ANL-02, ANL-03, ANL-04, ANL-05, ANL-07, ANL-08, ANL-09]

must_haves:
  truths:
    - "A partition-window migration creates the CURRENT + NEXT month partitions (2026-07, 2026-08) for ai_events AND git_events (Phase-1 tables whose partitions end 2026-07-01 / 2026-06-01 respectively) AND attaches a DEFAULT partition to each as a safety net — so every current-dated insert lands in a partition and does NOT throw 'no partition of relation found' (H1, ANL-01/CAP-09 blocker)"
    - "The two new partitioned tables (prompt_outcomes, model_fit_scores) declare partitions covering 2026-07 + 2026-08 (NOT the stale _2026_05/_2026_06 window) PLUS a DEFAULT partition, so correlation/model-fit writes for current-dated prompts succeed (H1)"
    - "A rolling monthly partition-ahead mechanism is specified — a pg_cron job (preferred, if the staging extension is available) OR a documented monthly manual migration task — and 'partition covers the build window' is a BLOCKING pre-capture gate task (H1)"
    - "ai_events is ALTERed to add nullable repo_remote TEXT, repo_branch TEXT, cwd TEXT so correlation can match repo/branch and rollups can derive project (H4 schema half; wire-envelope already carries these — canonical.ts L60-62)"
    - "daily_rollups_by_project.project_id is NULLABLE with a per-org 'default project' fallback for this phase; full repo→project mapping matures Phase 3/4 (noted in-migration + OPEN-QUESTIONS) (H4)"
    - "git_events gains an idempotency_key TEXT column with a UNIQUE constraint usable as an ON CONFLICT target so retries dedupe deterministically — NOT gen_random_uuid, NOT a UUIDv5 scheme (H5; the git ingest in Plan 01 writes + conflicts on this column)"
    - "prompt_outcomes stores confidence as an interval — confidence_band + confidence_low/confidence_high (both NOT NULL, [0,1]) — with a table-level CHECK (confidence_low < confidence_high) that REJECTS a degenerate single value (equality) per ANL-02 (D2-18, EVIDENCE C8-shape; MEDIUM tightening)"
    - "prompt_outcomes carries attribution_state ∈ {attributed, downgraded_by_revert, no_outcome} + downgraded_at + downgrade_reason so reverts are a recorded transition, never a silent decrement (ANL-03, D2-19)"
    - "model_fit_scores stores the numeric score AND the input signals (prompt_length, file_edit_size, tool_call_count, model, model_tier) AND fit_verdict ∈ {under_powered, fit, over_powered} (ANL-04, D2-21)"
    - "daily_rollups_by_user + daily_rollups_by_project carry distinct cost_estimated (NOT NULL), cost_billed (NULL until reconciliation), and cost_subscription — three separate columns (ANL-07/ANL-09, D2-25/D2-27)"
    - "model_pricing is a PLAIN REFERENCE table (NO org_id column, NO tenant sentinel) keyed by (model, token_kind, effective_from, effective_to) with a pricing_kind discriminator (per_token|subscription), an EXCLUDE USING gist non-overlap constraint proven on PG17.10, and a read-any RLS policy USING (true) (ANL-08, EVIDENCE C16, H8)"
    - "Every NEW customer-data table (prompt_outcomes, model_fit_scores, daily_rollups_by_user, daily_rollups_by_project) carries org_id + BOTH a USING and a WITH CHECK (org_id = (auth.jwt() ->> 'org_id')::uuid) RLS policy in its creation migration — write-side isolation, not USING-only (D2-28, EVIDENCE C32, H8)"
    - "The seed migration seeds per-token prices ONLY for Anthropic models whose 4-token accounting is proven this phase (Opus 4.8, Sonnet 5, Haiku 4.5 per EVIDENCE C31) as DATA, plus subscription rows (Copilot Pro $10/mo, ChatGPT Plus $20/mo), plus both Sonnet-5 effective-date rows across the 2026-08-31→09-01 cutover to exercise the EXCLUDE machinery live; GPT/OpenAI seed rows are DEFERRED with a note (no surface reliably supplies GPT tokens this phase) (ANL-08/ANL-09, D2-27, EVIDENCE C41/C16/C31, LOW seed-scope)"
  artifacts:
    - path: "supabase/migrations/20260531000008_partition_window_2026_07_08.sql"
      provides: "current+next month (2026-07/08) partitions + DEFAULT partition for ai_events AND git_events (fixes Phase-1 partition gap) + rolling-partition-ahead helper/doc"
      contains: "PARTITION OF ai_events"
    - path: "supabase/migrations/20260531000009_prompt_outcomes.sql"
      provides: "prompt_outcomes per-prompt table, partitioned by occurred_at (2026-07/08 + DEFAULT), interval confidence + attribution-state, org_id + USING/WITH CHECK RLS"
      contains: "confidence_band"
    - path: "supabase/migrations/20260531000010_model_fit_scores.sql"
      provides: "model_fit_scores per-prompt table, score + signals + fit_verdict, partitioned by occurred_at (2026-07/08 + DEFAULT), org_id + USING/WITH CHECK RLS"
      contains: "fit_verdict"
    - path: "supabase/migrations/20260531000011_model_pricing.sql"
      provides: "model_pricing PLAIN REFERENCE table (no org_id) + btree_gist EXCLUDE non-overlap + model→tier lookup + read-any RLS USING (true)"
      contains: "EXCLUDE USING gist"
    - path: "supabase/migrations/20260531000012_daily_rollups_by_user.sql"
      provides: "daily_rollups_by_user plain table, cost_estimated/cost_billed/cost_subscription, unique (org_id,user_id,day), org_id + USING/WITH CHECK RLS"
      contains: "cost_subscription"
    - path: "supabase/migrations/20260531000013_daily_rollups_by_project.sql"
      provides: "daily_rollups_by_project plain table, NULLABLE project_id + default-project fallback, unique (org_id,project_id,day), org_id + USING/WITH CHECK RLS"
      contains: "cost_subscription"
    - path: "supabase/migrations/20260531000014_ai_events_correlation_columns.sql"
      provides: "ALTER ai_events ADD nullable repo_remote/repo_branch/cwd for correlation + project derivation (H4)"
      contains: "ADD COLUMN"
    - path: "supabase/migrations/20260531000015_git_events_idempotency_key.sql"
      provides: "ALTER git_events ADD idempotency_key TEXT + UNIQUE constraint as the ON CONFLICT dedupe target (H5)"
      contains: "idempotency_key"
    - path: "supabase/migrations/20260531000016_seed_model_pricing.sql"
      provides: "idempotent ON CONFLICT DO NOTHING Anthropic-only per-token + subscription + tier seed data (GPT deferred)"
      contains: "ON CONFLICT"
  key_links:
    - from: "supabase/migrations/20260531000011_model_pricing.sql"
      to: "aggregator effective-date price selection (Plan 06/07)"
      via: "tstzrange(effective_from, effective_to, '[)') non-overlap per (model, token_kind)"
      pattern: "tstzrange"
    - from: "supabase/migrations/20260531000009_prompt_outcomes.sql"
      to: "20260531000006_rls_policies.sql idiom (extended with WITH CHECK)"
      via: "ENABLE ROW LEVEL SECURITY + CREATE POLICY ... USING (...) WITH CHECK (org_id = (auth.jwt() ->> 'org_id')::uuid)"
      pattern: "WITH CHECK"
    - from: "supabase/migrations/20260531000015_git_events_idempotency_key.sql"
      to: "backend/src/db/queries/git-events.ts insertGitEvent (Plan 01 Task 3)"
      via: "ON CONFLICT (idempotency_key) DO NOTHING dedupe target"
      pattern: "idempotency_key"
---

<objective>
Author the nine Supabase migrations (008-016) that (a) FIX the Phase-1 partition-coverage gap so current-dated inserts stop throwing "no partition of relation found" (H1), (b) create the five dashboard-read analysis tables — `prompt_outcomes`, `model_fit_scores`, `model_pricing`, `daily_rollups_by_user`, `daily_rollups_by_project` — with correct 2026-07/08 partitions + tenant-write-safe RLS, (c) ALTER the Phase-1 tables for correlation/dedupe (`ai_events` += repo_remote/repo_branch/cwd; `git_events` += idempotency_key), and (d) idempotently seed Anthropic pricing — then push them to staging. These tables are the write targets for the Plan 06 consumer worker and the Plan 07 aggregator cron; their columns encode the locked analysis contracts: interval confidence (ANL-02), revert-downgrade state (ANL-03), persisted model-fit signals + verdict (ANL-04), estimated-vs-billed-vs-subscription cost columns (ANL-07/ANL-09), and effective-date pricing with a proven non-overlap constraint (ANL-08).

Purpose: The workers cannot write a single row until these tables exist AND a valid partition covers the build-window date; the Phase-1 `ai_events` partitions end 2026-07-01 (exclusive) and `git_events` ends 2026-06-01, so as of today (2026-07-01) EVERY current-dated capture/analysis insert throws — that gap blocks ALL of Phase 2's live-capture acceptance (H1). Build/type checks pass WITHOUT a live schema push because backend types come from config not the DB, so the schema push is a mandatory blocking gate here (false-positive otherwise). Every NEW customer-data table is tenant-write-correct from creation (org_id + BOTH USING and WITH CHECK RLS) per D2-28/H8; `model_pricing` is a plain reference table (read-any), not customer data.

Output: nine migration files continuing the `20260531000007` timestamp sequence (008 partition-window fix; 009-013 the five tables; 014 ai_events correlation columns; 015 git_events idempotency_key; 016 the Anthropic seed), a README note describing the new tables + the btree_gist requirement + the rolling-partition mechanism, and a live `supabase db push` applying them to staging with "partition covers the build window" verified as a blocking gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-parallel-adapters-backend-analysis-layer/02-CONTEXT.md
@.planning/phases/02-parallel-adapters-backend-analysis-layer/02-SPEC.md
@.planning/phases/02-parallel-adapters-backend-analysis-layer/02-EVIDENCE.md
@.planning/phases/02-parallel-adapters-backend-analysis-layer/02-PATTERNS.md
@.planning/phases/02-parallel-adapters-backend-analysis-layer/02-OPEN-QUESTIONS.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase + EVIDENCE; use directly, do not re-explore. -->

From supabase/migrations/20260531000002_ai_events_partitioned.sql — the per-event partition idiom to copy for 009/010: `PARTITION BY RANGE (occurred_at)`, PK includes the partition column (`PRIMARY KEY (idempotency_key, occurred_at)`), and parent-level `CREATE INDEX ... (org_id, occurred_at)` inherited by partitions. CRITICAL — the Phase-1 partitions are STALE for a 2026-07-01 build: `ai_events` has only `_2026_05` (05-01→06-01) and `_2026_06` (06-01→07-01), so its coverage ENDS 2026-07-01 exclusive; `git_events` has only `_2026_05` (05-01→06-01), so its coverage ENDS 2026-06-01. DO NOT copy this stale window for the new tables — new tables partition on 2026-07 + 2026-08 and both new AND old tables get a DEFAULT partition. The correct new-partition shape (migration 008) is:
```
CREATE TABLE ai_events_2026_07 PARTITION OF ai_events FOR VALUES FROM ('2026-07-01') TO ('2026-08-01');
CREATE TABLE ai_events_2026_08 PARTITION OF ai_events FOR VALUES FROM ('2026-08-01') TO ('2026-09-01');
CREATE TABLE ai_events_default PARTITION OF ai_events DEFAULT;   -- safety net so no insert ever throws
```
(same for git_events; and for the new prompt_outcomes / model_fit_scores parents inside their own creation migrations). Rolling-ahead: prefer a `pg_cron` monthly job that runs a `CREATE TABLE IF NOT EXISTS <t>_<next_month> PARTITION OF <t> ...` helper (Supabase supports pg_cron); if pg_cron is unavailable on staging, fall back to a documented monthly manual migration task recorded in the README. NOTE: a DEFAULT partition means a routing-index degradation if it ever accumulates rows — it is a safety net, not a substitute for the monthly rollout; the pre-capture gate confirms a dedicated month partition exists for the build date.

From supabase/migrations/20260531000003_git_events_partitioned.sql — the CHECK-constraint idiom: `event_type TEXT NOT NULL CHECK (event_type IN ('commit','revert','file_edit','branch_switch'))`; `id UUID NOT NULL DEFAULT gen_random_uuid()`; PK `(id, occurred_at)`. Copy this CHECK shape for confidence_band / attribution_state / fit_verdict / pricing_kind. git_events has NO idempotency_key column (EVIDENCE C21) — migration 015 ADDs `idempotency_key TEXT` + a UNIQUE constraint that is the deterministic ON CONFLICT dedupe target for the Plan-01 git ingest (H5). Because the PK includes the partition column, a plain `UNIQUE (idempotency_key)` across a partitioned table is NOT creatable directly; use `UNIQUE (idempotency_key, occurred_at)` (partition-column-inclusive, matching the ai_events dedupe idiom) as the ON CONFLICT target, and have the git adapter derive a stable idempotency_key per reflog entry (Plan 01 Task 3). Do NOT use gen_random_uuid for dedupe; do NOT invent a UUIDv5 scheme.

From supabase/migrations/20260531000006_rls_policies.sql — the RLS idiom to EXTEND for the four new CUSTOMER-DATA tables (prompt_outcomes, model_fit_scores, daily_rollups_by_user, daily_rollups_by_project). The Phase-1 policies are USING-only; the new tables MUST add BOTH USING and WITH CHECK so a service_role-bypass-off future cannot write cross-tenant rows (H8):
```
ALTER TABLE <table> ENABLE ROW LEVEL SECURITY;
CREATE POLICY <table>_tenant_isolation ON <table>
  USING (org_id = (auth.jwt() ->> 'org_id')::uuid)
  WITH CHECK (org_id = (auth.jwt() ->> 'org_id')::uuid);
```
`model_pricing` is NOT customer data — it is a plain reference table. It gets NO org_id column, NO tenant policy, NO sentinel org. Its policy is read-any:
```
ALTER TABLE model_pricing ENABLE ROW LEVEL SECURITY;
CREATE POLICY model_pricing_read_any ON model_pricing USING (true);
```

From supabase/migrations/20260531000007_seed_phase1_test_data.sql — the EXACT idempotent-seed idiom for 016: `CREATE EXTENSION IF NOT EXISTS pgcrypto;` at top when needed; every `INSERT ... VALUES (...) ON CONFLICT (<key>) DO NOTHING;`.

From EVIDENCE C16 (PROVEN on local PG 17.10) — the non-overlap constraint that MUST be used verbatim:
```
CREATE EXTENSION IF NOT EXISTS btree_gist;
ALTER TABLE model_pricing ADD CONSTRAINT model_pricing_no_overlap
  EXCLUDE USING gist (model WITH =, token_kind WITH =,
    tstzrange(effective_from, effective_to, '[)') WITH &&)
  WHERE (pricing_kind = 'per_token');
```
Proven: two non-overlapping rows insert OK; an overlapping insert raises `conflicting key value violates exclusion constraint`; selection at 2026-08-15 → old price, at 2026-10-15 → new price.

From EVIDENCE C33 — the last Phase 1 migration is `20260531000007_seed_phase1_test_data.sql`; Phase 2 continues the sequence at 008 (008 = partition-window fix; 009-013 = the five tables; 014 = ai_events correlation columns; 015 = git_events idempotency_key; 016 = the Anthropic seed). From EVIDENCE C17/C16/Q16 — local PG is 17.10 (EXCLUDE proven); staging Supabase PG version is UNVERIFIABLE here but ≥15 assumed and EXCLUDE works on 15-17 (the `WITHOUT OVERLAPS` PG18 temporal PK is NOT to be used). From canonical.ts L60-62 — the wire envelope ALREADY carries `cwd`/`git_remote`/`git_branch` (all optional); migration 014 ALTERs ai_events so the ingest (Plan 01) can persist them (they were dropped before). From EVIDENCE C31 — only Anthropic's four token fields are proven captured this phase; the seed (016) covers Anthropic per-token prices only, and DEFERS GPT/OpenAI seed rows (no surface reliably supplies GPT tokens this phase — a GPT price row would be dead data that mis-signals coverage).

From EVIDENCE C41 (REFUTED SPEC figures — use these CORRECTED values): Copilot Pro = $10/mo (usage-based since 2026-06-01; flat line is a documented simplification, NOT $19), ChatGPT Plus = $20/mo (NOT "ChatGPT Pro $20"). Per-token seed prices (C37/C38, volatile — RE-VERIFY at build per Q12/Q15): Opus 4.8 $5/$25 input/output per MTok (cache write 1.25×, cache read 0.1× base input → $6.25/$0.50); Sonnet 5 intro $2/$10 through 2026-08-31, $3/$15 from 2026-09-01; Haiku 4.5 $1/$5.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 008 — FIX partition-coverage gap for ai_events + git_events (H1, blocking)</name>
  <files>supabase/migrations/20260531000008_partition_window_2026_07_08.sql, supabase/migrations/README.md</files>
  <read_first>
    - supabase/migrations/20260531000002_ai_events_partitioned.sql (Phase-1 ai_events partitions END 2026-07-01 exclusive — only _2026_05 + _2026_06 exist)
    - supabase/migrations/20260531000003_git_events_partitioned.sql (Phase-1 git_events partitions END 2026-06-01 — only _2026_05 exists)
    - .planning/phases/02-parallel-adapters-backend-analysis-layer/02-EVIDENCE.md (C21 git_events cols; C33 timestamp sequence)
    - .planning/phases/02-parallel-adapters-backend-analysis-layer/02-OPEN-QUESTIONS.md (Q11/Q16 staging PG version + extension availability)
  </read_first>
  <action>
Author `20260531000008_partition_window_2026_07_08.sql` — the H1 fix. Today is 2026-07-01, so the Phase-1 partition windows are exhausted: an insert dated now throws `no partition of relation "ai_events" found for row`. This migration MUST land before any live capture/analysis insert.

For BOTH `ai_events` and `git_events`, create the current + next month partitions and a DEFAULT safety-net partition:
```
CREATE TABLE IF NOT EXISTS ai_events_2026_07 PARTITION OF ai_events FOR VALUES FROM ('2026-07-01') TO ('2026-08-01');
CREATE TABLE IF NOT EXISTS ai_events_2026_08 PARTITION OF ai_events FOR VALUES FROM ('2026-08-01') TO ('2026-09-01');
CREATE TABLE IF NOT EXISTS ai_events_default PARTITION OF ai_events DEFAULT;
CREATE TABLE IF NOT EXISTS git_events_2026_06 PARTITION OF git_events FOR VALUES FROM ('2026-06-01') TO ('2026-07-01');
CREATE TABLE IF NOT EXISTS git_events_2026_07 PARTITION OF git_events FOR VALUES FROM ('2026-07-01') TO ('2026-08-01');
CREATE TABLE IF NOT EXISTS git_events_2026_08 PARTITION OF git_events FOR VALUES FROM ('2026-08-01') TO ('2026-09-01');
CREATE TABLE IF NOT EXISTS git_events_default PARTITION OF git_events DEFAULT;
```
(git_events also needs the 2026-06 month to close its 06-01→07-01 gap.) Use `IF NOT EXISTS` so re-running is safe even if a later hand-rolled partition already exists.

Rolling-ahead mechanism — pick ONE and implement/document it in THIS migration file + the README:
- PREFERRED: a `pg_cron` monthly job. Include (guarded by `CREATE EXTENSION IF NOT EXISTS pg_cron;` inside a `DO $$ ... EXCEPTION WHEN insufficient_privilege OR undefined_file THEN RAISE NOTICE ... $$` block so the migration does NOT hard-fail if staging forbids pg_cron) a `cron.schedule('fennec-partition-ahead', '0 0 1 * *', $$ SELECT fennec_create_next_partitions(); $$)` plus a small `fennec_create_next_partitions()` PL/pgSQL helper that `CREATE TABLE IF NOT EXISTS`-es the next month for both tables (and for prompt_outcomes/model_fit_scores once they exist).
- FALLBACK (if pg_cron unavailable): the migration still creates 07+08+DEFAULT; the README documents a "monthly partition-ahead migration task" that must be run before the start of each new month, and the pre-capture gate (Task 4) verifies a dedicated month partition exists for the build date.

Add a comment: the DEFAULT partition is a safety net against throws — it must NOT be relied on as the steady-state home for current data (it degrades partition pruning); the monthly job/task keeps a dedicated month partition present.

Update `supabase/migrations/README.md`: add a "Phase 2 partition window" note explaining the gap fix and the rolling mechanism chosen.

Do NOT create the analysis tables here (Task 2). Do NOT push yet (Task 4).
  </action>
  <verify>
    <automated>cd /Users/Tanmai.N/Documents/fennec && ls supabase/migrations/20260531000008_partition_window_2026_07_08.sql && grep -c "PARTITION OF ai_events" supabase/migrations/20260531000008_partition_window_2026_07_08.sql && grep -c "PARTITION OF git_events" supabase/migrations/20260531000008_partition_window_2026_07_08.sql && grep -Eq "2026-07-01|2026-08-01" supabase/migrations/20260531000008_partition_window_2026_07_08.sql && grep -q "DEFAULT" supabase/migrations/20260531000008_partition_window_2026_07_08.sql</automated>
  </verify>
  <acceptance_criteria>
    - `20260531000008_partition_window_2026_07_08.sql` exists.
    - ai_events gets `_2026_07` (07-01→08-01), `_2026_08` (08-01→09-01), and a DEFAULT partition.
    - git_events gets `_2026_06`, `_2026_07`, `_2026_08`, and a DEFAULT partition (closing its 06-01→07-01 gap too).
    - A rolling-ahead mechanism (pg_cron job guarded for privilege failure, OR a documented monthly migration task) is present in the migration + README.
    - Every partition create uses `IF NOT EXISTS` (re-run safe).
  </acceptance_criteria>
  <done>Partition-window gap fixed: ai_events + git_events cover 2026-07/08 + DEFAULT; rolling-ahead mechanism specified; README updated. No table creates, no push yet.</done>
</task>

<task type="auto">
  <name>Task 2: Migrations 009-013 — five analysis tables (correct partitions + org_id + USING/WITH CHECK RLS; model_pricing plain-reference)</name>
  <files>supabase/migrations/20260531000009_prompt_outcomes.sql, supabase/migrations/20260531000010_model_fit_scores.sql, supabase/migrations/20260531000011_model_pricing.sql, supabase/migrations/20260531000012_daily_rollups_by_user.sql, supabase/migrations/20260531000013_daily_rollups_by_project.sql, supabase/migrations/README.md</files>
  <read_first>
    - supabase/migrations/20260531000002_ai_events_partitioned.sql (partition + PK + index idiom for 009/010 — S6; but partition on 2026-07/08 + DEFAULT, NOT the stale _05/_06)
    - supabase/migrations/20260531000003_git_events_partitioned.sql (CHECK-constraint idiom)
    - supabase/migrations/20260531000006_rls_policies.sql (RLS idiom to EXTEND with WITH CHECK for the 4 customer tables — S5, EVIDENCE C32, H8)
    - supabase/migrations/20260531000001_orgs_users_keys.sql (plain-table style for 012/013 — projects table has the org_id + FK shape to mirror)
    - .planning/phases/02-parallel-adapters-backend-analysis-layer/02-EVIDENCE.md (C16 btree_gist EXCLUDE proof; C33 timestamp sequence)
    - .planning/phases/02-parallel-adapters-backend-analysis-layer/02-PATTERNS.md (§Pattern Assignments — exact column lists; S5/S6)
  </read_first>
  <action>
Author five migration files continuing the `20260531...` sequence. The four CUSTOMER-DATA tables (prompt_outcomes, model_fit_scores, daily_rollups_by_user, daily_rollups_by_project) get `org_id UUID NOT NULL` and END with the EXTENDED RLS block — BOTH `USING (...)` AND `WITH CHECK (org_id = (auth.jwt() ->> 'org_id')::uuid)` (H8 — write-side isolation, not USING-only). `model_pricing` is a PLAIN REFERENCE table — NO org_id, NO tenant policy; its policy is `USING (true)` (read-any). The two partitioned tables partition on 2026-07 + 2026-08 + a DEFAULT partition (NOT the stale _2026_05/_2026_06 window — H1).

`20260531000009_prompt_outcomes.sql` (per-prompt, partitioned by occurred_at per S6): columns `org_id UUID NOT NULL`, `prompt_idempotency_key TEXT NOT NULL` (the correlated prompt event's key), `occurred_at TIMESTAMPTZ NOT NULL` (partition col, = the prompt's occurred_at), `git_event_id UUID NULL` (nullable link — D2-17 null-link rows exist), `confidence_band TEXT NOT NULL CHECK (confidence_band IN ('low','medium','high'))`, `confidence_low NUMERIC NOT NULL CHECK (confidence_low >= 0 AND confidence_low <= 1)`, `confidence_high NUMERIC NOT NULL CHECK (confidence_high >= 0 AND confidence_high <= 1)`, a table-level `CHECK (confidence_low < confidence_high)` — STRICT less-than so a degenerate single value (confidence_low = confidence_high) is REJECTED, enforcing ANL-02's "true interval, never a bare single number" (D2-18; MEDIUM tightening — Plan 01's PromptOutcomeSchema refine must use the same strict `<`). `attribution_state TEXT NOT NULL DEFAULT 'attributed' CHECK (attribution_state IN ('attributed','downgraded_by_revert','no_outcome'))`, `downgraded_at TIMESTAMPTZ NULL`, `downgrade_reason TEXT NULL` (D2-19). PK `(prompt_idempotency_key, occurred_at)` (idempotent-upsert target for the correlation worker — D2-15/S7). `PARTITION BY RANGE (occurred_at)` with `_2026_07` (07-01→08-01), `_2026_08` (08-01→09-01), and a DEFAULT partition. Add `CREATE INDEX ... (org_id, occurred_at)`. RLS block WITH the WITH CHECK clause.

`20260531000010_model_fit_scores.sql` (per-prompt, partitioned by occurred_at): columns `org_id UUID NOT NULL`, `prompt_idempotency_key TEXT NOT NULL`, `occurred_at TIMESTAMPTZ NOT NULL`, `score NUMERIC NOT NULL`, `fit_verdict TEXT NOT NULL CHECK (fit_verdict IN ('under_powered','fit','over_powered'))`, and the persisted signals `prompt_length INTEGER NOT NULL`, `file_edit_size INTEGER NOT NULL`, `tool_call_count INTEGER NOT NULL`, `model TEXT NOT NULL`, `model_tier TEXT` (D2-21 — score AND signals AND verdict). PK `(prompt_idempotency_key, occurred_at)`. Same partition (2026-07/08 + DEFAULT) + index shape as 009. RLS block WITH WITH CHECK.

`20260531000011_model_pricing.sql` (PLAIN REFERENCE table — RESEARCH E13.2, H8): `CREATE EXTENSION IF NOT EXISTS btree_gist;` at top. Columns `id UUID NOT NULL DEFAULT gen_random_uuid()` (identity only, NOT a dedupe key — seed uses fixed UUIDs), `model TEXT NOT NULL`, `pricing_kind TEXT NOT NULL CHECK (pricing_kind IN ('per_token','subscription'))` (D2-27 leaner same-table discriminator, per Q7), `token_kind TEXT NULL CHECK (token_kind IS NULL OR token_kind IN ('input','output','cache_creation','cache_read'))`, `price_per_mtok NUMERIC NULL` (per-token rows), `monthly_price NUMERIC NULL` (subscription rows), `tier TEXT NULL CHECK (tier IS NULL OR tier IN ('frontier','mid','small'))` (the model→tier lookup lives in this column per D2-22), `effective_from TIMESTAMPTZ NOT NULL`, `effective_to TIMESTAMPTZ NULL`. NO `org_id` column. Add a comment: "model_pricing is REFERENCE data, not customer data — no org_id, no tenant sentinel; every tenant reads the same prices (H8)." Add the PROVEN non-overlap constraint EXACTLY as EVIDENCE C16: `ALTER TABLE model_pricing ADD CONSTRAINT model_pricing_no_overlap EXCLUDE USING gist (model WITH =, token_kind WITH =, tstzrange(effective_from, effective_to, '[)') WITH &&) WHERE (pricing_kind = 'per_token');`. Add a partial index on `(model, token_kind, effective_from)` for the aggregator's effective-date lookup. End with the read-any RLS: `ALTER TABLE model_pricing ENABLE ROW LEVEL SECURITY; CREATE POLICY model_pricing_read_any ON model_pricing USING (true);`.

`20260531000012_daily_rollups_by_user.sql` (PLAIN — RESEARCH E13.2): columns `org_id UUID NOT NULL`, `user_id UUID NULL`, `day DATE NOT NULL`, `event_count INTEGER NOT NULL DEFAULT 0`, `cost_estimated NUMERIC NOT NULL DEFAULT 0` (always populated — D2-24/D2-25), `cost_billed NUMERIC NULL` (NULL until reconciliation — D2-25, NEVER defaulted to 0), `cost_subscription NUMERIC NOT NULL DEFAULT 0` (distinct field, D2-27 — never summed into cost_estimated). UNIQUE `(org_id, user_id, day)` — the aggregator's ON CONFLICT target (S7). RLS block WITH WITH CHECK.

`20260531000013_daily_rollups_by_project.sql`: same as 012 but `project_id UUID NULL` instead of user_id (H4 — project_id is NULLABLE this phase; a per-org "default project" fallback covers rows whose repo→project mapping is not yet resolved; full repo→project mapping matures Phase 3/4 — add an in-migration comment saying so and log it to OPEN-QUESTIONS). UNIQUE `(org_id, project_id, day)`. RLS block WITH WITH CHECK.

Update `supabase/migrations/README.md`: extend the Phase-2 section to list the five new tables (009-013), note that `model_pricing` requires `btree_gist` AND is a plain reference table (read-any RLS), that the four customer tables carry both USING+WITH CHECK RLS, that `daily_rollups_by_project.project_id` is nullable-with-default-project this phase, and that prices are DATA (seeded in 016), never hardcoded in workers (ANL-08).

Do NOT write any worker/query code here — that is Plans 06/07. Do NOT push yet — that is Task 4.
  </action>
  <verify>
    <automated>cd /Users/Tanmai.N/Documents/fennec && ls supabase/migrations/20260531000009_prompt_outcomes.sql supabase/migrations/20260531000010_model_fit_scores.sql supabase/migrations/20260531000011_model_pricing.sql supabase/migrations/20260531000012_daily_rollups_by_user.sql supabase/migrations/20260531000013_daily_rollups_by_project.sql && grep -c "WITH CHECK" supabase/migrations/20260531000009_prompt_outcomes.sql supabase/migrations/20260531000010_model_fit_scores.sql supabase/migrations/20260531000012_daily_rollups_by_user.sql supabase/migrations/20260531000013_daily_rollups_by_project.sql && grep -c "EXCLUDE USING gist" supabase/migrations/20260531000011_model_pricing.sql && ! grep -Eq "^\s*org_id\s+UUID" supabase/migrations/20260531000011_model_pricing.sql && grep -q "model_pricing_read_any" supabase/migrations/20260531000011_model_pricing.sql</automated>
  </verify>
  <acceptance_criteria>
    - All five migration files exist with the `20260531000009..013` prefixes.
    - `grep -c "confidence_band" supabase/migrations/20260531000009_prompt_outcomes.sql` ≥1 AND a table-level `CHECK (confidence_low < confidence_high)` (STRICT less-than) is present (ANL-02 true-interval enforcement — rejects equality).
    - `grep -c "attribution_state" supabase/migrations/20260531000009_prompt_outcomes.sql` ≥1 with the three-value CHECK incl. `downgraded_by_revert` (ANL-03).
    - `grep -c "fit_verdict" supabase/migrations/20260531000010_model_fit_scores.sql` ≥1 and all five signal columns (prompt_length, file_edit_size, tool_call_count, model, model_tier) present (ANL-04).
    - `grep -c "cost_subscription" supabase/migrations/20260531000012_daily_rollups_by_user.sql` ≥1 and `cost_billed` is NULLable with no DEFAULT 0 (ANL-07/ANL-09).
    - `grep -c "EXCLUDE USING gist" supabase/migrations/20260531000011_model_pricing.sql` = 1, `pricing_kind` discriminator present (ANL-08/D2-27), and `model_pricing` has NO `org_id` column (plain reference; H8).
    - Each of the FOUR customer tables contains one `ENABLE ROW LEVEL SECURITY` + one `CREATE POLICY ..._tenant_isolation` with BOTH `USING` and `WITH CHECK` (H8/D2-28); `model_pricing` has a `USING (true)` read-any policy.
    - The two partitioned tables partition on 2026-07 + 2026-08 + DEFAULT (NOT the stale _2026_05/_2026_06); `daily_rollups_by_project.project_id` is NULLABLE (H4).
  </acceptance_criteria>
  <done>Five migrations authored (009-013): two partitioned per-prompt tables on the 2026-07/08+DEFAULT window, model_pricing as a plain reference table (no org_id, read-any RLS) with the proven EXCLUDE, two plain rollup tables with distinct cost columns; four customer tables carry USING+WITH CHECK RLS; project_id nullable; README updated. No worker code, no push yet.</done>
</task>

<task type="auto">
  <name>Task 3: Migrations 014 + 015 — ALTER ai_events (correlation columns) + ALTER git_events (idempotency_key) (H4/H5)</name>
  <files>supabase/migrations/20260531000014_ai_events_correlation_columns.sql, supabase/migrations/20260531000015_git_events_idempotency_key.sql, supabase/migrations/README.md</files>
  <read_first>
    - supabase/migrations/20260531000002_ai_events_partitioned.sql (ai_events current columns — repo/branch/cwd absent; PK (idempotency_key, occurred_at))
    - supabase/migrations/20260531000003_git_events_partitioned.sql (git_events current columns — NO idempotency_key; PK (id, occurred_at))
    - packages/shared/src/events/canonical.ts (L60-62 — wire envelope already carries cwd/git_remote/git_branch)
    - .planning/phases/02-parallel-adapters-backend-analysis-layer/02-EVIDENCE.md (C21 git_events cols; C31 Anthropic token capture)
  </read_first>
  <action>
`20260531000014_ai_events_correlation_columns.sql` (H4 schema half): `ALTER TABLE ai_events ADD COLUMN IF NOT EXISTS repo_remote TEXT; ALTER TABLE ai_events ADD COLUMN IF NOT EXISTS repo_branch TEXT; ALTER TABLE ai_events ADD COLUMN IF NOT EXISTS cwd TEXT;` — all NULLABLE (pre-existing rows and tools without a workspace context have NULL). These let the correlation worker match "same repo/branch → high confidence" against the ai_events side and let rollups derive project from repo_remote. The wire envelope already carries `git_remote`/`git_branch`/`cwd` (canonical.ts L60-62) — Plan 01's ingest threads them onto the row (InsertableAiEvent). Add a comment naming the mapping: envelope `git_remote`→`repo_remote`, `git_branch`→`repo_branch`, `cwd`→`cwd` (aligns column names with git_events' `repo_remote`/`repo_branch`). Optionally add an index `(org_id, repo_remote, occurred_at)` for the correlation join.

`20260531000015_git_events_idempotency_key.sql` (H5): `ALTER TABLE git_events ADD COLUMN IF NOT EXISTS idempotency_key TEXT;` then add a UNIQUE constraint usable as an ON CONFLICT target. Because git_events is partitioned by `occurred_at` and Postgres requires the partition column in any unique constraint, use `ALTER TABLE git_events ADD CONSTRAINT git_events_idempotency_unique UNIQUE (idempotency_key, occurred_at);` (partition-column-inclusive, mirroring the ai_events `(idempotency_key, occurred_at)` PK dedupe idiom). The Plan-01 git ingest writes this column with a stable per-reflog-entry key and conflicts on `(idempotency_key, occurred_at)` so a retried git event dedupes to one row. Add a comment: "H5 — do NOT rely on gen_random_uuid for dedupe (it makes every retry a new row); idempotency_key is the deterministic dedupe target the git adapter derives per reflog entry (see Plan 01 Task 3)."

Update `supabase/migrations/README.md`: note the two ALTERs (ai_events correlation columns; git_events idempotency_key) and that they are additive/backward-compatible.

Do NOT push yet — that is Task 4.
  </action>
  <verify>
    <automated>cd /Users/Tanmai.N/Documents/fennec && ls supabase/migrations/20260531000014_ai_events_correlation_columns.sql supabase/migrations/20260531000015_git_events_idempotency_key.sql && grep -Eq "repo_remote|repo_branch|cwd" supabase/migrations/20260531000014_ai_events_correlation_columns.sql && grep -q "idempotency_key" supabase/migrations/20260531000015_git_events_idempotency_key.sql && grep -q "UNIQUE" supabase/migrations/20260531000015_git_events_idempotency_key.sql</automated>
  </verify>
  <acceptance_criteria>
    - `20260531000014_ai_events_correlation_columns.sql` ADDs nullable `repo_remote`, `repo_branch`, `cwd` to ai_events (all `ADD COLUMN IF NOT EXISTS`).
    - `20260531000015_git_events_idempotency_key.sql` ADDs `idempotency_key TEXT` + a UNIQUE `(idempotency_key, occurred_at)` constraint (partition-column-inclusive) usable as an ON CONFLICT target.
    - No gen_random_uuid-based dedupe and no UUIDv5 scheme is introduced (H5).
    - README notes both ALTERs as additive.
  </acceptance_criteria>
  <done>ai_events ALTERed with repo_remote/repo_branch/cwd (H4); git_events ALTERed with idempotency_key + UNIQUE(idempotency_key, occurred_at) as the ON CONFLICT dedupe target (H5); README updated. No push yet.</done>
</task>

<task type="auto">
  <name>Task 4: Migration 016 — idempotent Anthropic-only model_pricing + subscription + tier seed (GPT deferred)</name>
  <files>supabase/migrations/20260531000016_seed_model_pricing.sql</files>
  <read_first>
    - supabase/migrations/20260531000007_seed_phase1_test_data.sql (EXACT idempotent-seed idiom — every INSERT ... ON CONFLICT DO NOTHING)
    - supabase/migrations/20260531000011_model_pricing.sql (the table + column names authored in Task 2 — NO org_id column)
    - .planning/phases/02-parallel-adapters-backend-analysis-layer/02-EVIDENCE.md (C31 Anthropic 4-token capture proven; C37/C38 per-token seed prices; C41 CORRECTED subscription figures; C16 the Sonnet-5 cutover exercises the EXCLUDE machinery)
    - .planning/phases/02-parallel-adapters-backend-analysis-layer/02-OPEN-QUESTIONS.md (Q12/Q15 — prices are volatile; RE-VERIFY at build; GPT-token deferral)
  </read_first>
  <action>
Author `20260531000016_seed_model_pricing.sql` seeding `model_pricing` with `ON CONFLICT (id) DO NOTHING` on a stable conflict key (use fixed UUIDs in the `id` column as `20260531000007` does, so re-running is idempotent). Note `model_pricing` has NO org_id column — do NOT reference a sentinel org (Task 2 made it a plain reference table, H8). Add a top-of-file comment: "PRICES ARE VOLATILE — re-verify all figures at build per Q12/Q15; these are DATA not code (ANL-08). Copilot Pro moved to usage-based billing 2026-06-01 — the flat subscription line is a documented simplification (EVIDENCE C41). SCOPE: seed Anthropic per-token prices ONLY — the four Anthropic Usage token fields are the only token accounting PROVEN captured this phase (EVIDENCE C31); GPT/OpenAI per-token rows are DEFERRED (no surface reliably supplies GPT token counts this phase — a GPT price row would be dead data that falsely signals coverage). Add GPT rows when a GPT token source lands (Phase 3+)."

Per-token rows (`pricing_kind='per_token'`), one row per (model, token_kind) with `price_per_mtok`, `tier`, and `effective_from`/`effective_to` — ANTHROPIC ONLY:
- Claude Opus 4.8 (tier frontier): input $5, output $25, cache_creation $6.25, cache_read $0.50 per MTok; `effective_from '2026-01-01'`, `effective_to NULL`.
- Claude Sonnet 5 (tier frontier) — seed BOTH effective-date rows per token_kind to exercise the EXCLUDE non-overlap constraint live (EVIDENCE C16): intro rows `effective_from '2026-01-01' effective_to '2026-09-01'` (input $2, output $10), then `effective_from '2026-09-01' effective_to NULL` (input $3, output $15). Include cache_creation/cache_read rows for at least the current window.
- Claude Haiku 4.5 (tier small): input $1, output $5; effective_from '2026-01-01', effective_to NULL.
- Do NOT seed GPT/OpenAI per-token rows this phase (deferred per the scope comment above + OPEN-QUESTIONS).

Subscription rows (`pricing_kind='subscription'`, `token_kind NULL`, `monthly_price`): GitHub Copilot Pro `monthly_price 10.00` (EVIDENCE C41 — NOT $19), ChatGPT Plus `monthly_price 20.00` (EVIDENCE C41 — NOT "ChatGPT Pro $20"). `effective_from '2026-06-01' effective_to NULL`. (These are subscription lines, not per-token — allowed even though the underlying tools' token counts aren't captured, because they represent a flat monthly charge, not a per-token estimate.)

Ensure the two Sonnet-5 per-token windows are NON-OVERLAPPING (`[effective_from, effective_to)` half-open) so the `model_pricing_no_overlap` EXCLUDE accepts them — this is the deliberate live test of the constraint (C16).
  </action>
  <verify>
    <automated>cd /Users/Tanmai.N/Documents/fennec && ls supabase/migrations/20260531000016_seed_model_pricing.sql && grep -c "ON CONFLICT" supabase/migrations/20260531000016_seed_model_pricing.sql && grep -c "monthly_price" supabase/migrations/20260531000016_seed_model_pricing.sql && grep -Eiq "10\.00|10\b" supabase/migrations/20260531000016_seed_model_pricing.sql && ! grep -Eiq "gpt|openai" supabase/migrations/20260531000016_seed_model_pricing.sql</automated>
  </verify>
  <acceptance_criteria>
    - `20260531000016_seed_model_pricing.sql` exists; every INSERT uses `ON CONFLICT (id) DO NOTHING`; no org_id referenced.
    - Per-token rows exist for all four token_kinds (input/output/cache_creation/cache_read) for at least one model — `grep -c "cache_creation" ...` ≥1 AND `grep -c "cache_read" ...` ≥1.
    - TWO non-overlapping Sonnet-5 effective-date windows are seeded (the `effective_to '2026-09-01'` row and the `effective_from '2026-09-01'` row both present).
    - Subscription rows present with `monthly_price` 10.00 (Copilot Pro) and 20.00 (ChatGPT Plus) — NOT $19 (C41 correction applied).
    - NO GPT/OpenAI per-token rows are seeded (deferred per EVIDENCE C31 — `grep -Ei "gpt|openai"` finds nothing).
    - A top-of-file comment flags prices as volatile DATA to re-verify at build (Q12/Q15) AND records the GPT-token deferral.
  </acceptance_criteria>
  <done>Seed migration authored: Anthropic-only four-token-kind per-token prices, both Sonnet-5 cutover rows (exercises EXCLUDE), Copilot Pro $10 + ChatGPT Plus $20 subscriptions, idempotent ON CONFLICT, volatility + GPT-deferral note.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: [BLOCKING] supabase db push — apply 008-016 to staging + verify partition covers the build window</name>
  <files>supabase/migrations/20260531000008_partition_window_2026_07_08.sql, supabase/migrations/20260531000009_prompt_outcomes.sql, supabase/migrations/20260531000010_model_fit_scores.sql, supabase/migrations/20260531000011_model_pricing.sql, supabase/migrations/20260531000012_daily_rollups_by_user.sql, supabase/migrations/20260531000013_daily_rollups_by_project.sql, supabase/migrations/20260531000014_ai_events_correlation_columns.sql, supabase/migrations/20260531000015_git_events_idempotency_key.sql, supabase/migrations/20260531000016_seed_model_pricing.sql</files>
  <action>
Apply migrations 008-016 to the live staging Supabase Postgres. Ensure `SUPABASE_ACCESS_TOKEN` is exported (non-TTY) and the project is linked, then run `supabase db push` (or the existing `scripts/db-push.sh` wrapper from Plan 01-10). This is a BLOCKING human-verified gate for two reasons: (1) `npm run typecheck`/build pass WITHOUT the push (backend types come from config, not the live DB), so verification of Plans 06/07 against real tables is a false-positive until these tables physically exist in staging; (2) PARTITION-COVERS-BUILD-WINDOW is a hard pre-capture gate (H1) — if a current-dated insert has no partition, ALL live-capture acceptance fails silently into the DLQ. If staging blocks `CREATE EXTENSION btree_gist`, record it and fall back to enforcing model_pricing non-overlap via a test only (Q11/Q16); note the staging PG version. If staging blocks `pg_cron`, the migration must not hard-fail (the guarded DO-block swallows it) and the README's monthly-manual-partition task governs rolling-ahead. Confirm the Sonnet-5 two-window seed does NOT raise the `model_pricing_no_overlap` exclusion error (proves the EXCLUDE + half-open ranges are correct — EVIDENCE C16 live). This task writes NO source — it only applies the .sql authored in Tasks 1-4.
  </action>
  <verify>
    <automated>cd /Users/Tanmai.N/Documents/fennec && supabase migration list 2>&1 | tail -20</automated>
    <human-check>supabase db push applied 008-016 to staging with exit 0; a current-dated (2026-07-01) test insert into ai_events AND git_events succeeds (partition present — H1 gate); the five tables + seed rows confirmed present via psql spot-check.</human-check>
  </verify>
  <done>Migrations 008-016 applied to staging; a current-dated insert into ai_events + git_events lands in a partition (H1 gate passed); the five analysis tables + model_pricing seed exist and are queryable; ai_events has repo_remote/repo_branch/cwd; git_events has idempotency_key + UNIQUE; staging PG version + btree_gist + pg_cron availability recorded in the SUMMARY.</done>
  <what-built>
Nine migrations (008-016) authored in Tasks 1-4: the partition-window gap fix, the five analysis tables, the two Phase-1 ALTERs (ai_events correlation columns, git_events idempotency_key), and the Anthropic seed. This task APPLIES them to the live staging Supabase Postgres. It is mandatory and blocking: `npm run typecheck`/build pass WITHOUT the push because backend types come from config not the live DB, AND a missing current-month partition would make every live-capture insert throw (H1) — verification of Plans 06/07 against real tables is a false-positive until these tables exist AND a partition covers today's date.
  </what-built>
  <how-to-verify>
1. Ensure `SUPABASE_ACCESS_TOKEN` is exported (non-TTY) and the project is linked (or run via the existing `scripts/db-push.sh` wrapper from Plan 01-10).
2. Run `supabase db push` (or `scripts/db-push.sh`). Watch for: the btree_gist step (if blocked → non-overlap-via-test fallback, note PG version); the pg_cron step (if blocked → the guarded DO-block should not fail the migration; README monthly-manual-task governs rolling-ahead).
3. H1 GATE: run a current-dated (`now()`) test INSERT into `ai_events` AND `git_events` and confirm it succeeds (no "no partition of relation found"). This proves migration 008 closed the gap. Roll the test rows back or use a throwaway org_id.
4. Confirm the Sonnet-5 two-window seed does NOT raise the `model_pricing_no_overlap` exclusion error (proves the EXCLUDE + half-open ranges are correct — EVIDENCE C16 live).
5. Spot-check: `select count(*) from model_pricing;` returns the seeded rows; `select confidence_band from prompt_outcomes limit 0;`, `select fit_verdict from model_fit_scores limit 0;`, `select cost_subscription from daily_rollups_by_user limit 0;`, `select repo_remote, cwd from ai_events limit 0;`, `select idempotency_key from git_events limit 0;` all resolve (columns exist).
Record the staging PG version + btree_gist + pg_cron availability + row counts + the H1 current-dated-insert result in the SUMMARY.
  </how-to-verify>
  <acceptance_criteria>
    - `supabase db push` applies migrations 008-016 to staging with exit 0.
    - H1 GATE: a current-dated insert into ai_events AND git_events succeeds (partition covers the build window).
    - The two Sonnet-5 effective-date rows seed without an exclusion-constraint error (non-overlap ranges correct).
    - `model_pricing` (no org_id), `prompt_outcomes`, `model_fit_scores`, `daily_rollups_by_user`, `daily_rollups_by_project` all exist with the interval/verdict/cost columns queryable; ai_events has repo_remote/repo_branch/cwd; git_events has idempotency_key + UNIQUE(idempotency_key, occurred_at).
    - Staging PG version + btree_gist + pg_cron availability recorded in the SUMMARY.
  </acceptance_criteria>
  <resume-signal>Type "applied" once `supabase db push` succeeds, the current-dated H1 test insert lands in a partition, and the five tables + seed are confirmed in staging — or describe the push failure (e.g. btree_gist blocked → fall-back-to-test disposition; pg_cron blocked → manual-monthly-task disposition).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker → new analysis tables | Plans 06/07 workers derive org_id from the SOURCE ai_events row (app-layer, with a test — H9) and write these tables; RLS WITH CHECK is the write-side backstop, not the primary control |
| tenant → row visibility | RLS USING policy gates every row by the JWT org_id claim; RLS WITH CHECK gates every write to the tenant's own org_id (Phase 3 activates user-scoped tokens; both clauses declared now so they cannot be retrofitted) |
| pricing rows → cost math | model_pricing is a plain REFERENCE table (read-any, no tenant scoping); a tampered/overlapping price row would corrupt cost_estimated |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Information disclosure | Cross-tenant READ of prompt_outcomes / model_fit_scores / rollups | HIGH | mitigate | RLS `tenant_isolation` USING policy in EACH creation migration extending 20260531000006 (Task 2); D2-28/D-26 |
| T-04-06 | Tampering | Cross-tenant WRITE to a customer analysis table (worker writes a row under another tenant's org_id) | HIGH | mitigate | PRIMARY control is APP-LAYER: Plans 06/07 derive org_id from the source ai_events row, NEVER the queue-message body (H9, tested). Belt-and-suspenders: RLS `WITH CHECK (org_id = jwt org_id)` on all four customer tables (Task 2) blocks the write once user-scoped tokens activate. RLS is NOT credited as the tenant-write control while workers run as service_role — the app-layer derivation is (H8 correction). |
| T-04-02 | Tampering | Overlapping / bogus effective-date price rows corrupt cost math | HIGH | mitigate | `EXCLUDE USING gist` non-overlap constraint on model_pricing (PROVEN C16); prices are seed DATA with fixed-UUID ON CONFLICT keys (Task 4) |
| T-04-03 | Tampering | prompt_outcomes confidence stored as a bare single number / degenerate interval (ANL-02 anti-pattern) | MEDIUM | mitigate | table-level `CHECK (confidence_low < confidence_high)` (STRICT) + both-bounds-NOT-NULL forces a TRUE interval; a single number and an equal-bounds degenerate value both fail (Task 2) |
| T-04-04 | Repudiation | Revert silently decrements a total (ANL-03 anti-pattern) | MEDIUM | mitigate | schema forces an explicit `attribution_state='downgraded_by_revert'` + downgraded_at/downgrade_reason recorded transition — no aggregate column to decrement (Task 2) |
| T-04-05 | Tampering | Migration false-positive: build passes but tables never applied | HIGH | mitigate | mandatory `[BLOCKING]` `supabase db push` (Task 5) before Plan 06/07 verification |
| T-04-07 | Denial of service | Current-dated insert throws "no partition of relation found" → every capture/analysis row fails silently into the DLQ (H1) | HIGH | mitigate | Migration 008 creates 2026-07/08 + DEFAULT partitions for ai_events + git_events (and the new partitioned tables); Task 5 `[BLOCKING]` gate proves a current-dated insert lands; rolling pg_cron/manual monthly partition-ahead keeps coverage |
| T-04-08 | Tampering | git_events retries duplicate rows (no dedupe target) skewing correlation/rollups (H5) | MEDIUM | mitigate | Migration 015 ADDs `idempotency_key` + UNIQUE `(idempotency_key, occurred_at)`; Plan 01 git ingest derives a deterministic per-reflog key and conflicts on it — retries dedupe to one row (no gen_random_uuid, no UUIDv5) |
| T-04-SC | Tampering | New DB extensions (btree_gist, pg_cron) enabled on staging | LOW | accept | btree_gist is a stock Postgres contrib extension (PROVEN on PG17.10 C16); pg_cron is guarded (migration does not hard-fail if forbidden) and falls back to a documented monthly manual task; no npm/package install involved |
</threat_model>

<verification>
- All nine migration files present with the 008-016 timestamp prefixes (EVIDENCE C33 sequence continued): 008 partition-window fix, 009-013 five tables, 014 ai_events correlation columns, 015 git_events idempotency_key, 016 Anthropic seed.
- Grep gates: 008 creates 2026-07/08 + DEFAULT partitions for ai_events + git_events (H1); the four customer tables carry USING + WITH CHECK RLS (H8); model_pricing has NO org_id + a read-any policy + EXCLUDE (H8); prompt_outcomes has a STRICT `CHECK (confidence_low < confidence_high)` (ANL-02); confidence_band/attribution_state/fit_verdict/cost_subscription present; 014 adds repo_remote/repo_branch/cwd; 015 adds idempotency_key + UNIQUE; 016 uses ON CONFLICT and seeds no GPT rows.
- `[BLOCKING]` `supabase db push` applied to staging with the Sonnet-5 two-window seed accepted by the EXCLUDE constraint AND a current-dated insert landing in a partition (H1 gate).
- No worker/query source written in this plan (docs-only guard: only .sql + README).
</verification>

<success_criteria>
- The partition-coverage gap is closed: ai_events + git_events (and the new partitioned tables) cover 2026-07/08 + DEFAULT, a current-dated insert succeeds, and a rolling partition-ahead mechanism is specified (H1).
- Five analysis tables created; the four customer tables carry org_id + BOTH USING and WITH CHECK RLS from their creation migration (H8/D2-28); model_pricing is a plain reference table (no org_id, read-any RLS).
- prompt_outcomes encodes a TRUE interval confidence rejecting equality (ANL-02, strict `<`) + revert-downgrade state (ANL-03); model_fit_scores encodes score + signals + verdict (ANL-04).
- Rollups carry distinct cost_estimated / cost_billed(null) / cost_subscription (ANL-07/ANL-09); daily_rollups_by_project.project_id is nullable with a default-project fallback this phase (H4).
- ai_events gains repo_remote/repo_branch/cwd (H4); git_events gains idempotency_key + UNIQUE(idempotency_key, occurred_at) as the deterministic dedupe target (H5).
- model_pricing has a proven non-overlap effective-date constraint (ANL-08); seed is Anthropic-only DATA with the C41-corrected subscription prices; GPT rows deferred (LOW seed-scope).
- Migrations pushed to staging (blocking gate satisfied, including the H1 partition gate).
</success_criteria>

<output>
Create `.planning/phases/02-parallel-adapters-backend-analysis-layer/02-04-SUMMARY.md` — record the staging PG version + btree_gist + pg_cron availability confirmed at push, the H1 current-dated-insert result (partition present), the rolling-partition mechanism chosen (pg_cron job vs documented monthly manual task), the re-verified per-token/subscription prices (or note that the seeded 2026-07-01 Anthropic figures were kept), the GPT-seed deferral, and any push fallback (test-only non-overlap) if btree_gist was blocked.
</output>
