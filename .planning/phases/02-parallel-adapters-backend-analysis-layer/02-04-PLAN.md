---
phase: 02-parallel-adapters-backend-analysis-layer
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260531000008_prompt_outcomes.sql
  - supabase/migrations/20260531000009_model_fit_scores.sql
  - supabase/migrations/20260531000010_model_pricing.sql
  - supabase/migrations/20260531000011_daily_rollups_by_user.sql
  - supabase/migrations/20260531000012_daily_rollups_by_project.sql
  - supabase/migrations/20260531000013_seed_model_pricing.sql
  - supabase/migrations/README.md
autonomous: false
requirements: [ANL-02, ANL-03, ANL-04, ANL-05, ANL-07, ANL-08, ANL-09]

must_haves:
  truths:
    - "prompt_outcomes stores confidence as an interval — confidence_band + confidence_low/confidence_high (both NOT NULL, [0,1], low<=high) — and a CHECK rejects a bare single number (ANL-02, D2-18, EVIDENCE C8-shape)"
    - "prompt_outcomes carries attribution_state ∈ {attributed, downgraded_by_revert, no_outcome} + downgraded_at + downgrade_reason so reverts are a recorded transition, never a silent decrement (ANL-03, D2-19)"
    - "model_fit_scores stores the numeric score AND the input signals (prompt_length, file_edit_size, tool_call_count, model, model_tier) AND fit_verdict ∈ {under_powered, fit, over_powered} (ANL-04, D2-21)"
    - "daily_rollups_by_user + daily_rollups_by_project carry distinct cost_estimated (NOT NULL), cost_billed (NULL until reconciliation), and cost_subscription — three separate columns (ANL-07/ANL-09, D2-25/D2-27)"
    - "model_pricing is the single source of truth keyed by (model, token_kind, effective_from, effective_to) with a pricing_kind discriminator (per_token|subscription) and an EXCLUDE USING gist non-overlap constraint proven on PG17.10 (ANL-08, D2-26, EVIDENCE C16)"
    - "Every one of the five new tables carries org_id + an RLS tenant-isolation policy in its creation migration, mirroring 20260531000006 (D2-28, EVIDENCE C32)"
    - "The seed migration seeds per-token prices for all four token kinds and subscription rows (Copilot Pro $10/mo, ChatGPT Plus $20/mo) as DATA, plus both Sonnet-5 effective-date rows across the 2026-08-31→09-01 cutover to exercise the EXCLUDE machinery live (ANL-08/ANL-09, D2-27, EVIDENCE C41/C16)"
  artifacts:
    - path: "supabase/migrations/20260531000008_prompt_outcomes.sql"
      provides: "prompt_outcomes per-prompt table, partitioned by occurred_at, interval confidence + attribution-state, RLS"
      contains: "confidence_band"
    - path: "supabase/migrations/20260531000009_model_fit_scores.sql"
      provides: "model_fit_scores per-prompt table, score + signals + fit_verdict, partitioned by occurred_at, RLS"
      contains: "fit_verdict"
    - path: "supabase/migrations/20260531000010_model_pricing.sql"
      provides: "model_pricing single-source-of-truth table + btree_gist EXCLUDE non-overlap + model→tier lookup, RLS"
      contains: "EXCLUDE USING gist"
    - path: "supabase/migrations/20260531000011_daily_rollups_by_user.sql"
      provides: "daily_rollups_by_user plain table, cost_estimated/cost_billed/cost_subscription, unique (org_id,user_id,day), RLS"
      contains: "cost_subscription"
    - path: "supabase/migrations/20260531000012_daily_rollups_by_project.sql"
      provides: "daily_rollups_by_project plain table, unique (org_id,project_id,day), RLS"
      contains: "cost_subscription"
    - path: "supabase/migrations/20260531000013_seed_model_pricing.sql"
      provides: "idempotent ON CONFLICT DO NOTHING price + subscription + tier seed data"
      contains: "ON CONFLICT"
  key_links:
    - from: "supabase/migrations/20260531000010_model_pricing.sql"
      to: "aggregator effective-date price selection (Plan 06/07)"
      via: "tstzrange(effective_from, effective_to, '[)') non-overlap per (model, token_kind)"
      pattern: "tstzrange"
    - from: "supabase/migrations/20260531000008_prompt_outcomes.sql"
      to: "20260531000006_rls_policies.sql idiom"
      via: "ENABLE ROW LEVEL SECURITY + CREATE POLICY ... USING (org_id = (auth.jwt() ->> 'org_id')::uuid)"
      pattern: "ENABLE ROW LEVEL SECURITY"
---

<objective>
Author the six Supabase migrations (008-013) that create the five dashboard-read analysis tables — `prompt_outcomes`, `model_fit_scores`, `model_pricing`, `daily_rollups_by_user`, `daily_rollups_by_project` — plus the idempotent `model_pricing` seed, then push them to staging. These tables are the write targets for the Plan 06 consumer worker and the Plan 07 aggregator cron; their columns encode the locked analysis contracts: interval confidence (ANL-02), revert-downgrade state (ANL-03), persisted model-fit signals + verdict (ANL-04), estimated-vs-billed-vs-subscription cost columns (ANL-07/ANL-09), and effective-date pricing with a proven non-overlap constraint (ANL-08).

Purpose: The workers cannot write a single row until these tables exist; build/type checks pass WITHOUT a live schema push because backend types come from config not the DB, so the schema push is a mandatory blocking gate here (false-positive otherwise). Every table is tenant-correct from creation (org_id + RLS) per D2-28.

Output: six migration files continuing the `20260531000007` timestamp sequence, a README note describing the new tables + the btree_gist requirement, and a live `supabase db push` applying them to staging.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-parallel-adapters-backend-analysis-layer/02-CONTEXT.md
@.planning/phases/02-parallel-adapters-backend-analysis-layer/02-SPEC.md
@.planning/phases/02-parallel-adapters-backend-analysis-layer/02-EVIDENCE.md
@.planning/phases/02-parallel-adapters-backend-analysis-layer/02-PATTERNS.md
@.planning/phases/02-parallel-adapters-backend-analysis-layer/02-OPEN-QUESTIONS.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase + EVIDENCE; use directly, do not re-explore. -->

From supabase/migrations/20260531000002_ai_events_partitioned.sql — the per-event partition idiom to copy for 008/009: `PARTITION BY RANGE (occurred_at)`, PK includes the partition column (`PRIMARY KEY (idempotency_key, occurred_at)`), explicit current + next month partitions (`ai_events_2026_05`, `ai_events_2026_06` for VALUES FROM '2026-05-01' TO '2026-06-01' etc.), and parent-level `CREATE INDEX ... (org_id, occurred_at)` inherited by partitions.

From supabase/migrations/20260531000003_git_events_partitioned.sql — the CHECK-constraint idiom: `event_type TEXT NOT NULL CHECK (event_type IN ('commit','revert','file_edit','branch_switch'))`; `id UUID NOT NULL DEFAULT gen_random_uuid()`; PK `(id, occurred_at)`. Copy this CHECK shape for confidence_band / attribution_state / fit_verdict / pricing_kind.

From supabase/migrations/20260531000006_rls_policies.sql — the EXACT RLS idiom to copy for all five new tables (lines 74-76 style):
```
ALTER TABLE <table> ENABLE ROW LEVEL SECURITY;
CREATE POLICY <table>_tenant_isolation ON <table>
  USING (org_id = (auth.jwt() ->> 'org_id')::uuid);
```

From supabase/migrations/20260531000007_seed_phase1_test_data.sql — the EXACT idempotent-seed idiom for 013: `CREATE EXTENSION IF NOT EXISTS pgcrypto;` at top when needed; every `INSERT ... VALUES (...) ON CONFLICT (<key>) DO NOTHING;`.

From EVIDENCE C16 (PROVEN on local PG 17.10) — the non-overlap constraint that MUST be used verbatim:
```
CREATE EXTENSION IF NOT EXISTS btree_gist;
ALTER TABLE model_pricing ADD CONSTRAINT model_pricing_no_overlap
  EXCLUDE USING gist (model WITH =, token_kind WITH =,
    tstzrange(effective_from, effective_to, '[)') WITH &&)
  WHERE (pricing_kind = 'per_token');
```
Proven: two non-overlapping rows insert OK; an overlapping insert raises `conflicting key value violates exclusion constraint`; selection at 2026-08-15 → old price, at 2026-10-15 → new price.

From EVIDENCE C33 — the last Phase 1 migration is `20260531000007_seed_phase1_test_data.sql`; Phase 2 continues the sequence at 008. From EVIDENCE C17/C16/Q16 — local PG is 17.10 (EXCLUDE proven); staging Supabase PG version is UNVERIFIABLE here but ≥15 assumed and EXCLUDE works on 15-17 (the `WITHOUT OVERLAPS` PG18 temporal PK is NOT to be used).

From EVIDENCE C41 (REFUTED SPEC figures — use these CORRECTED values): Copilot Pro = $10/mo (usage-based since 2026-06-01; flat line is a documented simplification, NOT $19), ChatGPT Plus = $20/mo (NOT "ChatGPT Pro $20"). Per-token seed prices (C37/C38, volatile — RE-VERIFY at build per Q12/Q15): Opus 4.8 $5/$25 input/output per MTok (cache write 1.25×, cache read 0.1× base input → $6.25/$0.50); Sonnet 5 intro $2/$10 through 2026-08-31, $3/$15 from 2026-09-01; Haiku 4.5 $1/$5.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrations 008-012 — five analysis tables (org_id + RLS + partitioning/EXCLUDE)</name>
  <files>supabase/migrations/20260531000008_prompt_outcomes.sql, supabase/migrations/20260531000009_model_fit_scores.sql, supabase/migrations/20260531000010_model_pricing.sql, supabase/migrations/20260531000011_daily_rollups_by_user.sql, supabase/migrations/20260531000012_daily_rollups_by_project.sql, supabase/migrations/README.md</files>
  <read_first>
    - supabase/migrations/20260531000002_ai_events_partitioned.sql (partition + PK + index idiom for 008/009 — S6)
    - supabase/migrations/20260531000003_git_events_partitioned.sql (CHECK-constraint idiom + gen_random_uuid default)
    - supabase/migrations/20260531000006_rls_policies.sql (EXACT RLS idiom for all five tables — S5, EVIDENCE C32)
    - supabase/migrations/20260531000001_orgs_users_keys.sql (plain-table style for 011/012 — projects table has the org_id + FK shape to mirror)
    - .planning/phases/02-parallel-adapters-backend-analysis-layer/02-EVIDENCE.md (C16 btree_gist EXCLUDE proof; C21 git_events cols; C33 timestamp sequence)
    - .planning/phases/02-parallel-adapters-backend-analysis-layer/02-PATTERNS.md (§Pattern Assignments 008-012 — exact column lists; S5/S6)
  </read_first>
  <action>
Author five migration files continuing the `20260531...` sequence. Each table gets `org_id UUID NOT NULL`, and each migration ENDS with the RLS block copied verbatim from `20260531000006` (`ENABLE ROW LEVEL SECURITY` + `CREATE POLICY <table>_tenant_isolation ON <table> USING (org_id = (auth.jwt() ->> 'org_id')::uuid);`) — D2-28.

`20260531000008_prompt_outcomes.sql` (per-prompt, partitioned by occurred_at per S6): columns `org_id UUID NOT NULL`, `prompt_idempotency_key TEXT NOT NULL` (the correlated prompt event's key), `occurred_at TIMESTAMPTZ NOT NULL` (partition col, = the prompt's occurred_at), `git_event_id UUID NULL` (nullable link — D2-17 null-link rows exist), `confidence_band TEXT NOT NULL CHECK (confidence_band IN ('low','medium','high'))`, `confidence_low NUMERIC NOT NULL CHECK (confidence_low >= 0 AND confidence_low <= 1)`, `confidence_high NUMERIC NOT NULL CHECK (confidence_high >= 0 AND confidence_high <= 1)`, a table-level `CHECK (confidence_low <= confidence_high)` (this pair-of-bounds CHECK is the ANL-02 "reject a bare single number" enforcement — D2-18), `attribution_state TEXT NOT NULL DEFAULT 'attributed' CHECK (attribution_state IN ('attributed','downgraded_by_revert','no_outcome'))`, `downgraded_at TIMESTAMPTZ NULL`, `downgrade_reason TEXT NULL` (D2-19). PK `(prompt_idempotency_key, occurred_at)` (idempotent-upsert target for the correlation worker — D2-15/S7). `PARTITION BY RANGE (occurred_at)` with current + next month partitions matching the ai_events window (`_2026_05` FROM '2026-05-01' TO '2026-06-01', `_2026_06` FROM '2026-06-01' TO '2026-07-01'). Add `CREATE INDEX ... (org_id, occurred_at)`.

`20260531000009_model_fit_scores.sql` (per-prompt, partitioned by occurred_at): columns `org_id UUID NOT NULL`, `prompt_idempotency_key TEXT NOT NULL`, `occurred_at TIMESTAMPTZ NOT NULL`, `score NUMERIC NOT NULL`, `fit_verdict TEXT NOT NULL CHECK (fit_verdict IN ('under_powered','fit','over_powered'))`, and the persisted signals `prompt_length INTEGER NOT NULL`, `file_edit_size INTEGER NOT NULL`, `tool_call_count INTEGER NOT NULL`, `model TEXT NOT NULL`, `model_tier TEXT` (D2-21 — score AND signals AND verdict). PK `(prompt_idempotency_key, occurred_at)`. Same partition + index shape as 008.

`20260531000010_model_pricing.sql` (PLAIN reference table — RESEARCH E13.2): `CREATE EXTENSION IF NOT EXISTS btree_gist;` at top. Columns `id UUID NOT NULL DEFAULT gen_random_uuid()`, `org_id UUID NOT NULL` (D2-28 stamps all five; document in a comment that pricing is reference data — the RLS policy is present for the belt-and-suspenders posture; seed rows use a well-known GLOBAL/system org_id constant so all tenants read the same prices — record the chosen sentinel in the SUMMARY), `model TEXT NOT NULL`, `pricing_kind TEXT NOT NULL CHECK (pricing_kind IN ('per_token','subscription'))` (D2-27 leaner same-table discriminator, per Q7), `token_kind TEXT NULL CHECK (token_kind IS NULL OR token_kind IN ('input','output','cache_creation','cache_read'))`, `price_per_mtok NUMERIC NULL` (per-token rows), `monthly_price NUMERIC NULL` (subscription rows), `tier TEXT NULL CHECK (tier IS NULL OR tier IN ('frontier','mid','small'))` (the model→tier lookup lives in this column per D2-22 — a model's tier rows carry it), `effective_from TIMESTAMPTZ NOT NULL`, `effective_to TIMESTAMPTZ NULL`. Add the PROVEN non-overlap constraint EXACTLY as EVIDENCE C16: `ALTER TABLE model_pricing ADD CONSTRAINT model_pricing_no_overlap EXCLUDE USING gist (model WITH =, token_kind WITH =, tstzrange(effective_from, effective_to, '[)') WITH &&) WHERE (pricing_kind = 'per_token');`. Add a partial index on `(model, token_kind, effective_from)` for the aggregator's effective-date lookup. RLS block at the end.

`20260531000011_daily_rollups_by_user.sql` (PLAIN — RESEARCH E13.2): columns `org_id UUID NOT NULL`, `user_id UUID NULL`, `day DATE NOT NULL`, `event_count INTEGER NOT NULL DEFAULT 0`, `cost_estimated NUMERIC NOT NULL DEFAULT 0` (always populated — D2-24/D2-25), `cost_billed NUMERIC NULL` (NULL until reconciliation — D2-25, NEVER defaulted to 0), `cost_subscription NUMERIC NOT NULL DEFAULT 0` (distinct field, D2-27 — never summed into cost_estimated). UNIQUE `(org_id, user_id, day)` — the aggregator's ON CONFLICT target (S7). RLS block.

`20260531000012_daily_rollups_by_project.sql`: same as 011 but `project_id UUID NOT NULL` instead of user_id, UNIQUE `(org_id, project_id, day)`. RLS block.

Update `supabase/migrations/README.md`: add a section listing the five new Phase-2 tables (008-013), noting that `model_pricing` requires the `btree_gist` extension (verify staging permits `CREATE EXTENSION btree_gist` per Q11/Q16 — record the staging PG version in the SUMMARY), and that prices are DATA (seeded in 013), never hardcoded in workers (ANL-08).

Do NOT write any worker/query code here — that is Plans 06/07. Do NOT push yet — that is Task 3.
  </action>
  <verify>
    <automated>cd /Users/Tanmai.N/Documents/fennec && ls supabase/migrations/20260531000008_prompt_outcomes.sql supabase/migrations/20260531000009_model_fit_scores.sql supabase/migrations/20260531000010_model_pricing.sql supabase/migrations/20260531000011_daily_rollups_by_user.sql supabase/migrations/20260531000012_daily_rollups_by_project.sql && grep -l "ENABLE ROW LEVEL SECURITY" supabase/migrations/2026053100000{8,9}_*.sql supabase/migrations/2026053100001{0,1,2}_*.sql | wc -l | grep -q 5 && grep -c "EXCLUDE USING gist" supabase/migrations/20260531000010_model_pricing.sql</automated>
  </verify>
  <acceptance_criteria>
    - All five migration files exist with the `20260531000008..012` prefixes.
    - `grep -c "confidence_band" supabase/migrations/20260531000008_prompt_outcomes.sql` ≥1 AND a table-level `CHECK (confidence_low <= confidence_high)` is present (ANL-02 interval enforcement).
    - `grep -c "attribution_state" supabase/migrations/20260531000008_prompt_outcomes.sql` ≥1 with the three-value CHECK incl. `downgraded_by_revert` (ANL-03).
    - `grep -c "fit_verdict" supabase/migrations/20260531000009_model_fit_scores.sql` ≥1 and all five signal columns (prompt_length, file_edit_size, tool_call_count, model, model_tier) present (ANL-04).
    - `grep -c "cost_subscription" supabase/migrations/20260531000011_daily_rollups_by_user.sql` ≥1 and `cost_billed` is NULLable with no DEFAULT 0 (ANL-07/ANL-09).
    - `grep -c "EXCLUDE USING gist" supabase/migrations/20260531000010_model_pricing.sql` = 1 and `pricing_kind` discriminator present (ANL-08/D2-27).
    - Each of the five migrations contains exactly one `ENABLE ROW LEVEL SECURITY` + one `CREATE POLICY ..._tenant_isolation` (D2-28).
  </acceptance_criteria>
  <done>Five migrations authored (008-012): two partitioned per-prompt tables, model_pricing with the proven EXCLUDE non-overlap, two plain rollup tables with distinct cost columns; every table org_id + RLS; README updated. No worker code, no push yet.</done>
</task>

<task type="auto">
  <name>Task 2: Migration 013 — idempotent model_pricing + subscription + tier seed</name>
  <files>supabase/migrations/20260531000013_seed_model_pricing.sql</files>
  <read_first>
    - supabase/migrations/20260531000007_seed_phase1_test_data.sql (EXACT idempotent-seed idiom — every INSERT ... ON CONFLICT DO NOTHING)
    - supabase/migrations/20260531000010_model_pricing.sql (the table + column names authored in Task 1)
    - .planning/phases/02-parallel-adapters-backend-analysis-layer/02-EVIDENCE.md (C37/C38 per-token seed prices; C41 CORRECTED subscription figures; C16 the Sonnet-5 cutover exercises the EXCLUDE machinery)
    - .planning/phases/02-parallel-adapters-backend-analysis-layer/02-OPEN-QUESTIONS.md (Q12/Q15 — prices are volatile; RE-VERIFY at build)
  </read_first>
  <action>
Author `20260531000013_seed_model_pricing.sql` seeding `model_pricing` with `ON CONFLICT DO NOTHING` on a stable conflict key (use fixed UUIDs in the `id` column as `20260531000007` does, so re-running is idempotent). Add a top-of-file comment: "PRICES ARE VOLATILE — re-verify all figures at build per Q12/Q15; these are DATA not code (ANL-08). Copilot Pro moved to usage-based billing 2026-06-01 — the flat subscription line is a documented simplification (EVIDENCE C41)." Use the GLOBAL/system org_id sentinel chosen in Task 1.

Per-token rows (`pricing_kind='per_token'`), one row per (model, token_kind) with `price_per_mtok`, `tier`, and `effective_from`/`effective_to`:
- Claude Opus 4.8 (tier frontier): input $5, output $25, cache_creation $6.25, cache_read $0.50 per MTok; `effective_from '2026-01-01'`, `effective_to NULL`.
- Claude Sonnet 5 (tier frontier) — seed BOTH effective-date rows per token_kind to exercise the EXCLUDE non-overlap constraint live (EVIDENCE C16): intro rows `effective_from '2026-01-01' effective_to '2026-09-01'` (input $2, output $10), then `effective_from '2026-09-01' effective_to NULL` (input $3, output $15). Include cache_creation/cache_read rows for at least the current window.
- Claude Haiku 4.5 (tier small): input $1, output $5; effective_from '2026-01-01', effective_to NULL.
- (Optional, if trivially derivable from C38) a GPT frontier + a GPT small tier row so the model→tier lookup covers the OpenAI surface.

Subscription rows (`pricing_kind='subscription'`, `token_kind NULL`, `monthly_price`): GitHub Copilot Pro `monthly_price 10.00` (EVIDENCE C41 — NOT $19), ChatGPT Plus `monthly_price 20.00` (EVIDENCE C41 — NOT "ChatGPT Pro $20"). `effective_from '2026-06-01' effective_to NULL`.

Ensure the two Sonnet-5 per-token windows are NON-OVERLAPPING (`[effective_from, effective_to)` half-open) so the `model_pricing_no_overlap` EXCLUDE accepts them — this is the deliberate live test of the constraint (C16).
  </action>
  <verify>
    <automated>cd /Users/Tanmai.N/Documents/fennec && ls supabase/migrations/20260531000013_seed_model_pricing.sql && grep -c "ON CONFLICT" supabase/migrations/20260531000013_seed_model_pricing.sql && grep -c "monthly_price" supabase/migrations/20260531000013_seed_model_pricing.sql && grep -Eiq "10\.00|10\b" supabase/migrations/20260531000013_seed_model_pricing.sql</automated>
  </verify>
  <acceptance_criteria>
    - `20260531000013_seed_model_pricing.sql` exists; every INSERT uses `ON CONFLICT ... DO NOTHING`.
    - Per-token rows exist for all four token_kinds (input/output/cache_creation/cache_read) for at least one model — `grep -c "cache_creation" supabase/migrations/20260531000013_seed_model_pricing.sql` ≥1 AND `grep -c "cache_read" ...` ≥1.
    - TWO non-overlapping Sonnet-5 effective-date windows are seeded (the `effective_to '2026-09-01'` row and the `effective_from '2026-09-01'` row both present).
    - Subscription rows present with `monthly_price` 10.00 (Copilot Pro) and 20.00 (ChatGPT Plus) — NOT $19 (C41 correction applied).
    - A top-of-file comment flags prices as volatile DATA to re-verify at build (Q12/Q15).
  </acceptance_criteria>
  <done>Seed migration authored: four-token-kind per-token prices, both Sonnet-5 cutover rows (exercises EXCLUDE), Copilot Pro $10 + ChatGPT Plus $20 subscriptions, idempotent ON CONFLICT, volatility note.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] supabase db push — apply 008-013 to staging</name>
  <files>supabase/migrations/20260531000008_prompt_outcomes.sql, supabase/migrations/20260531000009_model_fit_scores.sql, supabase/migrations/20260531000010_model_pricing.sql, supabase/migrations/20260531000011_daily_rollups_by_user.sql, supabase/migrations/20260531000012_daily_rollups_by_project.sql, supabase/migrations/20260531000013_seed_model_pricing.sql</files>
  <action>
Apply migrations 008-013 to the live staging Supabase Postgres. Ensure `SUPABASE_ACCESS_TOKEN` is exported (non-TTY) and the project is linked, then run `supabase db push` (or the existing `scripts/db-push.sh` wrapper from Plan 01-10). This is a BLOCKING human-verified gate: `npm run typecheck`/build pass WITHOUT the push (backend types come from config, not the live DB), so verification of Plans 06/07 against real tables is a false-positive until these tables physically exist in staging. If staging blocks `CREATE EXTENSION btree_gist`, record it and fall back to enforcing model_pricing non-overlap via a test only (Q11/Q16); note the staging PG version. Confirm the Sonnet-5 two-window seed does NOT raise the `model_pricing_no_overlap` exclusion error (proves the EXCLUDE + half-open ranges are correct — EVIDENCE C16 live). This task writes NO source — it only applies the .sql authored in Tasks 1-2.
  </action>
  <verify>
    <automated>cd /Users/Tanmai.N/Documents/fennec && supabase migration list 2>&1 | tail -20</automated>
    <human-check>supabase db push applied 008-013 to staging with exit 0; the five tables + seed rows are confirmed present via psql spot-check.</human-check>
  </verify>
  <done>Migrations 008-013 applied to staging; the five analysis tables + model_pricing seed exist and are queryable; staging PG version + btree_gist availability recorded in the SUMMARY.</done>
  <what-built>
Six migrations (008-013) authored in Tasks 1-2 creating the five analysis tables + seed. This task APPLIES them to the live staging Supabase Postgres. It is mandatory and blocking: `npm run typecheck`/build pass WITHOUT the push because backend types come from config not the live DB — verification of Plans 06/07 against real tables is a false-positive until these tables physically exist in staging.
  </what-built>
  <how-to-verify>
1. Ensure `SUPABASE_ACCESS_TOKEN` is exported (non-TTY) and the project is linked (or run via the existing `scripts/db-push.sh` wrapper from Plan 01-10).
2. Run `supabase db push` (or `scripts/db-push.sh`). Watch for the btree_gist step — if staging blocks `CREATE EXTENSION btree_gist`, record it and fall back to enforcing non-overlap via a test only (Q11/Q16); note the staging PG version.
3. Confirm the six migrations apply cleanly and the Sonnet-5 two-window seed does NOT raise the `model_pricing_no_overlap` exclusion error (proves the EXCLUDE + half-open ranges are correct — EVIDENCE C16 live).
4. Spot-check: `select count(*) from model_pricing;` returns the seeded rows; `select confidence_band from prompt_outcomes limit 0;` and `select fit_verdict from model_fit_scores limit 0;` and `select cost_subscription from daily_rollups_by_user limit 0;` all resolve (columns exist).
Record the staging PG version + btree_gist availability + row counts in the SUMMARY.
  </how-to-verify>
  <acceptance_criteria>
    - `supabase db push` applies migrations 008-013 to staging with exit 0.
    - The two Sonnet-5 effective-date rows seed without an exclusion-constraint error (non-overlap ranges correct).
    - `model_pricing`, `prompt_outcomes`, `model_fit_scores`, `daily_rollups_by_user`, `daily_rollups_by_project` all exist with the interval/verdict/cost columns queryable.
    - Staging PG version + btree_gist availability recorded in the SUMMARY.
  </acceptance_criteria>
  <resume-signal>Type "applied" once `supabase db push` succeeds and the five tables + seed are confirmed in staging, or describe the push failure (e.g. btree_gist blocked → fall-back-to-test disposition).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker → new analysis tables | Plans 06/07 workers derive org_id from the source ai_events row and write these tables; RLS is the backstop |
| tenant → row visibility | RLS policy must gate every row by the JWT org_id claim (Phase 3 activates user-scoped tokens; policy declared now so it cannot be retrofitted) |
| pricing rows → cost math | model_pricing is the single source of truth; a tampered/overlapping price row would corrupt cost_estimated |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Information disclosure | Cross-tenant read of prompt_outcomes / model_fit_scores / rollups | HIGH | mitigate | RLS `tenant_isolation` policy in EACH creation migration mirroring 20260531000006 (Task 1); belt-and-suspenders per D2-28/D-26 |
| T-04-02 | Tampering | Overlapping / bogus effective-date price rows corrupt cost math | HIGH | mitigate | `EXCLUDE USING gist` non-overlap constraint on model_pricing (PROVEN C16); prices are seed DATA with fixed-UUID ON CONFLICT keys (Task 2) |
| T-04-03 | Tampering | prompt_outcomes confidence stored as a bare single number (ANL-02 anti-pattern) | MEDIUM | mitigate | table-level `CHECK (confidence_low <= confidence_high)` + both-bounds-NOT-NULL forces an interval; a single number cannot satisfy it (Task 1) |
| T-04-04 | Repudiation | Revert silently decrements a total (ANL-03 anti-pattern) | MEDIUM | mitigate | schema forces an explicit `attribution_state='downgraded_by_revert'` + downgraded_at/downgrade_reason recorded transition — no aggregate column to decrement (Task 1) |
| T-04-05 | Tampering | Migration false-positive: build passes but tables never applied | HIGH | mitigate | mandatory `[BLOCKING]` `supabase db push` (Task 3) before Plan 06/07 verification |
| T-04-SC | Tampering | New DB extension (btree_gist) enabled on staging | LOW | accept | btree_gist is a stock Postgres contrib extension (PROVEN on PG17.10 C16); Supabase permits it; no npm/package install involved |
</threat_model>

<verification>
- All six migration files present with the 008-013 timestamp prefixes (EVIDENCE C33 sequence continued).
- Grep gates: RLS on all five tables; EXCLUDE on model_pricing; confidence_band/attribution_state/fit_verdict/cost_subscription present; ON CONFLICT in the seed.
- `[BLOCKING]` `supabase db push` applied to staging with the Sonnet-5 two-window seed accepted by the EXCLUDE constraint.
- No worker/query source written in this plan (docs-only guard: only .sql + README).
</verification>

<success_criteria>
- Five analysis tables created, each org_id + RLS from its creation migration (D2-28).
- prompt_outcomes encodes interval confidence (ANL-02) + revert-downgrade state (ANL-03); model_fit_scores encodes score + signals + verdict (ANL-04).
- Rollups carry distinct cost_estimated / cost_billed(null) / cost_subscription (ANL-07/ANL-09).
- model_pricing is the single source of truth with a proven non-overlap effective-date constraint (ANL-08); seed is DATA with the C41-corrected subscription prices.
- Migrations pushed to staging (blocking gate satisfied).
</success_criteria>

<output>
Create `.planning/phases/02-parallel-adapters-backend-analysis-layer/02-04-SUMMARY.md` — record the global/system org_id sentinel chosen for pricing rows, the staging PG version + btree_gist availability confirmed at push, the re-verified per-token/subscription prices (or note that the seeded 2026-07-01 figures were kept), and any push fallback (test-only non-overlap) if btree_gist was blocked.
</output>
