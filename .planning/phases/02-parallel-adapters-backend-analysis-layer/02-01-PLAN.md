---
phase: 02-parallel-adapters-backend-analysis-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - daemon/package.json
  - backend/package.json
  - packages/shared/src/events/codex-payload.ts
  - packages/shared/src/events/gemini-payload.ts
  - packages/shared/src/events/cursor-payload.ts
  - packages/shared/src/events/copilot-payload.ts
  - packages/shared/src/events/browser-payload.ts
  - packages/shared/src/events/git-payload.ts
  - packages/shared/src/analysis/prompt-outcome.ts
  - packages/shared/src/analysis/model-fit.ts
  - packages/shared/src/analysis/rollups.ts
  - packages/shared/src/index.ts
  - daemon/src/adapters/loopback-bridge/server.ts
  - daemon/src/adapters/loopback-bridge/server.test.ts
  - daemon/src/adapters/bridge-events/adapter.ts
  - daemon/src/adapters/bridge-events/adapter.test.ts
  - daemon/src/adapters/bridge-events/bridge-events-normaliser.ts
  - daemon/src/adapters/bridge-events/bridge-events-normaliser.test.ts
  - backend/src/api/events-batch.ts
  - backend/src/db/queries/git-events.ts
  - backend/src/db/queries/git-events.test.ts
autonomous: true
requirements: [CAP-06, CAP-07, CAP-08, CAP-09]

must_haves:
  truths:
    - "chokidar@^5.0.0 is a daemon dependency so the Wave 2 file-watch adapters can build (D2-02)"
    - "The loopback bridge accepts POST /v1/events authenticated by the pairing-token header and emits an `events` event (D2-07/D2-08/D2-09)"
    - "A daemon-side bridge-events adapter normalises out-of-process posts into tool=copilot|chatgpt-web|claude-ai-web (D2-09)"
    - "Ingest routes tool=git events into git_events and all other tools into ai_events via a dumb tool-discriminator branch, preserving ING-04 hot-path purity (Q13)"
    - "Per-tool Zod payload validators and analysis row types exist in @fennec/shared with no schema_version bump"
  artifacts:
    - path: "daemon/src/adapters/loopback-bridge/server.ts"
      provides: "POST /v1/events route mirroring /v1/hook, emits `events`"
      contains: "/v1/events"
    - path: "daemon/src/adapters/bridge-events/adapter.ts"
      provides: "BridgeEventsAdapter subscribing to bridge `events`"
      contains: "class BridgeEventsAdapter implements Adapter"
    - path: "backend/src/db/queries/git-events.ts"
      provides: "insertGitEvent parameterized upsert into git_events"
      exports: ["insertGitEvent"]
    - path: "packages/shared/src/analysis/prompt-outcome.ts"
      provides: "PromptOutcome row type + confidence/attribution-state Zod shapes"
      contains: "confidence_band"
  key_links:
    - from: "daemon/src/adapters/loopback-bridge/server.ts"
      to: "bridge-events adapter"
      via: "this.emit('events', parsed)"
      pattern: "emit\\(\"events\""
    - from: "backend/src/api/events-batch.ts"
      to: "git_events table"
      via: "tool-discriminator branch → insertGitEvent"
      pattern: "insertGitEvent"
---

<objective>
Lay the additive contracts every other Phase 2 plan depends on: add the missing `chokidar` daemon dependency and `@cloudflare/vitest-pool-workers` backend dev dependency; author the per-tool Zod payload validators + analysis row types in `@fennec/shared`; add the `POST /v1/events` route to the loopback bridge plus a daemon-side bridge-events normalising adapter for the out-of-process Copilot/browser clients; and resolve the git-transport fork by routing `tool: "git"` events through the standard ingest path into `git_events` with a dumb tool-discriminator branch (Q13 default, ING-04-safe).

Purpose: This is the interface-first plan. Wave 2 adapters import the shared payload validators; the queue/correlation/aggregator workers import the analysis row types; the Copilot sidecar + browser extension POST to `/v1/events`; the git-watcher emits `tool: "git"` events the ingest must route to `git_events`. Defining these contracts here prevents downstream scavenger hunts.

Output: chokidar in daemon deps, vitest-pool-workers in backend dev deps, six new payload validator files + three analysis row-type files + barrel exports in `@fennec/shared`, the `/v1/events` bridge route, the `BridgeEventsAdapter`, and the ingest git-branch + `insertGitEvent` query.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-parallel-adapters-backend-analysis-layer/02-CONTEXT.md
@.planning/phases/02-parallel-adapters-backend-analysis-layer/02-RESEARCH.md
@.planning/phases/02-parallel-adapters-backend-analysis-layer/02-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase; use directly, do not re-explore. -->

From daemon/src/adapters/adapter.ts (EmitInput the new adapters supply; registry stamps idempotency_key/hostname/os/redaction):
```typescript
export interface EmitInput {
  tool: Tool; adapter_version: string; kind: EventKind;
  payload: Record<string, unknown>; session_id: string; hook_event: string;
  occurred_at?: string; monotonic_seq?: number;
  cwd?: string; git_remote?: string; git_branch?: string;
}
export type Emit = (event: EmitInput) => Promise<void>;
export interface Adapter { readonly tool: Tool; readonly version: string; start(emit: Emit): Promise<void>; stop(): Promise<void>; }
```

From packages/shared/src/events/canonical.ts — ToolSchema ALREADY enumerates all 8 tools (claude-code, codex, gemini, cursor, copilot, chatgpt-web, claude-ai-web, git); CanonicalEventSchema.schema_version is z.literal(1) — DO NOT bump it.

From packages/shared/src/events/claude-code-payload.ts (copy this shape for the new payload validators; reuse AnthropicUsageSchema where token fields apply):
```typescript
export const AnthropicUsageSchema = z.object({ input_tokens, output_tokens, cache_creation_input_tokens, cache_read_input_tokens }).partial();
export const ClaudeCodePromptPayloadSchema = z.object({ prompt_text: z.string(), session_id: z.string(), cwd: z.string().optional(), hook_event: ..., usage: AnthropicUsageSchema.optional() });
```

From daemon/src/adapters/loopback-bridge/server.ts — `handleHookPost` (lines 121-172): checks `req.headers["x-fennec-shim-secret"]` BEFORE body read (401), buffers chunks, JSON.parse on end (400, never echoes body), `this.emit("hook", parsed)`, writeHead(202). The route table is `handleRequest` (lines 102-119). Binds 127.0.0.1 only.

From daemon/src/adapters/claude-code/adapter.ts — the EventEmitter-subscriber Adapter shape to copy for BridgeEventsAdapter (subscribe to "events" instead of "hook"; store handler ref for stop() unsubscribe; void this.forward(raw).catch(log)).

From backend/src/api/events-batch.ts — current ingest loop builds InsertableAiEvent + insertAiEvent per event. org_id from c.get("org_id"), NEVER request body (T-05-02). The git-branch is added inside this loop.

From backend/src/db/queries/ai-events.ts — insertAiEvent(client, row) shape + parameterized $1..$N pattern to mirror for insertGitEvent.

From supabase/migrations/20260531000003_git_events_partitioned.sql — git_events columns: id, org_id, user_id, occurred_at, repo_remote, repo_branch, event_type CHECK IN (commit|revert|file_edit|branch_switch), payload, schema_version. PK (id, occurred_at). NO idempotency_key/tool/kind columns.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add deps + author @fennec/shared payload validators and analysis row types</name>
  <files>daemon/package.json, backend/package.json, packages/shared/src/events/codex-payload.ts, packages/shared/src/events/gemini-payload.ts, packages/shared/src/events/cursor-payload.ts, packages/shared/src/events/copilot-payload.ts, packages/shared/src/events/browser-payload.ts, packages/shared/src/events/git-payload.ts, packages/shared/src/analysis/prompt-outcome.ts, packages/shared/src/analysis/model-fit.ts, packages/shared/src/analysis/rollups.ts, packages/shared/src/index.ts</files>
  <read_first>
    - packages/shared/src/events/claude-code-payload.ts (EXACT analog for payload validators — copy z.object shape + AnthropicUsageSchema reuse)
    - packages/shared/src/events/canonical.ts (ToolSchema + schema_version literal — do NOT change)
    - packages/shared/src/index.ts (barrel `export *` list to extend)
    - .planning/phases/02-parallel-adapters-backend-analysis-layer/02-EVIDENCE.md (C18/C19/C20 — no schema_version bump; C13/C14 chokidar; C35 vitest-pool-workers absent)
  </read_first>
  <action>
Add `"chokidar": "^5.0.0"` to `daemon/package.json` dependencies (per D2-02; EVIDENCE C13 proves it installs/imports on Node 22 ESM, C14 confirms it is currently absent). Add `"@cloudflare/vitest-pool-workers"` to `backend/package.json` devDependencies (pin to a version compatible with `wrangler@4.93.1` + `@cloudflare/workers-types@4.20260531.1` already present; if the corp-proxy blocks the install, document the mocked-env fallback in the SUMMARY per 02-VALIDATION Wave 0 note — the Worker handlers in Plans 06/07 stay testable as plain functions with a mocked Env either way). Run the workspace install so lockfiles update.

Author the per-tool payload validators (one file each), copying `claude-code-payload.ts`'s shape — `z.object({ prompt_text: z.string(), session_id: z.string(), ... })`, runtime-neutral (import `zod` only, NO `node:*` so Workers consume them):
- `codex-payload.ts` → `CodexPromptPayloadSchema` with `prompt_text`, `session_id`, optional `model`, optional `usage: AnthropicUsageSchema.optional()`.
- `gemini-payload.ts` → `GeminiPromptPayloadSchema`, same shape; `usage` optional (Gemini tokens best-effort/absent per Q9/A2.4).
- `cursor-payload.ts` → `CursorPromptPayloadSchema` with `prompt_text`, `session_id` (the composerId), optional `model`, optional `usage`.
- `copilot-payload.ts` → `CopilotPromptPayloadSchema` with `prompt_text`, `session_id`, optional `model` (Copilot carries NO token usage per EVIDENCE C10 — no `usage` field).
- `browser-payload.ts` → `BrowserPromptPayloadSchema` (shared by chatgpt-web + claude-ai-web) with `prompt_text`, `session_id` (conversation id), optional `model`, optional `usage`.
- `git-payload.ts` → `GitEventPayloadSchema` for the git-watcher's payload: `event_type: z.enum(["commit","revert","file_edit","branch_switch"])`, optional `subject`, optional `sha`, optional `reflog_message`. (This validates the git-watcher's emitted payload; the git transport is resolved in Task 3.)

Author the analysis row types under a NEW `packages/shared/src/analysis/` dir (consumed by the Plan 06/07 workers now and the Phase 4 frontend later), following `canonical.ts`'s Zod+type-export shape:
- `prompt-outcome.ts` → `PromptOutcomeSchema` with `org_id`, `prompt_event_id`/`idempotency_key`, `git_event_id` (nullable), `confidence_band: z.enum(["low","medium","high"])`, `confidence_low: z.number().min(0).max(1)`, `confidence_high: z.number().min(0).max(1)`, `attribution_state: z.enum(["attributed","downgraded_by_revert","no_outcome"])`, `downgraded_at` (nullable), `downgrade_reason` (nullable). A refine asserts confidence_low <= confidence_high. This is the ANL-02 interval shape (D2-18) and the ANL-03 attribution-state shape (D2-19).
- `model-fit.ts` → `ModelFitScoreSchema` with `score: z.number()`, `fit_verdict: z.enum(["under_powered","fit","over_powered"])`, `prompt_length`, `file_edit_size`, `tool_call_count`, `model`, `model_tier` (D2-21 persisted signals).
- `rollups.ts` → `DailyRollupByUserSchema` + `DailyRollupByProjectSchema` with `org_id`, `user_id`/`project_id`, `day` (date string), `event_count`, `cost_estimated`, `cost_billed` (nullable), `cost_subscription` (D2-25/D2-27 separate fields).

Extend `packages/shared/src/index.ts` barrel: add `export * from "./events/<tool>-payload.js";` for all six new payload files and `export * from "./analysis/prompt-outcome.js";` / `model-fit.js` / `rollups.js`. Do NOT touch `CanonicalEventSchema.schema_version` — it stays `z.literal(1)` (D2-06, EVIDENCE C19).
  </action>
  <verify>
    <automated>cd /Users/Tanmai.N/Documents/fennec && node -e "const d=require('./daemon/package.json');if(!d.dependencies.chokidar)throw new Error('chokidar missing');console.log('chokidar',d.dependencies.chokidar)" && npm run typecheck && npx vitest run packages/shared</automated>
  </verify>
  <acceptance_criteria>
    - `daemon/package.json` dependencies includes `chokidar` at `^5.0.0`.
    - `backend/package.json` devDependencies includes `@cloudflare/vitest-pool-workers` (OR the SUMMARY documents the corp-proxy-blocked mocked-env fallback).
    - `packages/shared/src/analysis/prompt-outcome.ts` exports `PromptOutcomeSchema` containing `confidence_band`, `confidence_low`, `confidence_high`, `attribution_state`.
    - `grep -c "z.literal(1)" packages/shared/src/events/canonical.ts` returns 1 (unchanged — no schema_version bump).
    - `npm run typecheck` exits 0; `npx vitest run packages/shared` exits 0.
  </acceptance_criteria>
  <done>chokidar + vitest-pool-workers declared; six payload validators + three analysis row types authored and barrel-exported; typecheck + shared tests green; schema_version untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add POST /v1/events bridge route + BridgeEventsAdapter</name>
  <files>daemon/src/adapters/loopback-bridge/server.ts, daemon/src/adapters/loopback-bridge/server.test.ts, daemon/src/adapters/bridge-events/adapter.ts, daemon/src/adapters/bridge-events/adapter.test.ts, daemon/src/adapters/bridge-events/bridge-events-normaliser.ts, daemon/src/adapters/bridge-events/bridge-events-normaliser.test.ts</files>
  <read_first>
    - daemon/src/adapters/loopback-bridge/server.ts (self-analog — copy handleHookPost verbatim, emit "events"; EVIDENCE C23/C24)
    - daemon/src/adapters/loopback-bridge/server.test.ts (existing bridge test shape to extend for the new route)
    - daemon/src/adapters/claude-code/adapter.ts (EXACT analog for BridgeEventsAdapter — EventEmitter subscriber, S1/S2/S4)
    - daemon/src/adapters/claude-code/payload-normaliser.ts (S3 — throw-on-malformed normaliser shape, pick<T> helper, frozen kind map)
    - packages/shared/src/events/copilot-payload.ts (CopilotPromptPayloadSchema authored in Task 1)
  </read_first>
  <action>
In `daemon/src/adapters/loopback-bridge/server.ts`, add a `POST /v1/events` branch to `handleRequest` (alongside the existing `/v1/hook` branch) that calls a new `handleEventsPost`. Copy `handleHookPost` (server.ts:121-172) VERBATIM into `handleEventsPost`, changing only `this.emit("hook", parsed)` → `this.emit("events", parsed)`. Preserve the security sequence exactly: validate the secret header BEFORE reading the body (401, log only `remoteAddr`, NEVER the secret/body), buffer chunks, `JSON.parse` in try/catch (400, never echo body), respond 202. `/v1/health` stays unchanged. Per D2-09 (Claude's Discretion), accept the pairing token under EITHER `x-fennec-shim-secret` (verbatim) OR a neutral `x-fennec-pairing-token` alias — pick one and document it; the trust model is identical (Pattern 9, threat T-02-04). Extend `server.test.ts` with cases asserting: a `/v1/events` POST with the correct header emits `events` and returns 202; a wrong/missing header returns 401 and does NOT emit; a malformed JSON body returns 400 and does NOT echo the body.

Create `daemon/src/adapters/bridge-events/` with:
- `bridge-events-normaliser.ts` — a pure `normalizeBridgeEvent(raw) => EmitInput` following the claude-code payload-normaliser shape (S3): a `pick<T>(obj, key)` helper, a frozen source→tool map, throw-on-malformed. The incoming bridge post carries a discriminator field (e.g. `source: "copilot" | "chatgpt-web" | "claude-ai-web"`); map it to `tool`, set `kind: "prompt_submitted"`, build `payload` validated by the matching Task-1 schema. For Copilot: strip EITHER `copilot/` OR `github.copilot-chat/` prefix from `modelId` to get the bare model, and TOLERATE a missing `modelId` (tag `unknown`, never throw) — per EVIDENCE C42. Leave `usage` undefined for Copilot (no token counts, C10). Throw if `source` is unknown or `prompt_text`/`session_id` is missing.
- `adapter.ts` — `class BridgeEventsAdapter implements Adapter` with `readonly tool` set per-instance OR a single adapter handling all three browser/copilot tools (since `Adapter.tool` is a single value, prefer the single-adapter-handles-many approach by setting `tool` from the normalised result rather than the interface field; if the registry requires a fixed `tool`, register one BridgeEventsAdapter that emits whatever `tool` the normaliser returns — document the chosen mechanism). Copy claude-code/adapter.ts: store the handler ref, subscribe to `bridge.on("events", handler)` in `start()`, `bridge.off("events", handler)` in `stop()`, `void this.forward(raw).catch(log)` swallow-on-malformed (S2/S4).
- co-located `*.test.ts` for both: normaliser maps copilot/chatgpt-web/claude-ai-web posts to the correct `tool` with `kind: "prompt_submitted"`; tolerates missing/multi-prefix `modelId`; throws on unknown source; the adapter forwards a valid `events` post to its emit and swallows a malformed one without throwing.

Do NOT wire the adapter into `daemon/src/cli/daemon.ts` here — Plan 02-08 (daemon adapter registration + wiring) registers it once the adapter classes exist, via a one-line `registry.register(new BridgeEventsAdapter(bridge))` add at daemon.ts step 7. (Keeping registration out of this plan makes daemon.ts owned solely by Plan 02-08, avoiding a file-ownership conflict with the adapter-building plans 02-02/02-03.)
  </action>
  <verify>
    <automated>cd /Users/Tanmai.N/Documents/fennec && npx vitest run daemon/src/adapters/loopback-bridge daemon/src/adapters/bridge-events && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "/v1/events" daemon/src/adapters/loopback-bridge/server.ts` returns ≥1.
    - `grep -c 'emit("events"' daemon/src/adapters/loopback-bridge/server.ts` returns ≥1.
    - `daemon/src/adapters/bridge-events/adapter.ts` contains `class BridgeEventsAdapter implements Adapter`.
    - `npx vitest run daemon/src/adapters/loopback-bridge daemon/src/adapters/bridge-events` exits 0, including a 401-on-bad-header case and a missing-modelId-tolerance case.
    - The bridge `/v1/events` 401-path test asserts the secret value never appears in any logged argument.
  </acceptance_criteria>
  <done>POST /v1/events added (mirrors /v1/hook security), bridge-events normaliser + adapter built with Copilot modelId tolerance, all bridge + adapter tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Resolve git transport — ingest routes tool=git into git_events (Q13)</name>
  <files>backend/src/api/events-batch.ts, backend/src/db/queries/git-events.ts, backend/src/db/queries/git-events.test.ts</files>
  <read_first>
    - backend/src/api/events-batch.ts (the ingest loop to add the tool-branch to — keep hot-path purity)
    - backend/src/api/events-batch.hot-path.test.ts (the ING-04 static-import guard that MUST keep passing — no correlation/model-fit/aggregator/analysis imports)
    - backend/src/db/queries/ai-events.ts (insertAiEvent analog — parameterized $1..$N, ON CONFLICT)
    - supabase/migrations/20260531000003_git_events_partitioned.sql (git_events columns + event_type CHECK + PK (id, occurred_at))
    - packages/shared/src/events/git-payload.ts (GitEventPayloadSchema from Task 1)
    - .planning/phases/02-parallel-adapters-backend-analysis-layer/02-OPEN-QUESTIONS.md (Q13 default — standard ingest path with dumb tool-branch)
  </read_first>
  <action>
Implement Q13's default transport: `tool: "git"` events ride the existing adapter → registry → JSONL queue → `POST /api/events/batch` path (the `git` tool value already exists in `ToolSchema`, EVIDENCE C18), and the ingest handler branches by tool — `git` → `git_events`, everything else → `ai_events`. This preserves ING-04 (one transport, one hot path) and is the locked Q13 choice; record in the SUMMARY that Option (b) separate-endpoint was rejected.

Create `backend/src/db/queries/git-events.ts` exporting `insertGitEvent(client, row)` mirroring `insertAiEvent`'s parameterized shape: INSERT into `git_events (id, org_id, user_id, occurred_at, repo_remote, repo_branch, event_type, payload, schema_version)` with `ON CONFLICT (id, occurred_at) DO NOTHING`. `event_type` is read from the validated git payload (`GitEventPayloadSchema`); `repo_remote`/`repo_branch` from the canonical envelope's `git_remote`/`git_branch`; `id` generated (use the event's idempotency_key as a deterministic id OR gen_random_uuid default — git_events has no idempotency_key column, so map the canonical idempotency_key to a stable id, e.g. via a deterministic UUID derivation, to keep retries idempotent). `org_id` from `c.get("org_id")` (T-05-02), `user_id` null pre-attach. All queries parameterized `$1..$N` (T-05-03). Co-locate `git-events.test.ts` with a mocked pg client (mirror `backend/src/test-utils/mock-db.ts`) asserting: a `commit`/`revert`/`file_edit`/`branch_switch` payload inserts with the matching `event_type`; an invalid `event_type` is rejected before the query (Zod validation); the query is parameterized (no string interpolation of values).

In `backend/src/api/events-batch.ts`, inside the existing per-event loop, add a dumb tool-discriminator: `if (event.tool === "git") { await insertGitEvent(client, gitRow); } else { await insertAiEvent(client, aiRow); }`. The `git` branch validates `event.payload` against `GitEventPayloadSchema` and builds the git row from the canonical envelope. CRITICAL (ING-04): import only `insertGitEvent` + the shared `GitEventPayloadSchema` — do NOT import any correlation/model-fit/aggregator/analysis module (the `events-batch.hot-path.test.ts` greps for those `from "..."` imports and MUST stay green; `insertGitEvent` is a db-query import, not an analytics import, so it is allowed). Keep `accepted` counting identical for both branches.
  </action>
  <verify>
    <automated>cd /Users/Tanmai.N/Documents/fennec && npx vitest run backend/src/api/events-batch.hot-path.test.ts backend/src/db/queries/git-events.test.ts backend/src/api/events-batch.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `backend/src/db/queries/git-events.ts` exports `insertGitEvent` and uses `ON CONFLICT (id, occurred_at) DO NOTHING`.
    - `backend/src/api/events-batch.ts` contains a `event.tool === "git"` branch calling `insertGitEvent`.
    - `npx vitest run backend/src/api/events-batch.hot-path.test.ts` exits 0 (all 4 ING-04 guard cases still pass — no analytics import added).
    - `git-events.test.ts` proves all four `event_type` values insert and an invalid `event_type` is rejected by Zod before the query.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>tool=git events route into git_events via insertGitEvent; ai_events path unchanged; ING-04 hot-path guard still green; git-events query tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| out-of-process client → loopback bridge | Browser extension / Copilot sidecar POST untrusted JSON to 127.0.0.1:/v1/events |
| daemon → backend ingest | Canonical events (incl. tool=git) cross to the Worker over bearer auth |
| request body → DB tenancy | org_id must come from auth context, never the client body |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04 | Spoofing | POST /v1/events forged by a cross-UID/external probe | HIGH | mitigate | Validate pairing-token header BEFORE body read (copy /v1/hook 401 path); bind 127.0.0.1 only; test asserts 401-no-emit (Task 2 verification) |
| T-02-05 | Information disclosure | Secret/body leaked via bridge logs on a bad /v1/events post | MEDIUM | mitigate | Log only remoteAddr+url, never the secret or body; test asserts secret absent from logged args (Task 2) |
| T-02-06 | Tampering | SQL injection via git_events insert | HIGH | mitigate | Parameterized `$1..$N` only in insertGitEvent (no string-concat); git-events.test asserts parameterization (Task 3) |
| T-02-07 | Spoofing | Client supplies org_id in body to write cross-tenant git rows | HIGH | mitigate | org_id from c.get("org_id"), never event body (T-05-02 reused); Task 3 builds git row with auth-context org_id |
| T-02-08 | DoS | Analytics import sneaks into the ingest hot path | HIGH | mitigate | ING-04 static-import guard kept green; only insertGitEvent (db-query) added, no analytics module import (Task 3) |
| T-02-SC | Tampering | New npm installs (chokidar, vitest-pool-workers) | MEDIUM | mitigate | Both packages legitimacy-verified in RESEARCH/EVIDENCE (C13 chokidar POC-proven, C35 vitest-pool-workers is the official Cloudflare test pool); slopcheck on install; see Source Audit note |
</threat_model>

<verification>
- `npm run typecheck` clean across all workspaces.
- `npx vitest run packages/shared daemon/src/adapters/loopback-bridge daemon/src/adapters/bridge-events backend/src/api/events-batch.hot-path.test.ts backend/src/db/queries/git-events.test.ts` all green.
- `events-batch.hot-path.test.ts` (4 ING-04 cases) still passes after the git-branch add.
- chokidar present in daemon deps; schema_version unchanged.
</verification>

<success_criteria>
- chokidar@^5.0.0 + @cloudflare/vitest-pool-workers declared (or fallback documented).
- Six per-tool payload validators + three analysis row types exist and are barrel-exported, no schema_version bump.
- `/v1/events` route added with the same security posture as `/v1/hook`; BridgeEventsAdapter normalises copilot/chatgpt-web/claude-ai-web with Copilot modelId tolerance.
- tool=git events route into git_events; ING-04 hot-path purity preserved.
</success_criteria>

<output>
Create `.planning/phases/02-parallel-adapters-backend-analysis-layer/02-01-SUMMARY.md` when done — record the pairing-header choice (verbatim vs alias), the BridgeEventsAdapter multi-tool registration mechanism, the git-id derivation method, and any vitest-pool-workers install fallback.
</output>
