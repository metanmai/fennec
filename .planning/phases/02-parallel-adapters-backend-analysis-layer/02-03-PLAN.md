---
phase: 02-parallel-adapters-backend-analysis-layer
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - daemon/src/adapters/cursor/adapter.ts
  - daemon/src/adapters/cursor/adapter.test.ts
  - daemon/src/adapters/cursor/cursor-normaliser.ts
  - daemon/src/adapters/cursor/cursor-normaliser.test.ts
  - daemon/src/adapters/cursor/sqlite-reader.ts
  - daemon/src/adapters/cursor/resolve-paths.ts
  - daemon/src/adapters/git/adapter.ts
  - daemon/src/adapters/git/adapter.test.ts
  - daemon/src/adapters/git/git-normaliser.ts
  - daemon/src/adapters/git/git-normaliser.test.ts
  - daemon/src/adapters/git/git-context.ts
  - daemon/test/fixtures/cursor-state.vscdb
  - daemon/test/fixtures/git-reflog-HEAD
autonomous: true
requirements: [CAP-05, CAP-09]

must_haves:
  truths:
    - "The Cursor adapter reads state.vscdb strictly read-only via node:sqlite (zero native deps); a write attempt would throw (D2-04, Q10)"
    - "The Cursor adapter emits one tool=cursor event per new bubbleId user message, filtered to createdAt newer than its watermark (CAP-05)"
    - "The git-watcher emits tool=git events with event_type ∈ {commit, revert, file_edit, branch_switch} derived from the reflog/HEAD (CAP-09, D2-06)"
    - "Revert detection matches the version-robust `Revert \"<subject>\"` rule AND the modern `revert:` reflog prefix (D2-06, EVIDENCE C11)"
    - "No better-sqlite3 / no schema_version bump (git kinds live in the git_events CHECK, not EventKindSchema) (D2-06)"
  artifacts:
    - path: "daemon/src/adapters/cursor/sqlite-reader.ts"
      provides: "node:sqlite read-only reader of cursorDiskKV bubbles"
      contains: "readOnly: true"
    - path: "daemon/src/adapters/git/adapter.ts"
      provides: "GitAdapter watching .git/HEAD + .git/logs/HEAD"
      contains: "class GitAdapter implements Adapter"
    - path: "daemon/src/adapters/git/git-normaliser.ts"
      provides: "reflog-message → event_type derivation"
      contains: "branch_switch"
  key_links:
    - from: "daemon/src/adapters/cursor/sqlite-reader.ts"
      to: "state.vscdb cursorDiskKV"
      via: "DatabaseSync(path, { readOnly: true })"
      pattern: "readOnly:\\s*true"
    - from: "daemon/src/adapters/git/adapter.ts"
      to: "tool=git EmitInput"
      via: "emit with payload.event_type"
      pattern: "this.emit\\("
---

<objective>
Build the Cursor IDE adapter (read-only `node:sqlite` reader of Cursor's global `state.vscdb`) and the git-watcher adapter (chokidar over `.git/HEAD` + `.git/logs/HEAD`). The Cursor adapter emits `tool: "cursor"` events from new `cursorDiskKV` bubbles; the git-watcher emits `tool: "git"` events with the four locked `event_type` values, which Plan 01's ingest tool-branch routes into `git_events`.

Purpose: The remaining two in-process capture surfaces (CAP-05, CAP-09). Disjoint file ownership from Plan 02 (codex/gemini) → fully parallel within Wave 2. The git-watcher feeds the correlation worker (Plan 06).

Output: `daemon/src/adapters/cursor/` (adapter + normaliser + sqlite-reader + resolve-paths + tests) and `daemon/src/adapters/git/` (adapter + normaliser + git-context + tests) plus a small fixture `.vscdb` and a reflog fixture.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-parallel-adapters-backend-analysis-layer/02-CONTEXT.md
@.planning/phases/02-parallel-adapters-backend-analysis-layer/02-RESEARCH.md
@.planning/phases/02-parallel-adapters-backend-analysis-layer/02-PATTERNS.md

<interfaces>
From daemon/src/adapters/adapter.ts — Adapter + EmitInput (see Plan 01). Adapters emit only via the registry callback (D2-01).

From packages/shared (Plan 01): `CursorPromptPayloadSchema`, `GitEventPayloadSchema`. Cursor prompts → `kind: "prompt_submitted"`; git events → `kind: "prompt_submitted"` is NOT used — git events carry their `event_type` in payload and ride the `tool: "git"` envelope routed to git_events by ingest (Plan 01 Task 3). Use an existing EventKind for the envelope's `kind` field (e.g. `prompt_submitted`) since EventKindSchema has no git kinds (EVIDENCE C20) and the git transport ignores `kind` on the git_events side.

From daemon/src/adapters/claude-code/adapter.ts — Adapter shape S1/S4. From payload-normaliser.ts — S3.
</interfaces>

<verified_facts>
Cursor (EVIDENCE C1/C2/C3/C4, RESEARCH A3):
- GLOBAL DB: `~/Library/Application Support/Cursor/User/globalStorage/state.vscdb` (WAL; `-wal`/`-shm` sidecars). NOT per-workspace .json — synapse cursor.ts is OUTDATED, do NOT copy it.
- Table `cursorDiskKV(key TEXT, value TEXT)`. Chat messages under `bubbleId:<composerId>:<bubbleId>`. Bubble value JSON: `type` (1=user, 2=assistant), `text`, `tokenCount:{inputTokens,outputTokens}` (often 0), `modelInfo:{modelName}`, `createdAt` (ISO8601), `requestId`.
- READ MECHANISM (PROVEN C1/C2): Node 22 built-in `node:sqlite` `new DatabaseSync(path, { readOnly: true })` opens the live WAL DB read-only while Cursor runs; a write throws "attempt to write a readonly database". NO better-sqlite3.
- node:sqlite emits an ExperimentalWarning but is require-able without a flag on Node 22.23.1 (C1). If a future pinned minor flag-gates it, add `--experimental-sqlite` to the daemon launch (A3 note / Q10).
- Watch `state.vscdb` AND `state.vscdb-wal` mtime (WAL writes may not bump the main file). On open failure (checkpoint lock): soft error → parse_errors, retry next tick; copy-then-read is the documented fallback (A3.5).

Git (EVIDENCE C11/C12/C21, RESEARCH A4):
- Watch `.git/HEAD` (~21 bytes; branch switch) + `.git/logs/HEAD` (reflog; appended on commit/checkout/revert). Working-tree `file_edit` from FS watch / `git status --porcelain`.
- event_type from reflog message: `commit: <subj>`→commit; subject matching `^Revert "`→revert (PRIMARY rule, version-robust) ALSO accept reflog prefix `revert:` (modern git, C11) and `commit (revert):` (older); `checkout: moving from A to B`→branch_switch; uncommitted change→file_edit.
- Context via porcelain (C12): `git config --get remote.origin.url`, `git rev-parse --abbrev-ref HEAD`, `git show -s --format=%cI HEAD` (ISO8601 occurred_at for a commit; new Date().toISOString() for branch_switch/file_edit).
- git_events.event_type CHECK = IN (commit, revert, file_edit, branch_switch) (C21). NO schema_version bump (git kinds NOT in EventKindSchema — D2-06/C20).
</verified_facts>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cursor adapter — node:sqlite read-only reader (CAP-05)</name>
  <files>daemon/src/adapters/cursor/adapter.ts, daemon/src/adapters/cursor/adapter.test.ts, daemon/src/adapters/cursor/cursor-normaliser.ts, daemon/src/adapters/cursor/cursor-normaliser.test.ts, daemon/src/adapters/cursor/sqlite-reader.ts, daemon/src/adapters/cursor/resolve-paths.ts, daemon/test/fixtures/cursor-state.vscdb</files>
  <read_first>
    - daemon/src/adapters/claude-code/adapter.ts (Adapter shape S1/S4)
    - daemon/src/adapters/claude-code/payload-normaliser.ts (S3)
    - .planning/phases/02-parallel-adapters-backend-analysis-layer/02-EVIDENCE.md (C1/C2/C3/C4 — node:sqlite read-only, cursorDiskKV bubble shape)
    - .planning/phases/02-parallel-adapters-backend-analysis-layer/02-RESEARCH.md (A3.4/A3.5 — read mechanism + WAL caveat + copy-then-read fallback)
  </read_first>
  <behavior>
    - Opening the fixture state.vscdb via the reader with { readOnly: true } returns cursorDiskKV bubble rows; an attempted write against the same handle throws "attempt to write a readonly database".
    - The normaliser maps a bubble with type:1 + text + modelInfo.modelName + createdAt to one tool=cursor EmitInput (kind=prompt_submitted), prompt_text=text, model=modelName, usage from tokenCount when non-zero (undefined when 0/absent).
    - A type:2 (assistant) bubble produces NO EmitInput.
    - Bubbles with createdAt <= the adapter's watermark are skipped (no re-emit); only createdAt-newer bubbles emit.
    - A locked/failed open is treated as a soft error (no throw up the loop) so the heartbeat keeps reporting.
  </behavior>
  <action>
Create `resolve-paths.ts` — `resolvePaths(os)` returning `{ dbPath }` macOS = `~/Library/Application Support/Cursor/User/globalStorage/state.vscdb` (D2-05; Linux/Windows throw "not implemented this phase"). Test override for the fixture path.

Create `sqlite-reader.ts` — a thin wrapper over Node's built-in `node:sqlite`: `import { DatabaseSync } from "node:sqlite"`; `readBubbles(dbPath, sinceCreatedAt): BubbleRow[]` opens `new DatabaseSync(dbPath, { readOnly: true })`, runs `db.prepare("SELECT key, value FROM cursorDiskKV WHERE key LIKE 'bubbleId:%'").all()`, JSON.parses each value, filters to `type === 1` with `createdAt > sinceCreatedAt`, `db.close()` (short-lived open per poll, D2-04). NEVER write/migrate. Wrap the open in try/catch — on failure return `[]` and let the caller count it as a soft retry (A3.5 copy-then-read fallback documented as a TODO comment, not required for the common WAL case which C2 proved works). Do NOT add better-sqlite3.

Create `cursor-normaliser.ts` — pure `normalizeCursorBubble(bubble) => EmitInput` (S3): `prompt_text` = `text`, `session_id` = the composerId portion of the key, `model` = `modelInfo.modelName` (tolerate absent → omit), `usage` from `tokenCount` only when inputTokens/outputTokens are present and non-zero (EVIDENCE C4 — often 0; omit when 0). Validate `payload` against `CursorPromptPayloadSchema`. Throw on a bubble missing `text`.

Create `adapter.ts` — `class CursorAdapter implements Adapter` `readonly tool = "cursor"`. chokidar-watch BOTH `dbPath` and `dbPath + "-wal"` mtime (WAL caveat, A3.5). On change: call `readBubbles(dbPath, this.watermark)`, emit one event per new bubble via the registry callback, advance `this.watermark` to the max createdAt seen. Persist watermark in-memory (a per-adapter offset file is Claude's Discretion — document the choice). start/stop mirror claude-code (S1/S4). On reader failure, log soft + continue (heartbeat still fires).

Check in `daemon/test/fixtures/cursor-state.vscdb` — a SMALL real-shaped SQLite DB built in the test (or a committed tiny fixture) with a `cursorDiskKV` table holding ~3 bubbles: one type:1 user bubble with text+modelInfo+createdAt+non-zero tokenCount, one type:1 with zero tokenCount, one type:2 assistant. Prefer building it programmatically in a test-setup helper via node:sqlite (write a temp DB, then read it read-only) so the fixture is reproducible — OR commit a trimmed real DB. Document the chosen approach.
  </action>
  <verify>
    <automated>cd /Users/Tanmai.N/Documents/fennec && npx vitest run daemon/src/adapters/cursor && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "readOnly" daemon/src/adapters/cursor/sqlite-reader.ts` returns ≥1 and the reader uses `node:sqlite` (not better-sqlite3).
    - `grep -c "better-sqlite3" daemon/src/adapters/cursor/` returns 0.
    - A test asserts a write against the read-only handle throws "attempt to write a readonly database".
    - The watermark test proves a bubble at/below the watermark is NOT re-emitted.
    - `npx vitest run daemon/src/adapters/cursor` exits 0; `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>CursorAdapter reads state.vscdb read-only via node:sqlite, emits one tool=cursor event per new user bubble, no native deps, watermark-deduped; tests + typecheck green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: git-watcher adapter + reflog event_type derivation (CAP-09)</name>
  <files>daemon/src/adapters/git/adapter.ts, daemon/src/adapters/git/adapter.test.ts, daemon/src/adapters/git/git-normaliser.ts, daemon/src/adapters/git/git-normaliser.test.ts, daemon/src/adapters/git/git-context.ts, daemon/test/fixtures/git-reflog-HEAD</files>
  <read_first>
    - daemon/src/adapters/claude-code/adapter.ts (Adapter shape S1/S4)
    - supabase/migrations/20260531000003_git_events_partitioned.sql (git_events event_type CHECK)
    - packages/shared/src/events/git-payload.ts (GitEventPayloadSchema from Plan 01)
    - .planning/phases/02-parallel-adapters-backend-analysis-layer/02-EVIDENCE.md (C11/C12 — reflog event_type derivation + porcelain context; the `revert:` prefix detail)
    - .planning/phases/02-parallel-adapters-backend-analysis-layer/02-OPEN-QUESTIONS.md (Q13 — git rides the standard ingest path; the adapter emits tool=git via the registry like any other adapter)
  </read_first>
  <behavior>
    - A reflog line `commit: <subject>` → event_type "commit".
    - A reflog line whose commit subject matches `^Revert "` OR whose reflog prefix is `revert:` (modern git, C11) OR `commit (revert):` (older) → event_type "revert".
    - A reflog line `checkout: moving from A to B` → event_type "branch_switch".
    - An uncommitted working-tree change → event_type "file_edit" (from FS watch / git status, NOT the reflog).
    - Each emitted git EmitInput carries tool="git", payload.event_type ∈ the 4 values, and (when available) git_remote/git_branch on the envelope + occurred_at.
    - A malformed/unknown reflog line counts as parse_errors (throws), watch loop continues.
  </behavior>
  <action>
Create `git-context.ts` — pure helpers wrapping porcelain via `node:child_process` `execFile` with argv arrays (NEVER a shell string — command-injection safe, mirrors the Phase 1 openssl-spawn discipline): `getRepoRemote(repoDir)` = `git config --get remote.origin.url`, `getRepoBranch(repoDir)` = `git rev-parse --abbrev-ref HEAD`, `getCommitDate(repoDir)` = `git show -s --format=%cI HEAD`. Each returns a trimmed string or undefined on non-zero exit. (Tests can stub these.)

Create `git-normaliser.ts` — pure `deriveGitEvent(reflogMessage, commitSubject?) => { event_type, subject?, reflog_message } | null`: apply the C11 rules above; the PRIMARY revert rule is `commitSubject` matching `/^Revert "/` (version-robust), with the reflog prefixes `revert:` / `commit (revert):` as secondary signals; return null for reflog lines that are none of the four kinds (the adapter skips). Build the `payload` validated by `GitEventPayloadSchema`. THROW on a structurally-malformed reflog line that claims a known prefix but lacks the expected fields → registry counts parse_errors.

Create `adapter.ts` — `class GitAdapter implements Adapter` `readonly tool = "git"`. Takes a list of registered repo dirs (for this phase, the daemon's watched repos — accept them via constructor options; default discovery is Claude's Discretion, e.g. the daemon's cwd repo or a configured list). For each repo: chokidar-watch `.git/HEAD` and `.git/logs/HEAD`; on `.git/logs/HEAD` change, tail-on-append the new reflog lines (offset bookkeeping like Plan 02), `deriveGitEvent` each, and for a non-null result `emit` a `tool: "git"` EmitInput with `kind` = an existing EventKind (e.g. `prompt_submitted` — the git transport ignores `kind`, EVIDENCE C22; document this), `payload` = the git payload, envelope `git_remote`/`git_branch` from `git-context`, `occurred_at` = commit date for a commit else now. Watch the working tree (or poll `git status --porcelain`) for `file_edit`. start/stop mirror claude-code (S1/S4); on any throw, swallow + log (S2). NO `schema_version` bump (D2-06).

Check in `daemon/test/fixtures/git-reflog-HEAD` — a real `.git/logs/HEAD`-shaped sample with one `commit:`, one `revert: Revert "..."`, and one `checkout: moving from ... to ...` line (generate via a throwaway repo with `git commit`, `git revert`, `git checkout -b`, then copy `.git/logs/HEAD`), so the event_type derivation test runs against real reflog text.
  </action>
  <verify>
    <automated>cd /Users/Tanmai.N/Documents/fennec && npx vitest run daemon/src/adapters/git && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `daemon/src/adapters/git/adapter.ts` contains `class GitAdapter implements Adapter` and `readonly tool = "git"`.
    - `git-normaliser.test.ts` proves all four event_type derivations against the fixture reflog, including a `Revert "` subject and a `revert:` prefix line both → "revert".
    - `git-context.ts` uses `execFile` with an argv array (no shell-string interpolation) — verified by a grep/test asserting no `exec(` with a template string.
    - `grep -c "schema_version" daemon/src/adapters/git/` shows no version bump introduced.
    - `npx vitest run daemon/src/adapters/git` exits 0; `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>GitAdapter watches HEAD/reflog, derives the 4 event_type values (robust revert detection), emits tool=git via the registry for ingest to route into git_events; porcelain via execFile argv; tests + typecheck green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cursor state.vscdb → adapter | Untrusted, version-fragile SQLite written by Cursor; must be read-only |
| reflog message / porcelain output → adapter | Untrusted strings; porcelain invoked as a subprocess |
| adapter → registry emit | Redaction/queue handled only by the registry |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-12 | Tampering/DoS | Cursor DB write/corruption by the adapter | HIGH | mitigate | `node:sqlite` `{ readOnly: true }` open; never write/migrate; test asserts a write throws (Task 1); copy-then-read documented fallback on lock |
| T-02-13 | Elevation/Tampering | Command injection via git porcelain | HIGH | mitigate | `execFile` with argv arrays only, never a shell string; test asserts no shell-string exec (Task 2) |
| T-02-14 | Information disclosure | Secret in a Cursor prompt / git subject reaches the queue unredacted | HIGH | mitigate | Adapter emits via registry only (D2-01); registry redacts before queue.append (PRIV-01, EVIDENCE C25) |
| T-02-15 | DoS | Malformed reflog / bubble crashes the watch loop | MEDIUM | mitigate | normaliser throws → registry parse_errors + drop; adapter swallows (S2); tests cover garbage input |
| T-02-SC | Tampering | node:sqlite is built-in (no install); no new deps | LOW | accept | Uses Node 22 stdlib `node:sqlite` (EVIDENCE C1) — zero native deps, no npm install |
</threat_model>

<verification>
- `npx vitest run daemon/src/adapters/cursor daemon/src/adapters/git` green.
- Read-only-throws test + watermark-dedupe test (Cursor); all-four-event_type test + execFile-argv test (git).
- `npm run typecheck` clean.
- Manual (build-time checklist, not blocking this plan's gate): one Cursor interaction → one `tool=cursor` staging row ≤5 min; a commit/revert/file_edit/branch_switch in a watched repo → matching `git_events` rows (depends on Plan 05 migrations pushed + Plan 01 git-branch deployed).
</verification>

<success_criteria>
- CursorAdapter reads via node:sqlite read-only (no better-sqlite3), one event per new user bubble.
- GitAdapter emits the four event_type values with robust revert detection, porcelain via execFile argv.
- Both emit only via the registry (D2-01); no schema_version bump.
</success_criteria>

<output>
Create `.planning/phases/02-parallel-adapters-backend-analysis-layer/02-03-SUMMARY.md` — record the Cursor fixture-build approach + watermark persistence, the git repo-discovery mechanism chosen, the `kind` value used on the git envelope, and note the Q10 build-time WAL-mtime + node:sqlite-flag verification follow-up.
</output>
