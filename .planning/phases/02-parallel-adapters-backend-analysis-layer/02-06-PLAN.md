---
phase: 02-parallel-adapters-backend-analysis-layer
plan: 06
type: execute
wave: 3
depends_on: ["02-01", "02-04"]
files_modified:
  - backend/wrangler.jsonc
  - backend/src/env.ts
  - backend/src/api/events-batch.ts
  - backend/src/index.ts
  - backend/src/workers/correlation-and-model-fit.ts
  - backend/src/workers/correlation.ts
  - backend/src/workers/correlation.test.ts
  - backend/src/workers/model-fit.ts
  - backend/src/workers/model-fit.test.ts
  - backend/src/workers/model-fit.no-llm.test.ts
  - backend/src/db/queries/prompt-outcomes.ts
  - backend/src/db/queries/model-fit-scores.ts
autonomous: true
requirements: [ANL-01, ANL-02, ANL-03, ANL-04]

must_haves:
  truths:
    - "Ingest enqueues each accepted event onto the EVENTS_QUEUE binding AFTER the existing insert loop; it is a binding call not an analytics import, so events-batch.hot-path.test.ts stays green (ANL-01, D2-12, EVIDENCE C30)"
    - "wrangler.jsonc has ONE queue producer (EVENTS_QUEUE→fennec-events) + ONE consumer with max_batch_size:100, max_batch_timeout:5, max_retries:3, dead_letter_queue (D2-13, EVIDENCE C15/C34)"
    - "ONE consumer Worker runs correlation AND model-fit as two idempotent functions in the single batch handler — Cloudflare allows only one push consumer per queue (D2-14, EVIDENCE C36/Q14)"
    - "Correlation joins each prompt to git_events within a ±15-min window (tunable config constant, not a buried literal) and writes exactly one prompt_outcomes row per prompt including null-link rows for no-git prompts (ANL-01, D2-16/D2-17)"
    - "prompt_outcomes rows carry interval confidence (band + low/high) and a revert writes attribution_state='downgraded_by_revert' as a recorded transition, never a silent decrement (ANL-02/ANL-03, D2-18/D2-19); upserts are idempotent on the prompt key (D2-15)"
    - "Model-fit is rule-based only — a static-import guard test asserts no fetch/http/undici/node:net/node:https import and no fetch( in the scorer source (ANL-04, D2-20, EVIDENCE C30/C35, PATTERNS S9)"
    - "One model_fit_scores row per prompt storing score + all input signals + fit_verdict; idempotent upsert (ANL-04, D2-21)"
  artifacts:
    - path: "backend/src/workers/correlation-and-model-fit.ts"
      provides: "queue(batch,env,ctx) consumer running correlateEvent + scoreModelFit per message"
      contains: "batch.messages"
    - path: "backend/src/workers/correlation.ts"
      provides: "correlateEvent — ±15-min git join, interval confidence, revert-downgrade transition, idempotent upsert"
      contains: "CORRELATION_WINDOW"
    - path: "backend/src/workers/model-fit.ts"
      provides: "scoreModelFit — pure rule-based scorer, zero network"
      contains: "fit_verdict"
    - path: "backend/src/db/queries/prompt-outcomes.ts"
      provides: "parameterized idempotent upsert into prompt_outcomes"
      exports: ["upsertPromptOutcome"]
    - path: "backend/src/db/queries/model-fit-scores.ts"
      provides: "parameterized idempotent upsert into model_fit_scores"
      exports: ["upsertModelFitScore"]
  key_links:
    - from: "backend/src/api/events-batch.ts"
      to: "EVENTS_QUEUE"
      via: "c.env.EVENTS_QUEUE.send(...) after the insert loop"
      pattern: "EVENTS_QUEUE.send"
    - from: "backend/src/workers/correlation.ts"
      to: "git_events"
      via: "±15-min occurred_at BETWEEN join"
      pattern: "CORRELATION_WINDOW"
    - from: "backend/src/index.ts"
      to: "queue consumer"
      via: "export { queue } sibling of default app"
      pattern: "export .*queue"
---

<objective>
Wire the Cloudflare Queue and build the single async consumer Worker that turns raw events into analysis rows. Un-comment + populate the `queues` producer/consumer block in `backend/wrangler.jsonc`, add the `EVENTS_QUEUE` binding to `Env`, add the one enqueue call to the ingest hot path (a binding call, not an analytics import), and export a `queue()` handler from `backend/src/index.ts` that runs BOTH correlation and rule-based model-fit as two idempotent functions over each batch (Cloudflare allows only one push consumer per queue — D2-14). Correlation writes interval-confidence `prompt_outcomes` with explicit revert-downgrade state; model-fit writes `model_fit_scores` with score + signals + verdict, guarded by a no-LLM static-import test.

Purpose: The correlation + model-fit half of the backend analysis layer (ANL-01/02/03/04). It consumes Plan 01's shared analysis types + the enqueue point, and writes the `prompt_outcomes`/`model_fit_scores` tables created in Plan 04. The Plan 07 aggregator later reads these rows.

Output: queue config in wrangler.jsonc, EVENTS_QUEUE in Env, the enqueue call in events-batch.ts (2nd modification after Plan 01's git-branch), the consumer Worker (correlation + model-fit functions + parameterized upsert queries), the no-LLM guard test, and the `queue` export in index.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-parallel-adapters-backend-analysis-layer/02-CONTEXT.md
@.planning/phases/02-parallel-adapters-backend-analysis-layer/02-RESEARCH.md
@.planning/phases/02-parallel-adapters-backend-analysis-layer/02-EVIDENCE.md
@.planning/phases/02-parallel-adapters-backend-analysis-layer/02-PATTERNS.md
@.planning/phases/02-parallel-adapters-backend-analysis-layer/02-OPEN-QUESTIONS.md
@.planning/phases/02-parallel-adapters-backend-analysis-layer/02-01-PLAN.md
@.planning/phases/02-parallel-adapters-backend-analysis-layer/02-04-PLAN.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase + prior plans; use directly, do not re-explore. -->

From backend/src/api/events-batch.ts — CURRENT ingest handler: `app.post("/api/events/batch", zValidator(...), async (c) => { const { events } = c.req.valid("json"); const org_id = c.get("org_id"); const client = pgClient(c.env); await client.connect(); try { let accepted=0; for (const event of events) { ... insertAiEvent(client, row); accepted+=1; } return c.json({accepted}); } finally { await client.end(); } })`. NOTE: Plan 01 (02-01) adds a `event.tool === "git"` branch inside this loop (git → insertGitEvent). READ Plan 01's landed version FIRST — the enqueue is a SECOND modification appended AFTER the insert (for both branches). org_id comes from `c.get("org_id")`, NEVER the body (T-05-02).

From backend/src/api/events-batch.hot-path.test.ts (EVIDENCE C30) — the ING-04 guard greps this handler's source for `from "...correlation"`, `from "...model-fit"`, `from "...aggregator"`, `from "...analysis"` (4 cases). The enqueue is `c.env.EVENTS_QUEUE.send(...)` — a BINDING call, NOT an import — so it does not trip the guard. Do NOT `import` any worker/analysis module into events-batch.ts.

From backend/src/env.ts — the `Env` interface (HYPERDRIVE, OAUTH_STATE_KV, secrets) + `Variables` (org_id, api_key_id, daemon_machine_id, hostname). Add `EVENTS_QUEUE: Queue` to `Env` mirroring the HYPERDRIVE binding line.

From backend/src/index.ts — currently `export default app;` (Hono app). Cloudflare needs a module object with `{ fetch, queue }` (and later `scheduled` in Plan 07). Add a `queue` export re-exporting the consumer's handler, keeping the default `app` as the fetch handler (Hono exposes `app.fetch`). Follow the Cloudflare ES-module-worker convention: `export default { fetch: app.fetch, queue };`.

From backend/src/db/client.ts (S8) — `pgClient(env)` per-invocation; `await client.connect(); try { ... } finally { ctx.waitUntil(client.end()); }`. NEVER cache across invocations.

From backend/src/db/queries/ai-events.ts (S7) — the parameterized `$1..$N` + `ON CONFLICT ... DO NOTHING` idiom to mirror for `upsertPromptOutcome` / `upsertModelFitScore` (use `DO UPDATE SET ...` since a redelivery re-derives the same row; both keyed on the prompt idempotency_key).

From @fennec/shared (authored in Plan 01) — `PromptOutcomeSchema` (org_id, prompt_event_id/idempotency_key, git_event_id nullable, confidence_band, confidence_low, confidence_high, attribution_state, downgraded_at, downgrade_reason) and `ModelFitScoreSchema` (score, fit_verdict, prompt_length, file_edit_size, tool_call_count, model, model_tier). Import these row types for the query/worker signatures.

From supabase/migrations/20260531000008_prompt_outcomes.sql + ...009_model_fit_scores.sql (Plan 04) — the physical columns + PK `(prompt_idempotency_key, occurred_at)` the upserts target. From ...003_git_events_partitioned.sql — git_events columns for the correlation join: `org_id, user_id, occurred_at, repo_remote, repo_branch, event_type, payload`.

From EVIDENCE C15/C34 — the wrangler queue+cron config is VALID (`max_batch_size:100, max_batch_timeout:5, max_retries:3, dead_letter_queue`); the current `queues` block is commented in wrangler.jsonc lines 62-66. From EVIDENCE C36/Q14 — ONE push consumer per queue is a documented Cloudflare limit (verify at live deploy); design = one consumer, two functions.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Queue wiring — wrangler.jsonc producer+consumer, Env binding, ingest enqueue (ANL-01)</name>
  <files>backend/wrangler.jsonc, backend/src/env.ts, backend/src/api/events-batch.ts</files>
  <read_first>
    - backend/wrangler.jsonc (the commented Phase-2 queue stub, lines 62-66 — self-analog)
    - backend/src/env.ts (Env interface — add EVENTS_QUEUE mirroring HYPERDRIVE)
    - backend/src/api/events-batch.ts (READ Plan 01's landed version WITH the git-branch — the enqueue appends after the insert)
    - backend/src/api/events-batch.hot-path.test.ts (the ING-04 static-import guard that MUST stay green — EVIDENCE C30)
    - .planning/phases/02-parallel-adapters-backend-analysis-layer/02-01-PLAN.md (Task 3 — the git-discriminator branch this plan appends the enqueue to; ordering rationale)
    - .planning/phases/02-parallel-adapters-backend-analysis-layer/02-EVIDENCE.md (C15/C34 — valid wrangler queue config)
  </read_first>
  <action>
In `backend/wrangler.jsonc`, replace the commented Phase-2 `queues` stub (lines 62-66) with a live block (EVIDENCE C15/C34, D2-13): `"queues": { "producers": [{ "binding": "EVENTS_QUEUE", "queue": "fennec-events" }], "consumers": [{ "queue": "fennec-events", "max_batch_size": 100, "max_batch_timeout": 5, "max_retries": 3, "dead_letter_queue": "fennec-events-dlq" }] }`. Keep the existing hyperdrive + kv blocks unchanged. Add a comment that the DLQ (`fennec-events-dlq`) must be created at deploy (`wrangler queues create fennec-events-dlq`) and that the one-push-consumer-per-queue limit (EVIDENCE C36/Q14) is why correlation + model-fit share one consumer.

In `backend/src/env.ts`, add `EVENTS_QUEUE: Queue;` to the `Env` interface (Cloudflare Workers `Queue` type from `@cloudflare/workers-types`), documented alongside HYPERDRIVE as the Phase-2 producer binding.

In `backend/src/api/events-batch.ts`, after the existing per-event insert (BOTH the ai_events and, per Plan 01, the git_events branch — enqueue after either insert succeeds so a failed insert never enqueues a non-persisted event, RESEARCH C7.5), add ONE binding call: `await c.env.EVENTS_QUEUE.send({ idempotency_key: event.idempotency_key, occurred_at: event.occurred_at, org_id, tool: event.tool });`. This message body is the minimal envelope the consumer needs to re-fetch the full row. CRITICAL (ING-04): this is a BINDING call (`env.EVENTS_QUEUE.send`) — do NOT import any `correlation`/`model-fit`/`aggregator`/`analysis` module into this file; the hot-path guard greps for exactly those import strings (EVIDENCE C30) and MUST stay green. Keep `accepted` counting identical. (Claude's Discretion: git-tool messages MAY be enqueued too so the correlation worker can react to a revert arriving after a prompt — document whether git events are enqueued or the correlation worker reads git_events by time-window only; the ±window join reads git_events regardless, so enqueuing git is optional.)

Run the ING-04 guard to confirm it still passes after the enqueue add.
  </action>
  <verify>
    <automated>cd /Users/Tanmai.N/Documents/fennec && npx vitest run backend/src/api/events-batch.hot-path.test.ts backend/src/api/events-batch.test.ts && npm run typecheck && grep -c "EVENTS_QUEUE" backend/wrangler.jsonc && grep -c "EVENTS_QUEUE.send" backend/src/api/events-batch.ts</automated>
  </verify>
  <acceptance_criteria>
    - `backend/wrangler.jsonc` has a live `queues` block with one producer (`EVENTS_QUEUE`→`fennec-events`) + one consumer with `max_batch_size:100`, `max_batch_timeout:5`, `max_retries:3`, `dead_letter_queue` (D2-13).
    - `backend/src/env.ts` `Env` includes `EVENTS_QUEUE: Queue`.
    - `backend/src/api/events-batch.ts` contains `c.env.EVENTS_QUEUE.send(` and imports NO correlation/model-fit/aggregator/analysis module.
    - `npx vitest run backend/src/api/events-batch.hot-path.test.ts` exits 0 (all 4 ING-04 cases still green — ANL-01 preserves hot-path purity).
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>Queue producer+consumer wired in wrangler.jsonc; EVENTS_QUEUE in Env; one enqueue binding call added after the insert; ING-04 hot-path guard still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Correlation function — ±15-min join, interval confidence, revert-downgrade (ANL-01/02/03)</name>
  <files>backend/src/workers/correlation.ts, backend/src/workers/correlation.test.ts, backend/src/db/queries/prompt-outcomes.ts</files>
  <read_first>
    - backend/src/db/queries/ai-events.ts (parameterized $1..$N + ON CONFLICT idiom — S7)
    - backend/src/db/client.ts (pgClient lifecycle — S8)
    - supabase/migrations/20260531000008_prompt_outcomes.sql (physical columns + PK the upsert targets — from Plan 04)
    - supabase/migrations/20260531000003_git_events_partitioned.sql (git_events columns for the join)
    - packages/shared/src/analysis/prompt-outcome.ts (PromptOutcomeSchema from Plan 01 — the row shape)
    - .planning/phases/02-parallel-adapters-backend-analysis-layer/02-CONTEXT.md (D2-16 window, D2-17 always-one-row, D2-18 interval, D2-19 revert-transition)
    - .planning/phases/02-parallel-adapters-backend-analysis-layer/02-OPEN-QUESTIONS.md (Q1 window value, Q5 confidence-band thresholds)
  </read_first>
  <behavior>
    - A prompt followed by a commit within ±15 min in the same repo/branch → one prompt_outcomes row linking the two, attribution_state='attributed', a confidence interval with both bounds populated (band + low<=high).
    - A prompt with NO git activity in-window → one prompt_outcomes row with git_event_id NULL and attribution_state='no_outcome' (a row exists, D2-17 — never a missing row).
    - Confidence is an interval: the row NEVER carries a bare single number; confidence_low and confidence_high are both set (D2-18/ANL-02).
    - A prompt→commit→later-revert sequence: re-running correlation for the prompt writes attribution_state='downgraded_by_revert' + downgraded_at + downgrade_reason on the EXISTING row (the prior 'attributed' state and confidence remain traceable) — no aggregate total is decremented (D2-19/ANL-03).
    - Idempotency: correlating the same prompt twice produces one row (upsert on the prompt key, D2-15), not two.
    - The ±window is a named config constant CORRELATION_WINDOW (default 15 min), not a literal buried in the SQL (D2-16/Q1).
  </behavior>
  <action>
Create `backend/src/db/queries/prompt-outcomes.ts` exporting `upsertPromptOutcome(client, row)` — a parameterized `$1..$N` INSERT into `prompt_outcomes (org_id, prompt_idempotency_key, occurred_at, git_event_id, confidence_band, confidence_low, confidence_high, attribution_state, downgraded_at, downgrade_reason)` with `ON CONFLICT (prompt_idempotency_key, occurred_at) DO UPDATE SET ...` (redelivery re-derives the same row — D2-15/S7). Add a second export `downgradeOnRevert(client, {prompt_idempotency_key, occurred_at, downgraded_at, downgrade_reason})` that `UPDATE prompt_outcomes SET attribution_state='downgraded_by_revert', downgraded_at=$_, downgrade_reason=$_ WHERE prompt_idempotency_key=$_ AND occurred_at=$_ AND attribution_state <> 'downgraded_by_revert'` — a recorded transition, never a decrement (D2-19). All queries parameterized (T-05-03).

Create `backend/src/workers/correlation.ts` exporting `export const CORRELATION_WINDOW_MINUTES = 15;` (D2-16 — a named tunable constant; note in a comment it can move to env/config once staging data exists, Q1) and `export async function correlateEvent(msg, client): Promise<void>`. Logic: (1) fetch the prompt-class ai_events row by `(idempotency_key, occurred_at)` from the message body; if the event's tool is `git`, instead run the revert path (see below) and return. (2) Query git_events for the same org where `occurred_at BETWEEN prompt.occurred_at - (CORRELATION_WINDOW_MINUTES || ' minutes')::interval AND prompt.occurred_at + ...` AND matching `repo_remote`/`repo_branch` when present. (3) Derive confidence: rule-based band (D2-18/Q5) — tight time gap + same repo/branch + a commit touching files → 'high' with a narrow [low,high]; loose gap or branch mismatch → 'low' with a wide interval; in-between → 'medium'. Document the exact gap cutoffs + bound values as named constants (Claude's Discretion per Q5). (4) Upsert exactly one prompt_outcomes row (linking git_event_id when matched, NULL + 'no_outcome' when not — D2-17). (5) Revert path: when the message is a git revert (event_type='revert') correlating to a previously-attributed prompt within the window, call `downgradeOnRevert` on that prompt's row (D2-19). org_id is taken from the source event's org_id, never client-supplied (T-05-02).

Create `backend/src/workers/correlation.test.ts` with the six behaviours above using the mock pg client (`backend/src/test-utils/mock-db.ts` `createMockClient` + `setHandler`): stub git_events SELECT rows and assert the upsert params (band/bounds/state/git_event_id). Assert the no-git case still upserts one row with 'no_outcome'; assert the revert case issues the downgrade UPDATE (not a delete/decrement); assert confidence has both bounds; assert the window uses `CORRELATION_WINDOW_MINUTES` (grep or param inspection).
  </action>
  <verify>
    <automated>cd /Users/Tanmai.N/Documents/fennec && npx vitest run backend/src/workers/correlation.test.ts && npm run typecheck && grep -c "CORRELATION_WINDOW" backend/src/workers/correlation.ts</automated>
  </verify>
  <acceptance_criteria>
    - `backend/src/db/queries/prompt-outcomes.ts` exports `upsertPromptOutcome` (parameterized, `ON CONFLICT ... DO UPDATE`) and `downgradeOnRevert` (UPDATE to `downgraded_by_revert`, no decrement).
    - `backend/src/workers/correlation.ts` defines a named `CORRELATION_WINDOW`/`CORRELATION_WINDOW_MINUTES` constant (=15) used in the join — no bare 15-min literal buried in the SQL string (D2-16).
    - Test: a no-git prompt still writes exactly ONE prompt_outcomes row with git_event_id NULL + 'no_outcome' (D2-17).
    - Test: every written row has confidence_low AND confidence_high populated with low<=high (ANL-02); no bare single number.
    - Test: a revert issues the `downgraded_by_revert` UPDATE (recorded transition), asserted to NOT be a DELETE or a numeric decrement (ANL-03).
    - `npx vitest run backend/src/workers/correlation.test.ts` exits 0; `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>correlateEvent joins prompts to git_events in a ±15-min tunable window, always writes one interval-confidence prompt_outcomes row, downgrades reverts via a recorded transition, idempotent upsert; tests + typecheck green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Model-fit scorer + consumer wiring + no-LLM guard (ANL-04)</name>
  <files>backend/src/workers/model-fit.ts, backend/src/workers/model-fit.test.ts, backend/src/workers/model-fit.no-llm.test.ts, backend/src/db/queries/model-fit-scores.ts, backend/src/workers/correlation-and-model-fit.ts, backend/src/index.ts</files>
  <read_first>
    - backend/src/api/events-batch.ts (pg lifecycle S8 to mirror in the queue() handler)
    - backend/src/db/queries/ai-events.ts (parameterized upsert idiom S7)
    - backend/src/api/events-batch.hot-path.test.ts (the EXACT static-import-grep structure to copy for the no-LLM guard — S9, EVIDENCE C30)
    - backend/src/index.ts (currently `export default app` — add the queue export per the Cloudflare module-worker convention)
    - packages/shared/src/analysis/model-fit.ts (ModelFitScoreSchema from Plan 01)
    - supabase/migrations/20260531000009_model_fit_scores.sql (physical columns from Plan 04)
    - .planning/phases/02-parallel-adapters-backend-analysis-layer/02-CONTEXT.md (D2-14 one-consumer-two-functions, D2-20 no-network, D2-21 signals+verdict, D2-22 model→tier data lookup)
    - .planning/phases/02-parallel-adapters-backend-analysis-layer/02-OPEN-QUESTIONS.md (Q6 heuristic weights/cutoffs)
  </read_first>
  <behavior>
    - scoreModelFit is a PURE function of the prompt event (+ optional correlated git edit size): prompt_length, file_edit_size, tool_call_count, model tier (from a data lookup, D2-22) → a numeric score + fit_verdict ∈ {under_powered, fit, over_powered}.
    - A long prompt + large edit + many tool calls on a frontier model → verdict 'fit'; a tiny prompt + tiny edit + no tool calls on a frontier model → 'over_powered' (a cheaper model would fit); a heavy task on a small model → 'under_powered'.
    - Exactly one model_fit_scores row per prompt, storing the score AND all input signals AND the verdict (D2-21); a redelivery upserts (one row, D2-15).
    - The scorer makes NO network/LLM call — the static-import guard test asserts no fetch/http/undici/node:net/node:https import and no `fetch(` / `XMLHttpRequest` in model-fit.ts source (ANL-04/D2-20).
    - The consumer queue() handler runs correlateEvent AND scoreModelFit over each message, then acks the batch; a pg client is opened once per batch (S8).
  </behavior>
  <action>
Create `backend/src/db/queries/model-fit-scores.ts` exporting `upsertModelFitScore(client, row)` — parameterized INSERT into `model_fit_scores (org_id, prompt_idempotency_key, occurred_at, score, fit_verdict, prompt_length, file_edit_size, tool_call_count, model, model_tier)` with `ON CONFLICT (prompt_idempotency_key, occurred_at) DO UPDATE SET ...` (D2-15/S7). Parameterized only (T-05-03).

Create `backend/src/workers/model-fit.ts` — a PURE `scoreModelFit(promptEvent, correlatedEditSize?)` (zero network — D2-20). Compute the four signals: `prompt_length` (chars/tokens from payload.prompt_text), `file_edit_size` (from the correlated git event if available, else 0), `tool_call_count` (count of tool_call-kind events in the session — Claude's Discretion on the source: from payload or a session query; document it), `model_tier` via a lookup (read the tier from `model_pricing.tier` for the event's model, or an in-memory map derived from that table — the point is data-driven per D2-22, NOT a giant switch; document the mechanism). Combine into a transparent weighted "task heaviness" score with NAMED documented weight/cutoff constants (Q6 — Claude's Discretion on values) and output `fit_verdict`. Store BOTH the score AND the signals (D2-21). Export `scoreModelFit` + an async `runModelFit(msg, client)` that fetches the event, scores it, and calls `upsertModelFitScore`. NO import of fetch/http/undici/node:net/node:https; NO `fetch(` call.

Create `backend/src/workers/model-fit.no-llm.test.ts` — copy the `readFileSync` + `expect(source).not.toMatch(...)` structure VERBATIM from events-batch.hot-path.test.ts (S9). Assert `model-fit.ts` source contains no import of `fetch`/`http`/`undici`/`node:net`/`node:https` and no `fetch(` / `XMLHttpRequest` token (ANL-04). Use `grep -v '^ *//'`-equivalent care so a comment mentioning "no fetch" does not self-invalidate the grep (assert on `import ... from` and call-site patterns, not the bare word).

Create `backend/src/workers/model-fit.test.ts` — behaviour tests for the verdict logic (the three heaviness scenarios above) + the one-row-per-prompt upsert (mock pg client), asserting the upsert params include score + all signals + verdict.

Create `backend/src/workers/correlation-and-model-fit.ts` exporting `export async function queue(batch: MessageBatch, env: Env, ctx: ExecutionContext): Promise<void>` (D2-14): open one `pgClient(env)`, connect, `for (const msg of batch.messages) { await correlateEvent(msg.body, client); await runModelFit(msg.body, client); }`, `batch.ackAll()`, `finally { ctx.waitUntil(client.end()); }` (S8). Import `correlateEvent` from ./correlation.js and `runModelFit` from ./model-fit.js. (This file is the ONE consumer for the ONE queue — D2-14; a comment records the Cloudflare one-push-consumer limit EVIDENCE C36.)

In `backend/src/index.ts`, add the queue export using the Cloudflare module-worker convention: `import { queue } from "./workers/correlation-and-model-fit.js";` and change the default export to `export default { fetch: app.fetch, queue };` (keep the Hono `app` as the fetch handler). Do NOT add `scheduled` here — that is Plan 07 (which appends to this same export object in Wave 4).
  </action>
  <verify>
    <automated>cd /Users/Tanmai.N/Documents/fennec && npx vitest run backend/src/workers && npm run typecheck && grep -c "batch.messages" backend/src/workers/correlation-and-model-fit.ts</automated>
  </verify>
  <acceptance_criteria>
    - `backend/src/workers/model-fit.no-llm.test.ts` mirrors the hot-path-guard grep and asserts no fetch/http/undici/node:net/node:https import + no `fetch(`/`XMLHttpRequest` in model-fit.ts (ANL-04) — bound to `npx vitest run backend/src/workers/model-fit.no-llm.test.ts`.
    - `backend/src/workers/model-fit.ts` produces one model_fit_scores row per prompt with score + all five signals + fit_verdict (D2-21) — asserted by model-fit.test.ts upsert-param check.
    - `backend/src/db/queries/model-fit-scores.ts` exports `upsertModelFitScore` (parameterized, ON CONFLICT DO UPDATE).
    - `backend/src/workers/correlation-and-model-fit.ts` runs BOTH `correlateEvent` and `runModelFit` per message and `ackAll`s the batch (D2-14) — `grep -c "correlateEvent" ...` ≥1 and `grep -c "runModelFit" ...` ≥1.
    - `backend/src/index.ts` exports `{ fetch, queue }` (module-worker convention) — `grep -c "queue" backend/src/index.ts` ≥1.
    - `npx vitest run backend/src/workers` exits 0; `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>Rule-based scoreModelFit (zero network, guarded), one model_fit_scores row/prompt with signals+verdict, single consumer runs both functions + acks, queue exported from index.ts; tests + typecheck green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ingest hot path → queue | events-batch enqueues a minimal envelope; must not import analytics (ING-04) |
| queue message → consumer | at-least-once delivery; a redelivered/tampered message must not create duplicate or cross-tenant rows |
| consumer → prompt_outcomes / model_fit_scores | worker queries must be parameterized and org_id-scoped from the source event |
| scorer → network | model-fit must make ZERO network/LLM calls (rule-based only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | DoS | Analytics import sneaks into the ingest hot path | HIGH | mitigate | enqueue is a BINDING call (`env.EVENTS_QUEUE.send`), no analytics import; ING-04 static-import guard kept green (Task 1, EVIDENCE C30) |
| T-06-02 | Tampering | Replayed/duplicated queue message writes duplicate outcome/score rows | MEDIUM | mitigate | idempotent upsert on `(prompt_idempotency_key, occurred_at)` — a redelivery re-derives one row (D2-15/S7); tests assert one row (Tasks 2/3) |
| T-06-03 | Tampering | SQL injection in worker queries | HIGH | mitigate | parameterized `$1..$N` only in prompt-outcomes.ts + model-fit-scores.ts (T-05-03); no string-concat of values |
| T-06-04 | Spoofing | Cross-tenant rows via a forged org_id in the queue message | HIGH | mitigate | org_id derived from the source ai_events row (re-fetched by key), not trusted blindly from the message body for tenancy writes (T-05-02); a comment + test document the source-of-truth |
| T-06-05 | Info disclosure / Exfiltration | model-fit calls an external LLM/endpoint leaking prompt text | HIGH | mitigate | rule-based scorer with a static-import no-LLM guard test (ANL-04/D2-20, S9); no fetch/http import allowed |
| T-06-06 | Repudiation | Revert silently subtracts from a total (ANL-03 anti-pattern) | MEDIUM | mitigate | `downgradeOnRevert` records an explicit state transition on the existing row; no aggregate decrement path exists (Task 2) |
| T-06-SC | Tampering | New npm install | LOW | accept | No new runtime dependency — uses existing `pg`, Hono, `@fennec/shared`, and Cloudflare `Queue` binding types (@cloudflare/workers-types already present); nothing installed |
</threat_model>

<verification>
- `npx vitest run backend/src/api/events-batch.hot-path.test.ts backend/src/workers` all green.
- ING-04 guard still passes after the enqueue add (Task 1).
- Correlation: no-git one-row, interval confidence, revert-downgrade transition, idempotent upsert (Task 2).
- Model-fit: no-LLM static guard, one-row-with-signals-and-verdict (Task 3).
- `npm run typecheck` clean.
- Manual (build-time, gated on Plan 04 pushed + a live Cloudflare deploy — Q14): deploy the single consumer to staging; confirm it drains fennec-events and writes prompt_outcomes + model_fit_scores.
</verification>

<success_criteria>
- Ingest enqueues onto EVENTS_QUEUE; hot-path purity preserved (ANL-01).
- One consumer runs correlation + model-fit as two idempotent functions (D2-14).
- prompt_outcomes carry interval confidence (ANL-02) + revert-downgrade state (ANL-03).
- model_fit_scores are rule-based (no-LLM guard), one row/prompt with signals + verdict (ANL-04).
</success_criteria>

<output>
Create `.planning/phases/02-parallel-adapters-backend-analysis-layer/02-06-SUMMARY.md` — record the confidence-band cutoffs + model-fit weights chosen (Q5/Q6), the tool_call_count + model→tier lookup mechanism (D2-22), whether git events are enqueued or joined by time-window only, and the module-worker export shape adopted in index.ts (so Plan 07 appends `scheduled` cleanly).
</output>
