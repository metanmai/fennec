---
phase: 02-parallel-adapters-backend-analysis-layer
plan: 06
type: execute
wave: 3
depends_on: ["02-01", "02-04"]
files_modified:
  - backend/wrangler.jsonc
  - backend/src/env.ts
  - backend/src/api/events-batch.ts
  - backend/src/index.ts
  - backend/src/workers/correlation.ts
  - backend/src/workers/correlation.test.ts
  - backend/src/workers/model-fit.ts
  - backend/src/workers/model-fit.test.ts
  - backend/src/workers/model-fit.no-llm.test.ts
  - backend/src/workers/dlq-drain.ts
  - backend/src/workers/dlq-drain.test.ts
  - backend/src/workers/module-worker.typecheck.test.ts
  - backend/src/db/queries/prompt-outcomes.ts
  - backend/src/db/queries/model-fit-scores.ts
autonomous: true
requirements: [ANL-01, ANL-02, ANL-03, ANL-04]

must_haves:
  truths:
    - "Ingest enqueues onto the EVENTS_QUEUE binding AFTER the insert loop via ONE sendBatch (not per-event send inside the loop), aligning with RESEARCH C7.5 after-loop placement; it is a binding call not an analytics import, so events-batch.hot-path.test.ts stays green (ANL-01, D2-12, H11, EVIDENCE C30)"
    - "Each queue message envelope carries a table discriminator (`table: 'ai_events' | 'git_events'`) + `event_type` + the source-row lookup key (idempotency_key + occurred_at) + org_id as a ROUTING HINT ONLY; enqueue is UNCONDITIONAL on idempotency_key (not gated on rows-inserted, since ON CONFLICT DO NOTHING returns 0) so a re-delivered/re-ingested event is never dropped (H3, H9, H11)"
    - "Git-event enqueue is MANDATORY (not optional) — every git_events insert enqueues a `table:'git_events'` message so the correlation worker can react to a revert that lands hours-to-days after the prompt→commit, decoupled from the ±15-min prompt-join window (H3)"
    - "wrangler.jsonc has ONE queue producer (EVENTS_QUEUE→fennec-events) + ONE consumer with max_batch_size:100, max_batch_timeout:5, max_retries:3, dead_letter_queue:fennec-events-dlq, AND a SECOND consumer block draining fennec-events-dlq to a monitored DLQ handler (poison messages are not silently lost) (D2-13, H10, EVIDENCE C15/C34)"
    - "ONE main consumer runs correlation AND model-fit as two idempotent functions in the single batch handler inlined in index.ts (no separate wrapper module) — Cloudflare allows only one push consumer per queue (D2-14, EVIDENCE C36/Q14, simplicity)"
    - "BOTH correlateEvent AND runModelFit re-read org_id from the persisted source row (looked up by idempotency_key + occurred_at + table discriminator); the message-body org_id is a routing hint only and is NEVER used as the tenant for a write — a forged/replayed message with a swapped org_id cannot write cross-tenant rows (H9, T-05-02)"
    - "A source row not yet visible on re-fetch (read-after-write race under Hyperdrive) is treated as a TRANSIENT retry (msg.retry()), NOT a hard failure that poisons the message; only a genuinely malformed message body is nacked to the DLQ (H10)"
    - "Correlation joins each prompt to git_events within a ±15-min window (tunable config constant, not a buried literal) and writes exactly one prompt_outcomes row per prompt including null-link rows for no-git prompts (ANL-01, D2-16/D2-17)"
    - "The revert path is DECOUPLED from the ±15-min prompt-join window: a git revert message parses the reverted commit (`Revert \"<subject>\"` subject / reverted SHA), finds the prompt_outcomes row via its git_event_id linkage to the ORIGINAL commit REGARDLESS of age, and sets attribution_state='downgraded_by_revert' as a recorded transition, never a silent decrement (ANL-03, D2-19, H3)"
    - "prompt_outcomes rows carry interval confidence (band + low/high with low<high) and upserts are idempotent on the prompt key (ANL-02, D2-18/D2-15)"
    - "Model-fit is rule-based only — a static-import guard test asserts no fetch/http/undici/node:net/node:https import and no fetch( in the scorer source (ANL-04, D2-20, EVIDENCE C30/C35, PATTERNS S9)"
    - "One model_fit_scores row per prompt storing score + all input signals + fit_verdict; idempotent upsert (ANL-04, D2-21)"
    - "The Cloudflare module-worker `{ fetch, queue, scheduled? }` export + `MessageBatch`/`ScheduledController` typings compile against the pinned @cloudflare/workers-types — a STATIC-VALIDATED typecheck test asserts a minimal module object typechecks (H12, EVIDENCE C43)"
  artifacts:
    - path: "backend/src/index.ts"
      provides: "module-worker default export { fetch, queue } with the two-function batch handler inlined (correlateEvent + runModelFit per message) — no separate wrapper module (H12, simplicity)"
      contains: "batch.messages"
    - path: "backend/src/workers/correlation.ts"
      provides: "correlateEvent — ±15-min git join, interval confidence, age-decoupled revert-downgrade transition, idempotent upsert, org_id re-read from source row"
      contains: "CORRELATION_WINDOW"
    - path: "backend/src/workers/model-fit.ts"
      provides: "scoreModelFit — pure rule-based scorer, zero network; runModelFit re-reads org_id from source row"
      contains: "fit_verdict"
    - path: "backend/src/workers/dlq-drain.ts"
      provides: "queueDlq(batch,env,ctx) — the DLQ consumer that logs/alerts poison messages + supports manual replay, so failed analysis is not silently lost (H10)"
      contains: "batch.messages"
    - path: "backend/src/db/queries/prompt-outcomes.ts"
      provides: "parameterized idempotent upsert into prompt_outcomes + downgradeOnRevert + a git_event_id lookup for the revert path"
      exports: ["upsertPromptOutcome", "downgradeOnRevert"]
    - path: "backend/src/db/queries/model-fit-scores.ts"
      provides: "parameterized idempotent upsert into model_fit_scores"
      exports: ["upsertModelFitScore"]
  key_links:
    - from: "backend/src/api/events-batch.ts"
      to: "EVENTS_QUEUE"
      via: "c.env.EVENTS_QUEUE.sendBatch(...) ONCE after the insert loop"
      pattern: "EVENTS_QUEUE.sendBatch"
    - from: "backend/src/workers/correlation.ts"
      to: "git_events"
      via: "±15-min occurred_at BETWEEN join + git_event_id revert lookup"
      pattern: "CORRELATION_WINDOW"
    - from: "backend/src/index.ts"
      to: "queue consumer"
      via: "export { queue } sibling of default app; queue re-reads org_id from source rows"
      pattern: "export .*queue"
---

<objective>
Wire the Cloudflare Queue and build the single async consumer that turns raw events into analysis rows. Un-comment + populate the `queues` producer/consumer block in `backend/wrangler.jsonc` (with a SECOND consumer draining the DLQ — H10), add the `EVENTS_QUEUE` binding to `Env`, add ONE `sendBatch` enqueue AFTER the ingest insert loop (a binding call, not an analytics import; unconditional on idempotency_key so ON-CONFLICT-DO-NOTHING zero-row inserts still enqueue — H11), and export an INLINED `queue()` handler from `backend/src/index.ts` that runs BOTH correlation and rule-based model-fit as two idempotent functions over each batch (Cloudflare allows only one push consumer per queue — D2-14; the wrapper module is inlined per H12/simplicity). Every message envelope carries a `table` discriminator + `event_type` + the source-row lookup key (idempotency_key + occurred_at); both worker functions re-read org_id from the persisted source row (the body org_id is a routing hint only — H9). Git-event enqueue is MANDATORY so a revert landing days after the prompt→commit is still processed, age-decoupled from the ±15-min join (H3). Correlation writes interval-confidence `prompt_outcomes` with explicit revert-downgrade state; model-fit writes `model_fit_scores` with score + signals + verdict, guarded by a no-LLM static-import test.

Purpose: The correlation + model-fit half of the backend analysis layer (ANL-01/02/03/04). It consumes Plan 01's shared analysis types + the enqueue point, and writes the `prompt_outcomes`/`model_fit_scores` tables created in Plan 04. The Plan 07 aggregator later reads these rows.

Output: queue config in wrangler.jsonc (main consumer + DLQ consumer), EVENTS_QUEUE in Env, the `sendBatch` enqueue in events-batch.ts (2nd modification after Plan 01's git-branch), the correlation + model-fit worker functions + parameterized upsert queries, the DLQ drain handler, the no-LLM guard test, the module-worker typecheck test, and the inlined `queue` export in index.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-parallel-adapters-backend-analysis-layer/02-CONTEXT.md
@.planning/phases/02-parallel-adapters-backend-analysis-layer/02-RESEARCH.md
@.planning/phases/02-parallel-adapters-backend-analysis-layer/02-EVIDENCE.md
@.planning/phases/02-parallel-adapters-backend-analysis-layer/02-PATTERNS.md
@.planning/phases/02-parallel-adapters-backend-analysis-layer/02-OPEN-QUESTIONS.md
@.planning/phases/02-parallel-adapters-backend-analysis-layer/02-01-PLAN.md
@.planning/phases/02-parallel-adapters-backend-analysis-layer/02-04-PLAN.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase + prior plans; use directly, do not re-explore. -->

From backend/src/api/events-batch.ts — AFTER Plan 01 (02-01) landed, the ingest handler loop has a `if (event.tool === "git") { await insertGitEvent(client, gitRow); } else { await insertAiEvent(client, aiRow); }` tool-discriminator branch, and the ai row now threads `repo_remote`/`repo_branch`/`cwd` from the envelope; both inserts use `ON CONFLICT (idempotency_key, occurred_at) DO NOTHING`. READ Plan 01's landed version FIRST. The enqueue this plan adds is a SECOND modification appended AFTER the whole insert loop (NOT inside it — RESEARCH C7.5, H11): collect one message per accepted event into an array during the loop, then issue a SINGLE `c.env.EVENTS_QUEUE.sendBatch(messages)` after the loop. org_id comes from `c.get("org_id")`, NEVER the body (T-05-02) — and is put into the message ONLY as a routing hint (the consumer re-reads it from the source row — H9). The canonical envelope carries `event.idempotency_key`, `event.occurred_at`, `event.tool`; the git payload carries `event_type`.

Enqueue is UNCONDITIONAL on the event's idempotency_key — do NOT gate it on "rows inserted", because `ON CONFLICT DO NOTHING` returns 0 rows on a re-ingested (already-persisted) event yet that event may still lack its prompt_outcomes/model_fit_scores row; gating on insert count would silently skip re-enqueue and the event would never be analyzed (H11). (The consumer's idempotent upserts make an occasional duplicate enqueue harmless.)

Message envelope shape (minimal, the consumer re-fetches the full row): `{ table: event.tool === "git" ? "git_events" : "ai_events", event_type: <git event_type | "prompt">, idempotency_key: event.idempotency_key, occurred_at: event.occurred_at, org_id /* ROUTING HINT ONLY */ }`. Git-event messages are MANDATORY (H3) — the git branch pushes a `table:'git_events'` message so the correlation worker can process a revert arriving days after the prompt→commit.

From backend/src/api/events-batch.hot-path.test.ts (EVIDENCE C30) — the ING-04 guard greps this handler's source for `from "...correlation"`, `from "...model-fit"`, `from "...aggregator"`, `from "...analysis"` (4 cases). The enqueue is `c.env.EVENTS_QUEUE.sendBatch(...)` — a BINDING call, NOT an import — so it does not trip the guard. Do NOT `import` any worker/analysis module into events-batch.ts.

From backend/src/env.ts — the `Env` interface (HYPERDRIVE, OAUTH_STATE_KV, secrets) + `Variables` (org_id, api_key_id, daemon_machine_id, hostname). Add `EVENTS_QUEUE: Queue` to `Env` mirroring the HYPERDRIVE binding line (the Cloudflare `Queue<T>` type from `@cloudflare/workers-types`).

From backend/src/index.ts — currently `export default app;` (Hono app). Cloudflare needs a module object with `{ fetch, queue }` (and later `scheduled` in Plan 07). INLINE the two-function batch handler here (H12/simplicity — no separate `correlation-and-model-fit.ts` wrapper): define `async function queue(batch: MessageBatch, env: Env, ctx: ExecutionContext)` that opens one pg client and, per message, calls `correlateEvent(msg.body, client)` + `runModelFit(msg.body, client)`; keep the default `app` as the fetch handler (Hono exposes `app.fetch`). Export: `export default { fetch: app.fetch, queue };`. See the batch-handler design in Task 3.

From backend/src/db/client.ts (S8) — `pgClient(env)` per-invocation; `await client.connect(); try { ... } finally { ctx.waitUntil(client.end()); }`. NEVER cache across invocations.

From backend/src/db/queries/ai-events.ts (S7) — the parameterized `$1..$N` + `ON CONFLICT ... DO NOTHING` idiom to mirror for `upsertPromptOutcome` / `upsertModelFitScore` (use `DO UPDATE SET ...` since a redelivery re-derives the same row; both keyed on the prompt idempotency_key). NOTE (H9): the source-row re-fetch used for tenancy is a parameterized SELECT on `ai_events`/`git_events` by `(idempotency_key, occurred_at)` returning `org_id` (+ the fields the scorer/correlator needs) — the tenant for every write comes from THAT row, never `msg.body.org_id`.

From @fennec/shared (authored in Plan 01) — `PromptOutcomeSchema` (org_id, prompt_event_id/idempotency_key, git_event_id nullable, confidence_band, confidence_low, confidence_high, attribution_state, downgraded_at, downgrade_reason) and `ModelFitScoreSchema` (score, fit_verdict, prompt_length, file_edit_size, tool_call_count, model, model_tier). Import these row types for the query/worker signatures.

From supabase/migrations/20260531000009_prompt_outcomes.sql + ...010_model_fit_scores.sql (Plan 04 — note the corrected numbering: prompt_outcomes is 009, model_fit_scores is 010) — the physical columns + PK `(prompt_idempotency_key, occurred_at)` the upserts target. prompt_outcomes has a nullable `git_event_id UUID` link (the revert path looks a prompt_outcomes row up by its git_event_id to the ORIGINAL commit — H3). From ...003_git_events_partitioned.sql (+ Plan 01/04 migration 015 which ADDs `idempotency_key TEXT` with UNIQUE `(idempotency_key, occurred_at)`) — git_events columns for the correlation join AND the git-message re-fetch: `id, org_id, user_id, occurred_at, repo_remote, repo_branch, event_type, payload, idempotency_key`. The git message is re-fetched by `(idempotency_key, occurred_at)` (H3/H9). The reverted-commit link parses the revert commit's payload subject (`Revert "<subject>"`) / reverted SHA (EVIDENCE C11) to find the ORIGINAL commit's git_events.id.

From EVIDENCE C15/C34 — the wrangler queue+cron config is VALID (`max_batch_size:100, max_batch_timeout:5, max_retries:3, dead_letter_queue`); the current `queues` block is commented in wrangler.jsonc lines 62-66. This plan adds BOTH the main consumer AND a second consumer entry that drains `fennec-events-dlq` (H10). From EVIDENCE C36/Q14 — ONE push consumer per queue is a documented Cloudflare limit (verify at live deploy); the main queue has ONE consumer running two functions, and the DLQ is a SEPARATE queue with its own single consumer (this does not violate the one-consumer-per-queue rule).

From EVIDENCE C43 (STATIC-VALIDATED, added by the Plan 06 replan — see 02-EVIDENCE.md) — a minimal `{ fetch, queue }` module object with `queue(batch: MessageBatch, env, ctx)` typechecks against the pinned `@cloudflare/workers-types`; the module-worker typecheck test binds this claim (H12).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Queue wiring — wrangler.jsonc producer + main-consumer + DLQ-consumer, Env binding, after-loop sendBatch enqueue (ANL-01, H3, H10, H11)</name>
  <files>backend/wrangler.jsonc, backend/src/env.ts, backend/src/api/events-batch.ts</files>
  <read_first>
    - backend/wrangler.jsonc (the commented Phase-2 queue stub, lines 62-66 — self-analog)
    - backend/src/env.ts (Env interface — add EVENTS_QUEUE mirroring HYPERDRIVE)
    - backend/src/api/events-batch.ts (READ Plan 01's landed version WITH the `event.tool === "git"` branch — the enqueue appends AFTER the whole loop, not inside it)
    - backend/src/api/events-batch.hot-path.test.ts (the ING-04 static-import guard that MUST stay green — EVIDENCE C30)
    - .planning/phases/02-parallel-adapters-backend-analysis-layer/02-01-PLAN.md (Task 3 — the git-discriminator branch + repo/branch/cwd threading this plan appends the enqueue to; ordering rationale)
    - .planning/phases/02-parallel-adapters-backend-analysis-layer/02-EVIDENCE.md (C15/C34 valid wrangler queue config; RESEARCH C7.5 after-loop enqueue placement)
  </read_first>
  <action>
In `backend/wrangler.jsonc`, replace the commented Phase-2 `queues` stub (lines 62-66) with a live block (EVIDENCE C15/C34, D2-13, H10): `"queues": { "producers": [{ "binding": "EVENTS_QUEUE", "queue": "fennec-events" }], "consumers": [{ "queue": "fennec-events", "max_batch_size": 100, "max_batch_timeout": 5, "max_retries": 3, "dead_letter_queue": "fennec-events-dlq" }, { "queue": "fennec-events-dlq", "max_batch_size": 100, "max_batch_timeout": 30, "max_retries": 1 }] }`. Keep the existing hyperdrive + kv blocks unchanged. Add comments that: (a) the DLQ (`fennec-events-dlq`) must be created at deploy (`wrangler queues create fennec-events-dlq`); (b) the SECOND consumer entry drains the DLQ into the `queueDlq` handler (Task 3 / index.ts) so poison messages are logged/alerted, not silently lost (H10) — the DLQ is a SEPARATE queue with its own single consumer, so the one-push-consumer-per-queue limit (EVIDENCE C36/Q14) is NOT violated; (c) the main `fennec-events` queue has ONE consumer running correlation + model-fit as two functions (D2-14).

In `backend/src/env.ts`, add `EVENTS_QUEUE: Queue;` to the `Env` interface (Cloudflare Workers `Queue<T>` type from `@cloudflare/workers-types`), documented alongside HYPERDRIVE as the Phase-2 producer binding.

In `backend/src/api/events-batch.ts`, build ONE message per accepted event INSIDE the loop into a local `messages` array, then issue a SINGLE `await c.env.EVENTS_QUEUE.sendBatch(messages)` AFTER the whole insert loop (RESEARCH C7.5, H11 — NOT a per-event `send` inside the loop). The message envelope is: `{ body: { table: event.tool === "git" ? "git_events" : "ai_events", event_type: event.tool === "git" ? gitPayload.event_type : "prompt", idempotency_key: event.idempotency_key, occurred_at: event.occurred_at, org_id } }`. Rules:
  - Enqueue is MANDATORY for BOTH branches, including the git branch (H3) — every git_events insert pushes a `table:'git_events'` message so a revert landing days after the prompt→commit is still processed (the correlation revert path is age-decoupled from the ±15-min join).
  - Enqueue UNCONDITIONALLY on the event's idempotency_key — do NOT gate on rows-inserted. `ON CONFLICT DO NOTHING` returns 0 for a re-ingested event, but that event may still lack its analysis rows; skipping re-enqueue would drop it from analysis forever (H11). The consumer's idempotent upserts make a duplicate enqueue harmless.
  - `org_id` (from `c.get("org_id")`, T-05-02) is placed in the message as a ROUTING HINT ONLY — the consumer re-reads org_id from the persisted source row for every write (H9). Never trust the message org_id for tenancy.
  - Push a message for an event even when its insert was a no-op conflict (so the message array is built per iterated event, not per new row).
CRITICAL (ING-04): `sendBatch` is a BINDING call (`env.EVENTS_QUEUE.sendBatch`) — do NOT import any `correlation`/`model-fit`/`aggregator`/`analysis` module into this file; the hot-path guard greps for exactly those import strings (EVIDENCE C30) and MUST stay green. Keep `accepted` counting identical.

Run the ING-04 guard to confirm it still passes after the enqueue add.
  </action>
  <verify>
    <automated>cd /Users/Tanmai.N/Documents/fennec && npx vitest run backend/src/api/events-batch.hot-path.test.ts backend/src/api/events-batch.test.ts && npm run typecheck && grep -c "EVENTS_QUEUE" backend/wrangler.jsonc && grep -c "fennec-events-dlq" backend/wrangler.jsonc && grep -c "EVENTS_QUEUE.sendBatch" backend/src/api/events-batch.ts</automated>
  </verify>
  <acceptance_criteria>
    - `backend/wrangler.jsonc` has a live `queues` block with one producer (`EVENTS_QUEUE`→`fennec-events`) + a main consumer (`max_batch_size:100`, `max_batch_timeout:5`, `max_retries:3`, `dead_letter_queue:fennec-events-dlq`) AND a second consumer draining `fennec-events-dlq` (H10) — `grep -c "fennec-events-dlq" backend/wrangler.jsonc` ≥2 (the DLQ named as the main queue's DLQ AND as a drained queue).
    - `backend/src/env.ts` `Env` includes `EVENTS_QUEUE: Queue`.
    - `backend/src/api/events-batch.ts` contains `c.env.EVENTS_QUEUE.sendBatch(` called ONCE after the loop (H11), builds a message for BOTH the ai and git branches (git enqueue mandatory — H3), and imports NO correlation/model-fit/aggregator/analysis module.
    - The message envelope carries `table`, `event_type`, `idempotency_key`, `occurred_at`, and `org_id` (routing hint) — asserted by an events-batch test that inspects the sendBatch argument (enqueue is unconditional on idempotency_key, H11; git branch enqueues, H3).
    - `npx vitest run backend/src/api/events-batch.hot-path.test.ts` exits 0 (all 4 ING-04 cases still green — ANL-01 preserves hot-path purity).
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>Queue producer + main consumer + DLQ consumer wired in wrangler.jsonc (H10); EVENTS_QUEUE in Env; ONE after-loop sendBatch enqueue, mandatory for git (H3) and unconditional on idempotency_key (H11), with a table-discriminator + event_type + routing-hint org_id envelope; ING-04 hot-path guard still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Correlation function — ±15-min join, interval confidence, age-decoupled revert-downgrade, source-row org_id (ANL-01/02/03, H3, H9, H10)</name>
  <files>backend/src/workers/correlation.ts, backend/src/workers/correlation.test.ts, backend/src/db/queries/prompt-outcomes.ts</files>
  <read_first>
    - backend/src/db/queries/ai-events.ts (parameterized $1..$N + ON CONFLICT idiom — S7)
    - backend/src/db/client.ts (pgClient lifecycle — S8)
    - supabase/migrations/20260531000009_prompt_outcomes.sql (physical columns + PK + nullable git_event_id link the upsert/revert-lookup target — Plan 04; note the corrected 009 numbering)
    - supabase/migrations/20260531000003_git_events_partitioned.sql (+ migration 015 adding idempotency_key) — git_events columns for the join AND the git-message re-fetch by (idempotency_key, occurred_at)
    - packages/shared/src/analysis/prompt-outcome.ts (PromptOutcomeSchema from Plan 01 — the row shape)
    - .planning/phases/02-parallel-adapters-backend-analysis-layer/02-CONTEXT.md (D2-16 window, D2-17 always-one-row, D2-18 interval, D2-19 revert-transition)
    - .planning/phases/02-parallel-adapters-backend-analysis-layer/02-OPEN-QUESTIONS.md (Q1 window value, Q5 confidence-band thresholds)
    - .planning/phases/02-parallel-adapters-backend-analysis-layer/02-EVIDENCE.md (C11 revert reflog + `Revert "<subject>"` subject rule for reverted-commit matching)
  </read_first>
  <behavior>
    - A prompt followed by a commit within ±15 min in the same repo/branch → one prompt_outcomes row linking the two, attribution_state='attributed', a confidence interval with both bounds populated (band + low<high).
    - A prompt with NO git activity in-window → one prompt_outcomes row with git_event_id NULL and attribution_state='no_outcome' (a row exists, D2-17 — never a missing row).
    - Confidence is an interval: the row NEVER carries a bare single number; confidence_low and confidence_high are both set with low<high (D2-18/ANL-02).
    - REVERT (age-decoupled, H3): a git revert message — even one arriving hours-to-days after the prompt→commit — parses the reverted commit (`Revert "<subject>"` subject / reverted SHA from the revert commit's payload) to find the ORIGINAL commit's git_events row, then finds the prompt_outcomes row LINKED to that commit via git_event_id (REGARDLESS of how old the prompt is — NOT constrained to the ±15-min window), and sets attribution_state='downgraded_by_revert' + downgraded_at + downgrade_reason on the EXISTING row (prior 'attributed' state + confidence remain traceable) — no aggregate total is decremented (D2-19/ANL-03).
    - TENANCY (H9): org_id is re-read from the persisted source row (looked up by the message's idempotency_key + occurred_at + table discriminator); the message-body org_id is a routing hint only and is NEVER the tenant for a write. A message whose body org_id was swapped/forged writes under the SOURCE row's org_id, not the forged one.
    - READ-AFTER-WRITE (H10): if the source row is not yet visible on re-fetch (Hyperdrive replica lag right after ingest), correlateEvent signals a TRANSIENT retry (throws a typed `RowNotYetVisibleError` the batch handler maps to `msg.retry()`), NOT a hard failure — the message is retried, not poisoned to the DLQ.
    - Idempotency: correlating the same prompt twice produces one row (upsert on the prompt key, D2-15), not two.
    - The ±window is a named config constant CORRELATION_WINDOW (default 15 min), not a literal buried in the SQL (D2-16/Q1). The revert path does NOT use this window.
  </behavior>
  <action>
Create `backend/src/db/queries/prompt-outcomes.ts` exporting: (1) `upsertPromptOutcome(client, row)` — a parameterized `$1..$N` INSERT into `prompt_outcomes (org_id, prompt_idempotency_key, occurred_at, git_event_id, confidence_band, confidence_low, confidence_high, attribution_state, downgraded_at, downgrade_reason)` with `ON CONFLICT (prompt_idempotency_key, occurred_at) DO UPDATE SET ...` (redelivery re-derives the same row — D2-15/S7); (2) `findOutcomeByGitEventId(client, { org_id, git_event_id })` — a parameterized SELECT of the prompt_outcomes row linked to a given ORIGINAL-commit git_event_id (the revert path's lookup, age-unbounded — H3); (3) `downgradeOnRevert(client, {org_id, prompt_idempotency_key, occurred_at, downgraded_at, downgrade_reason})` that `UPDATE prompt_outcomes SET attribution_state='downgraded_by_revert', downgraded_at=$_, downgrade_reason=$_ WHERE org_id=$_ AND prompt_idempotency_key=$_ AND occurred_at=$_ AND attribution_state <> 'downgraded_by_revert'` — a recorded transition, never a decrement (D2-19). All queries parameterized (T-05-03) and org_id-scoped.

Create `backend/src/workers/correlation.ts` exporting `export const CORRELATION_WINDOW_MINUTES = 15;` (D2-16 — a named tunable constant; note in a comment it can move to env/config once staging data exists, Q1), a `RowNotYetVisibleError` class (H10), and `export async function correlateEvent(msg, client): Promise<void>`. Logic:
  (0) Read the message discriminator: `msg.table` ('ai_events' | 'git_events') + `msg.event_type`. Do NOT trust `msg.org_id` for tenancy (H9).
  (1) SOURCE-ROW RE-FETCH + TENANCY: SELECT the source row by `(idempotency_key, occurred_at)` from the discriminated table (`ai_events` for prompts, `git_events` for git messages). If the SELECT returns no row, throw `RowNotYetVisibleError` (H10 — transient retry, not poison). Take `org_id` from THIS fetched row for every subsequent write (H9).
  (2) If `msg.table === 'git_events'` and `event_type === 'revert'` → run the REVERT PATH: from the revert commit's payload parse the reverted commit (`Revert "<subject>"` subject / reverted SHA — EVIDENCE C11), SELECT the ORIGINAL commit's git_events row (same org_id) by that subject/SHA, then `findOutcomeByGitEventId(client, { org_id, git_event_id: original.id })` and, if found, `downgradeOnRevert(...)` on it (age-unbounded — H3). Return. (Non-revert git messages: no prompt_outcomes write is required — a comment records that git commits are consumed by the prompt-side ±window join; document this.)
  (3) PROMPT PATH (`msg.table === 'ai_events'`): query git_events for the SOURCE row's org_id where `occurred_at BETWEEN prompt.occurred_at - (CORRELATION_WINDOW_MINUTES || ' minutes')::interval AND prompt.occurred_at + ...` AND matching `repo_remote`/`repo_branch` when present.
  (4) Derive confidence: rule-based band (D2-18/Q5) — tight time gap + same repo/branch + a commit touching files → 'high' with a narrow [low,high]; loose gap or branch mismatch → 'low' with a wide interval; in-between → 'medium'. Document the exact gap cutoffs + bound values as named constants (Claude's Discretion per Q5); ensure low<high (matches the strict DB CHECK, ANL-02).
  (5) Upsert exactly one prompt_outcomes row under the SOURCE row's org_id (linking git_event_id when matched, NULL + 'no_outcome' when not — D2-17).

Create `backend/src/workers/correlation.test.ts` with the behaviours above using the mock pg client (`backend/src/test-utils/mock-db.ts` `createMockClient` + `setHandler`): stub the source-row SELECT + git_events SELECT rows and assert the upsert params (band/bounds/state/git_event_id). Assert: the no-git case still upserts one row with 'no_outcome'; confidence has both bounds with low<high; a git revert arriving with an occurred_at FAR outside the ±15-min window still downgrades the linked prompt_outcomes row via `downgradeOnRevert` (age-decoupled — H3), asserted to be an UPDATE (not a DELETE/decrement); a message whose body org_id ≠ the source row's org_id writes under the SOURCE row's org_id (H9); a missing source row throws `RowNotYetVisibleError` (H10); the window uses `CORRELATION_WINDOW_MINUTES` (grep or param inspection).
  </action>
  <verify>
    <automated>cd /Users/Tanmai.N/Documents/fennec && npx vitest run backend/src/workers/correlation.test.ts && npm run typecheck && grep -c "CORRELATION_WINDOW" backend/src/workers/correlation.ts && grep -c "RowNotYetVisibleError" backend/src/workers/correlation.ts</automated>
  </verify>
  <acceptance_criteria>
    - `backend/src/db/queries/prompt-outcomes.ts` exports `upsertPromptOutcome` (parameterized, `ON CONFLICT ... DO UPDATE`), `findOutcomeByGitEventId`, and `downgradeOnRevert` (UPDATE to `downgraded_by_revert`, org_id-scoped, no decrement).
    - `backend/src/workers/correlation.ts` defines `CORRELATION_WINDOW_MINUTES` (=15) used in the prompt join — no bare 15-min literal in the SQL string (D2-16); the revert path does NOT use the window (H3).
    - Test: a no-git prompt still writes exactly ONE prompt_outcomes row with git_event_id NULL + 'no_outcome' (D2-17).
    - Test: every written row has confidence_low AND confidence_high with low<high (ANL-02); no bare single number.
    - Test: a git revert whose occurred_at is FAR outside the ±15-min window still downgrades the linked prompt via `downgradeOnRevert` (age-decoupled — H3), asserted to be an UPDATE, not a DELETE/decrement (ANL-03).
    - Test: a message with a mismatched body org_id writes under the SOURCE row's org_id (H9); a missing source row throws `RowNotYetVisibleError` (H10).
    - `npx vitest run backend/src/workers/correlation.test.ts` exits 0; `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>correlateEvent joins prompts to git_events in a ±15-min tunable window, always writes one interval-confidence prompt_outcomes row, re-reads org_id from the source row (H9), downgrades reverts via an age-decoupled recorded transition keyed on git_event_id (H3), treats a missing source row as a transient retry (H10), idempotent upsert; tests + typecheck green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Model-fit scorer + inlined consumer handler + DLQ drain + no-LLM guard + module-worker typecheck (ANL-04, H9, H10, H12)</name>
  <files>backend/src/workers/model-fit.ts, backend/src/workers/model-fit.test.ts, backend/src/workers/model-fit.no-llm.test.ts, backend/src/db/queries/model-fit-scores.ts, backend/src/workers/dlq-drain.ts, backend/src/workers/dlq-drain.test.ts, backend/src/workers/module-worker.typecheck.test.ts, backend/src/index.ts</files>
  <read_first>
    - backend/src/api/events-batch.ts (pg lifecycle S8 to mirror in the inlined queue() handler)
    - backend/src/db/queries/ai-events.ts (parameterized upsert idiom S7)
    - backend/src/api/events-batch.hot-path.test.ts (the EXACT static-import-grep structure to copy for the no-LLM guard — S9, EVIDENCE C30)
    - backend/src/index.ts (currently `export default app` — inline the queue handler + add the export per the Cloudflare module-worker convention)
    - backend/src/workers/correlation.ts (Task 2 — correlateEvent + RowNotYetVisibleError the handler maps to msg.retry())
    - packages/shared/src/analysis/model-fit.ts (ModelFitScoreSchema from Plan 01)
    - supabase/migrations/20260531000010_model_fit_scores.sql (physical columns from Plan 04 — note the corrected 010 numbering; model_pricing is 011 for the tier lookup)
    - .planning/phases/02-parallel-adapters-backend-analysis-layer/02-CONTEXT.md (D2-14 one-consumer-two-functions, D2-20 no-network, D2-21 signals+verdict, D2-22 model→tier data lookup)
    - .planning/phases/02-parallel-adapters-backend-analysis-layer/02-EVIDENCE.md (C43 module-worker typecheck; RESEARCH C7.3 MessageBatch handler signature)
    - .planning/phases/02-parallel-adapters-backend-analysis-layer/02-OPEN-QUESTIONS.md (Q6 heuristic weights/cutoffs)
  </read_first>
  <behavior>
    - scoreModelFit is a PURE function of the prompt event (+ optional correlated git edit size): prompt_length, file_edit_size, tool_call_count, model tier (from a data lookup, D2-22) → a numeric score + fit_verdict ∈ {under_powered, fit, over_powered}.
    - A long prompt + large edit + many tool calls on a frontier model → verdict 'fit'; a tiny prompt + tiny edit + no tool calls on a frontier model → 'over_powered' (a cheaper model would fit); a heavy task on a small model → 'under_powered'.
    - Exactly one model_fit_scores row per prompt, storing the score AND all input signals AND the verdict (D2-21); a redelivery upserts (one row, D2-15).
    - runModelFit re-reads org_id from the persisted source row (looked up by idempotency_key + occurred_at); the message-body org_id is NEVER the tenant for the write (H9 — same rule as correlateEvent). runModelFit only runs for `table:'ai_events'` prompt messages; for git messages it is a no-op. A not-yet-visible source row throws `RowNotYetVisibleError` (transient retry — H10).
    - The scorer makes NO network/LLM call — the static-import guard test asserts no fetch/http/undici/node:net/node:https import and no `fetch(` / `XMLHttpRequest` in model-fit.ts source (ANL-04/D2-20).
    - The INLINED queue() handler runs correlateEvent AND runModelFit over each message; per-message it acks on success, and on a `RowNotYetVisibleError` it calls `msg.retry()` (transient, H10) rather than failing the whole batch; a genuinely malformed message body (fails envelope validation) is `msg.ack()`-ed after logging so it flows to neither infinite-retry nor silent loss — but the Cloudflare max_retries→DLQ path handles repeated hard failures. A pg client is opened once per batch (S8).
    - The DLQ drain handler (`queueDlq`) logs + counts each dead-lettered message (structured log the platform can alert on) and supports manual replay (documented), so poison messages are visible, not silently lost (H10).
  </behavior>
  <action>
Create `backend/src/db/queries/model-fit-scores.ts` exporting `upsertModelFitScore(client, row)` — parameterized INSERT into `model_fit_scores (org_id, prompt_idempotency_key, occurred_at, score, fit_verdict, prompt_length, file_edit_size, tool_call_count, model, model_tier)` with `ON CONFLICT (prompt_idempotency_key, occurred_at) DO UPDATE SET ...` (D2-15/S7). Parameterized only (T-05-03).

Create `backend/src/workers/model-fit.ts` — a PURE `scoreModelFit(promptEvent, correlatedEditSize?)` (zero network — D2-20). Compute the four signals: `prompt_length` (chars/tokens from payload.prompt_text), `file_edit_size` (from the correlated git event if available, else 0), `tool_call_count` (count of tool_call-kind events in the session — Claude's Discretion on the source: from payload or a session query; document it), `model_tier` via a lookup (read the tier from `model_pricing.tier` (migration 011) for the event's model, or an in-memory map derived from that table — the point is data-driven per D2-22, NOT a giant switch; document the mechanism). Combine into a transparent weighted "task heaviness" score with NAMED documented weight/cutoff constants (Q6 — Claude's Discretion on values) and output `fit_verdict`. Store BOTH the score AND the signals (D2-21). Export `scoreModelFit` + an async `runModelFit(msg, client)` that: (a) returns early if `msg.table !== 'ai_events'` (git no-op); (b) re-fetches the ai_events source row by `(idempotency_key, occurred_at)`, throwing `RowNotYetVisibleError` if absent (H10); (c) takes org_id from THAT row (H9), scores it, and calls `upsertModelFitScore` under the source-row org_id. NO import of fetch/http/undici/node:net/node:https; NO `fetch(` call. (Reuse the `RowNotYetVisibleError` from correlation.ts — import it, don't redefine.)

Create `backend/src/workers/model-fit.no-llm.test.ts` — copy the `readFileSync` + `expect(source).not.toMatch(...)` structure VERBATIM from events-batch.hot-path.test.ts (S9). Assert `model-fit.ts` source contains no import of `fetch`/`http`/`undici`/`node:net`/`node:https` and no `fetch(` / `XMLHttpRequest` token (ANL-04). Use `grep -v '^ *//'`-equivalent care so a comment mentioning "no fetch" does not self-invalidate the grep (assert on `import ... from` and call-site patterns, not the bare word).

Create `backend/src/workers/model-fit.test.ts` — behaviour tests for the verdict logic (the three heaviness scenarios above) + the one-row-per-prompt upsert (mock pg client), asserting the upsert params include score + all signals + verdict, and that a message with a mismatched body org_id writes the score under the SOURCE row's org_id (H9), and a git-table message is a no-op.

Create `backend/src/workers/dlq-drain.ts` exporting `export async function queueDlq(batch: MessageBatch, env: Env, ctx: ExecutionContext): Promise<void>` (H10): for each message, emit a structured console log/metric (`{ event: "fennec.dlq.message", table, event_type, idempotency_key, attempts: msg.attempts }`) the platform can alert on, then `msg.ack()` (draining the DLQ so it does not back up). A header comment documents the MANUAL REPLAY path: an operator can re-enqueue a DLQ body onto `EVENTS_QUEUE` after fixing the root cause (the body carries the same envelope). This ensures poison/failed-analysis messages are surfaced + recoverable, never silently lost.

Create `backend/src/workers/dlq-drain.test.ts` — feed a mock DLQ MessageBatch and assert `queueDlq` logs one structured record per message (spy on console) and acks each (no throw, batch drained).

Create `backend/src/workers/module-worker.typecheck.test.ts` (H12, EVIDENCE C43) — a STATIC-VALIDATED-style test: import the default export from `../index.js` and assert at type + runtime level that it is an object exposing `fetch` and `queue` function members (and, once Plan 07 lands, `scheduled`). The real gate is that `npm run typecheck` compiles the `{ fetch, queue }` (later `{ fetch, queue, scheduled }`) object against `@cloudflare/workers-types` `ExportedHandler<Env>` with `MessageBatch`/`ScheduledController` handler signatures (C43). Add a comment referencing EVIDENCE C43 and that C15 covered only the wrangler config, not the TS handler contract.

In `backend/src/index.ts`, INLINE the two-function batch handler (H12/simplicity — do NOT create a separate `correlation-and-model-fit.ts` wrapper): `import { correlateEvent, RowNotYetVisibleError } from "./workers/correlation.js"; import { runModelFit } from "./workers/model-fit.js"; import { pgClient } from "./db/client.js";` then define
```
export async function queue(batch: MessageBatch, env: Env, ctx: ExecutionContext): Promise<void> {
  const client = pgClient(env);
  await client.connect();
  try {
    for (const msg of batch.messages) {
      try {
        await correlateEvent(msg.body, client);
        await runModelFit(msg.body, client);
        msg.ack();
      } catch (e) {
        if (e instanceof RowNotYetVisibleError) { msg.retry(); } // transient (H10)
        else { throw e; } // hard failure → Cloudflare retry→DLQ (H10)
      }
    }
  } finally { ctx.waitUntil(client.end()); }
}
```
(a comment records the Cloudflare one-push-consumer limit — EVIDENCE C36 — and that both functions re-read org_id from the source row, H9). Change the default export to `export default { fetch: app.fetch, queue };` (keep the Hono `app` as the fetch handler). Do NOT add `scheduled` here — that is Plan 07 (which appends to this same export object in Wave 4). The DLQ consumer `queueDlq` is bound to the `fennec-events-dlq` queue via wrangler; note that Cloudflare routes each queue's messages to the handler matching its consumer config — if a single `queue()` entrypoint must service both queues, branch on `batch.queue === "fennec-events-dlq"` to delegate to `queueDlq` (document the chosen wiring in the SUMMARY).
  </action>
  <verify>
    <automated>cd /Users/Tanmai.N/Documents/fennec && npx vitest run backend/src/workers && npm run typecheck && grep -c "batch.messages" backend/src/index.ts && grep -c "correlateEvent" backend/src/index.ts && grep -c "runModelFit" backend/src/index.ts && grep -c "queueDlq" backend/src/workers/dlq-drain.ts</automated>
  </verify>
  <acceptance_criteria>
    - `backend/src/workers/model-fit.no-llm.test.ts` mirrors the hot-path-guard grep and asserts no fetch/http/undici/node:net/node:https import + no `fetch(`/`XMLHttpRequest` in model-fit.ts (ANL-04) — bound to `npx vitest run backend/src/workers/model-fit.no-llm.test.ts`.
    - `backend/src/workers/model-fit.ts` produces one model_fit_scores row per prompt with score + all five signals + fit_verdict (D2-21), under the SOURCE row's org_id (H9) — asserted by model-fit.test.ts.
    - `backend/src/db/queries/model-fit-scores.ts` exports `upsertModelFitScore` (parameterized, ON CONFLICT DO UPDATE).
    - The INLINED handler in `backend/src/index.ts` runs BOTH `correlateEvent` and `runModelFit` per message, retries on `RowNotYetVisibleError` (H10), and there is NO `correlation-and-model-fit.ts` wrapper file (H12/simplicity) — `grep -c "correlateEvent" backend/src/index.ts` ≥1 and `grep -c "runModelFit" backend/src/index.ts` ≥1.
    - `backend/src/workers/dlq-drain.ts` exports `queueDlq` that logs + acks each DLQ message (H10) — asserted by dlq-drain.test.ts.
    - `backend/src/workers/module-worker.typecheck.test.ts` asserts the default export exposes `fetch` + `queue` and is bound to the `npm run typecheck` gate against `@cloudflare/workers-types` (H12, C43).
    - `backend/src/index.ts` default export is `{ fetch, queue }` (module-worker convention).
    - `npx vitest run backend/src/workers` exits 0; `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>Rule-based scoreModelFit (zero network, guarded, source-row org_id — H9), one model_fit_scores row/prompt with signals+verdict, the two-function consumer handler INLINED in index.ts (no wrapper — H12) with transient-retry on read-after-write (H10), a DLQ drain handler that surfaces poison messages (H10), and a module-worker typecheck test (H12/C43); tests + typecheck green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ingest hot path → queue | events-batch enqueues a minimal envelope via one after-loop sendBatch; must not import analytics (ING-04) |
| queue message → consumer | at-least-once delivery; a redelivered/tampered message must not create duplicate or cross-tenant rows; the body org_id is a routing hint, NOT a tenant authority |
| consumer → source row (ai_events/git_events) | the consumer re-fetches the source row by (idempotency_key, occurred_at) and derives org_id from it; a not-yet-visible row is a transient retry, not a failure |
| consumer → prompt_outcomes / model_fit_scores | worker queries must be parameterized and org_id-scoped from the source row (never the message body) |
| DLQ queue → DLQ drain handler | dead-lettered messages must be logged/alerted + replayable, never silently dropped |
| scorer → network | model-fit must make ZERO network/LLM calls (rule-based only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | DoS | Analytics import sneaks into the ingest hot path | HIGH | mitigate | enqueue is a BINDING call (`env.EVENTS_QUEUE.sendBatch`), no analytics import; ING-04 static-import guard kept green (Task 1, EVIDENCE C30) |
| T-06-02 | Tampering | Replayed/duplicated queue message writes duplicate outcome/score rows | MEDIUM | mitigate | idempotent upsert on `(prompt_idempotency_key, occurred_at)` — a redelivery re-derives one row (D2-15/S7); tests assert one row (Tasks 2/3) |
| T-06-03 | Tampering | SQL injection in worker queries | HIGH | mitigate | parameterized `$1..$N` only in prompt-outcomes.ts + model-fit-scores.ts + the source-row re-fetch (T-05-03); no string-concat of values |
| T-06-04 | Spoofing | Cross-tenant rows via a forged/replayed org_id in the queue message (BOTH correlation AND model-fit) | HIGH | mitigate | BOTH correlateEvent AND runModelFit re-read org_id from the persisted source row (re-fetched by idempotency_key + occurred_at + table discriminator); the body org_id is a ROUTING HINT ONLY, never the tenant for a write (H9, T-05-02); tests assert a mismatched body org_id writes under the source-row org_id (Tasks 2/3) |
| T-06-05 | Info disclosure / Exfiltration | model-fit calls an external LLM/endpoint leaking prompt text | HIGH | mitigate | rule-based scorer with a static-import no-LLM guard test (ANL-04/D2-20, S9); no fetch/http import allowed |
| T-06-06 | Repudiation | Revert silently subtracts from a total (ANL-03 anti-pattern) | MEDIUM | mitigate | `downgradeOnRevert` records an explicit state transition on the existing row (keyed on git_event_id, age-decoupled — H3); no aggregate decrement path exists (Task 2) |
| T-06-07 | Denial of Service / Data loss | Poison message silently lost (DLQ configured, no consumer) or an event never enqueued (per-event send fails after insert, or gated on 0-row conflict) → permanent analysis loss | HIGH | mitigate | (a) a SECOND consumer drains `fennec-events-dlq` to `queueDlq` which logs/alerts + supports manual replay (H10); (b) enqueue is ONE after-loop sendBatch, UNCONDITIONAL on idempotency_key (not gated on rows-inserted — H11); (c) a not-yet-visible source row is a transient `msg.retry()`, not a hard failure that poisons the message (H10) |
| T-06-08 | Repudiation / Correctness | A revert landing days after the prompt→commit never downgrades attribution (±15-min window misses it) | HIGH | mitigate | git enqueue is MANDATORY (H3); the revert path matches the reverted commit and looks up the prompt_outcomes row via git_event_id REGARDLESS of age, decoupled from the ±15-min join (Task 2) |
| T-06-SC | Tampering | New npm install | LOW | accept | No new runtime dependency — uses existing `pg`, Hono, `@fennec/shared`, and Cloudflare `Queue` binding types (@cloudflare/workers-types already present); nothing installed |
</threat_model>

<verification>
- `npx vitest run backend/src/api/events-batch.hot-path.test.ts backend/src/workers` all green.
- ING-04 guard still passes after the after-loop sendBatch enqueue (Task 1); the message envelope carries table + event_type + idempotency_key + occurred_at + routing-hint org_id; git enqueue is mandatory (H3); enqueue is unconditional on idempotency_key (H11).
- Correlation: no-git one-row, interval confidence (low<high), age-decoupled revert-downgrade via git_event_id (H3), source-row org_id (H9), transient-retry on missing source row (H10), idempotent upsert (Task 2).
- Model-fit: no-LLM static guard, one-row-with-signals-and-verdict, source-row org_id (H9) (Task 3).
- Consumer handler inlined in index.ts (no wrapper — H12); DLQ drain handler surfaces poison messages (H10); module-worker typecheck test green (H12/C43).
- `npm run typecheck` clean.
- Manual (build-time, gated on Plan 04 pushed + a live Cloudflare deploy — Q14): deploy the single consumer + the DLQ consumer to staging; confirm the main consumer drains fennec-events and writes prompt_outcomes + model_fit_scores, and that a deliberately-failing message lands in fennec-events-dlq and is logged by queueDlq.
</verification>

<success_criteria>
- Ingest enqueues onto EVENTS_QUEUE via one after-loop sendBatch, unconditional on idempotency_key, git-mandatory; hot-path purity preserved (ANL-01, H3, H11).
- One main consumer runs correlation + model-fit as two idempotent functions inlined in index.ts; a separate DLQ consumer surfaces poison messages (D2-14, H10, H12).
- BOTH worker functions re-read org_id from the source row; body org_id is a routing hint only (H9).
- prompt_outcomes carry interval confidence (ANL-02) + an age-decoupled revert-downgrade state via git_event_id (ANL-03, H3).
- model_fit_scores are rule-based (no-LLM guard), one row/prompt with signals + verdict (ANL-04).
</success_criteria>

<output>
Create `.planning/phases/02-parallel-adapters-backend-analysis-layer/02-06-SUMMARY.md` — record the confidence-band cutoffs + model-fit weights chosen (Q5/Q6), the tool_call_count + model→tier lookup mechanism (D2-22), the message envelope shape (table + event_type + idempotency_key + occurred_at + routing-hint org_id), the source-row org_id re-read mechanism (H9), the revert-via-git_event_id lookup (H3), the DLQ drain + manual-replay wiring (H10), and the inlined module-worker export shape in index.ts (so Plan 07 appends `scheduled` cleanly — H12).
</output>
