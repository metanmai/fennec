---
phase: 02-parallel-adapters-backend-analysis-layer
plan: 07
type: execute
wave: 4
depends_on: ["02-04", "02-06"]
files_modified:
  - backend/wrangler.jsonc
  - backend/src/index.ts
  - backend/src/workers/aggregator.ts
  - backend/src/workers/aggregator.test.ts
  - backend/src/workers/aggregator.reconcile.test.ts
  - backend/src/workers/no-hardcoded-price.test.ts
  - backend/src/workers/cost.ts
  - backend/src/workers/cost.test.ts
  - backend/src/db/queries/model-pricing.ts
  - backend/src/db/queries/daily-rollups.ts
autonomous: true
requirements: [ANL-05, ANL-06, ANL-07, ANL-08, ANL-09]

must_haves:
  truths:
    - "A cron-triggered scheduled() worker (NOT a queue consumer) reads the day's ai_events/git_events/prompt_outcomes/model_fit_scores and writes one daily_rollups_by_user + one daily_rollups_by_project per entity/day; re-running replaces the day (idempotent upsert) (ANL-05, D2-23, EVIDENCE C15)"
    - "The scheduled() run takes a per-day advisory lock (pg_try_advisory_xact_lock hashed on the target day) as an idempotent single-flight — an overlapping tick or manual re-trigger that fails to acquire the lock exits early instead of racing concurrent full-day upserts on the same (org_id,*,day) rows (H14)"
    - "Per-user rollups group by COALESCE(user_id::text, user_id_unknown); for NULL-user (Phase-2) machines a DETERMINISTIC synthetic UUID derived from user_id_unknown is written into the rollup user_id so per-machine events form DISTINCT rows under UNIQUE(org_id,user_id,day) instead of collapsing into one NULL row; the synthetic key is namespaced so Phase-3 attach can re-key (H4, OPEN-QUESTIONS Q23). Per-project rollups use the NULLABLE project_id with a per-org default-project fallback, deriving project from ai_events.repo_remote where available (H4)"
    - "cost_estimated = input×input_price + output×output_price + cache_creation×cache_creation_price + cache_read×cache_read_price — FOUR distinct cache-token line items, no 70%-collapse; a test asserts cache_creation and cache_read are distinct multiplications (ANL-06/D2-24)"
    - "cost_billed is a distinct column, null until reconciliation; cost_subscription is a distinct field NEVER summed into cost_estimated — tests confirm both (ANL-07/ANL-09, D2-25/D2-27)"
    - "cost_subscription is scoped to an ORG-LEVEL line this phase (no per-seat/assignment table exists yet) — it is NOT split per-user by guessing; per-user subscription attribution matures later (MEDIUM, OPEN-QUESTIONS Q23)"
    - "The cost model + seed cover only models with PROVEN token accounting this phase (Anthropic, migration 016); GPT/OpenAI pricing is deferred with a note (no surface reliably supplies GPT tokens) (LOW, OPEN-QUESTIONS Q15/Q24)"
    - "Prices are READ FROM model_pricing effective-date rows (occurred_at ∈ [effective_from, effective_to)); a grep-guard test asserts no numeric price literal in the aggregator/cost source (ANL-08, D2-26, PATTERNS S9)"
    - "Rollup totals reconcile with a direct raw-event query for the day — via BOTH a mocked-arithmetic unit test AND a DISTINCT staging-data reconciliation check (the mock only proves arithmetic; the staging check proves it against real seeded rows) (ANL-05 acceptance, H14)"
    - "The aggregator is org_id-scoped from the source rows and all queries are parameterized (T-05-02/T-05-03)"
  artifacts:
    - path: "backend/src/workers/aggregator.ts"
      provides: "scheduled(controller,env,ctx) cron: per-day advisory-lock single-flight (H14) → reads raw rows → groups per-user by COALESCE(user_id,user_id_unknown) + per-project by repo_remote→project/default (H4) → upserts daily rollups"
      contains: "scheduled"
    - path: "backend/src/workers/cost.ts"
      provides: "pure computeEstimatedCost — four distinct token×price line items, no hardcoded price"
      contains: "cache_creation"
    - path: "backend/src/workers/no-hardcoded-price.test.ts"
      provides: "grep guard — no numeric price literal in aggregator/cost source (ANL-08)"
      contains: "readFileSync"
    - path: "backend/src/workers/aggregator.reconcile.test.ts"
      provides: "staging-data reconciliation check DISTINCT from the mocked-arithmetic unit test — asserts rollup totals equal a direct raw-event aggregate against real seeded rows (H14)"
      contains: "reconcile"
    - path: "backend/src/db/queries/model-pricing.ts"
      provides: "effective-date price lookup by (model, token_kind, occurred_at) + org-level subscription lookup"
      exports: ["getEffectivePrice", "getSubscriptionCost"]
    - path: "backend/src/db/queries/daily-rollups.ts"
      provides: "parameterized idempotent upsert of daily_rollups_by_user/_by_project + a per-day advisory-lock helper (H14)"
      exports: ["upsertUserRollup", "upsertProjectRollup", "tryAcquireDayLock"]
  key_links:
    - from: "backend/src/workers/aggregator.ts"
      to: "daily_rollups_by_user / daily_rollups_by_project"
      via: "ON CONFLICT (org_id,user_id,day) / (org_id,project_id,day) DO UPDATE (replace the day)"
      pattern: "ON CONFLICT"
    - from: "backend/src/workers/cost.ts"
      to: "model_pricing"
      via: "getEffectivePrice(model, token_kind, occurred_at) — effective-date row"
      pattern: "getEffectivePrice"
    - from: "backend/src/index.ts"
      to: "cron handler"
      via: "export { scheduled } sibling of fetch + queue"
      pattern: "scheduled"
---

<objective>
Build the daily aggregator cron and the cost model. Add a Cloudflare `triggers.crons` entry and a `scheduled()` handler (a SEPARATE cron trigger, NOT a queue consumer — D2-14/D2-23) that, under a per-day advisory-lock single-flight (H14), reads the day's raw `ai_events`/`git_events`/`prompt_outcomes`/`model_fit_scores` and writes one `daily_rollups_by_user` + one `daily_rollups_by_project` per entity/day via an idempotent upsert. Per-user rollups group by `COALESCE(user_id::text, user_id_unknown)` so per-machine NULL-user events don't collapse to one row; per-project rollups use the nullable `project_id` + per-org default-project fallback, deriving project from `ai_events.repo_remote` where available (H4). Cost is estimated with FOUR distinct cache-token line items (no 70%-collapse — ANL-06), prices READ FROM the `model_pricing` effective-date table (never hardcoded — grep-guarded, ANL-08), with `cost_billed` distinct + null until reconciliation (ANL-07) and `cost_subscription` distinct, ORG-LEVEL, never summed into the estimate (ANL-09). The cost model + seed cover only Anthropic (proven token accounting); GPT is deferred with a note.

Purpose: The aggregator half of the backend analysis layer — the last piece of Phase-2 backend analysis. It reads the `prompt_outcomes`/`model_fit_scores` written by Plan 06, costs raw events against Plan 04's `model_pricing`, and populates the rollup tables the Phase-4 dashboard reads. It shares `backend/src/index.ts` + `backend/wrangler.jsonc` with Plan 06, so it runs in a later wave (appends `scheduled` to the module-worker export + adds the cron trigger).

Output: cron trigger in wrangler.jsonc, the `scheduled` export in index.ts, the aggregator worker (with the advisory-lock single-flight + H4 grouping), the pure cost module + effective-date price query + org-level subscription lookup + rollup upsert queries + day-lock helper, the no-hardcoded-price grep guard, and a staging-data reconciliation check distinct from the mocked unit test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-parallel-adapters-backend-analysis-layer/02-CONTEXT.md
@.planning/phases/02-parallel-adapters-backend-analysis-layer/02-RESEARCH.md
@.planning/phases/02-parallel-adapters-backend-analysis-layer/02-EVIDENCE.md
@.planning/phases/02-parallel-adapters-backend-analysis-layer/02-PATTERNS.md
@.planning/phases/02-parallel-adapters-backend-analysis-layer/02-OPEN-QUESTIONS.md
@.planning/phases/02-parallel-adapters-backend-analysis-layer/02-04-PLAN.md
@.planning/phases/02-parallel-adapters-backend-analysis-layer/02-06-PLAN.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase + prior plans; use directly, do not re-explore. -->

From backend/src/db/client.ts (S8) — `pgClient(env)` per-invocation lifecycle: `await client.connect(); try { ... } finally { ctx.waitUntil(client.end()); }`. The `scheduled()` handler uses this exact lifecycle.

From backend/src/db/queries/ai-events.ts (S7) — the parameterized `$1..$N` idiom to mirror. The four Anthropic Usage token fields live inside `ai_events.payload` (per AnthropicUsageSchema; EVIDENCE C31): `input_tokens`, `output_tokens`, `cache_creation_input_tokens`, `cache_read_input_tokens` — read them from payload JSONB. `ai_events` also carries `user_id UUID NULL`, `user_id_unknown TEXT` (= `"unknown@${hostname}"` pre-attach — the per-machine synthetic identity), and (after Plan 01/04) `repo_remote`/`repo_branch`/`cwd` used for the H4 per-project derivation.

From backend/src/index.ts — after Plan 06 it is `export default { fetch: app.fetch, queue };`. This plan APPENDS `scheduled` to that object: `export default { fetch: app.fetch, queue, scheduled };` (import `scheduled` from ./workers/aggregator.js). READ Plan 06's landed shape FIRST — do not clobber the `queue` export.

From backend/wrangler.jsonc — after Plan 06 it has a live `queues` block. This plan ADDS a sibling `"triggers": { "crons": ["0 2 * * *"] }` (daily 02:00 UTC — EVIDENCE C15 proved a crons config is valid). Do not remove the queues block.

From supabase/migrations/20260531000011_model_pricing.sql (Plan 04 — note the corrected 011 numbering; it is a PLAIN REFERENCE table, NO org_id, read-any RLS) — columns for the effective-date lookup: `model`, `pricing_kind` ('per_token'|'subscription'), `token_kind` ('input'|'output'|'cache_creation'|'cache_read'|null), `price_per_mtok`, `monthly_price`, `tier`, `effective_from`, `effective_to`. Effective-date rule: pick the row where `occurred_at >= effective_from AND (effective_to IS NULL OR occurred_at < effective_to)` — the half-open `[effective_from, effective_to)` window the EXCLUDE constraint enforces (EVIDENCE C16). Migration 016 seeds ONLY Anthropic per-token + subscription rows — NO GPT rows this phase (LOW deferral, Q15/Q24) — so the cost model exercises only proven-token models; GPT events fall through to the null-token graceful path.

From supabase/migrations/20260531000012_daily_rollups_by_user.sql + ...013_daily_rollups_by_project.sql (Plan 04 — corrected 012/013 numbering) — upsert targets: UNIQUE `(org_id, user_id, day)` / `(org_id, project_id, day)`; columns `event_count`, `cost_estimated` (NOT NULL), `cost_billed` (NULL), `cost_subscription` (NOT NULL default 0). `daily_rollups_by_user.user_id` is `UUID NULL`; `daily_rollups_by_project.project_id` is `UUID NULL` with a per-org default-project fallback this phase (H4). NOTE (H4 — per-user grouping): Phase-2 events have `user_id=NULL` (identity attaches in Phase 3), so grouping raw on `user_id` would collapse every machine into ONE NULL rollup row. To keep per-user (per-machine) rollups DISTINCT this phase, the aggregator groups SOURCE rows by `COALESCE(user_id::text, user_id_unknown)` and, for NULL-user machines, writes a DETERMINISTIC synthetic UUID derived from `user_id_unknown` (e.g. `uuidv5(user_id_unknown, FENNEC_NS)` or a stable hash-to-uuid) into the rollup's `user_id` column — so `UNIQUE (org_id, user_id, day)` yields one row PER MACHINE, not one shared NULL row. The synthetic UUID is namespaced/documented so Phase-3 attach can re-key (the raw `user_id_unknown → real user_id` backfill in `attachUser` re-derives it); the synthetic key is a rollup-layer convenience, never written back onto `ai_events`. Real `user_id` (once non-NULL) is used directly. Log the maturation to OPEN-QUESTIONS Q23. This keeps arithmetic per-machine-correct so reconciliation holds.

From backend/src/api/events-batch.hot-path.test.ts (S9, EVIDENCE C30) — the `readFileSync` + `expect(source).not.toMatch(...)` grep-guard structure to copy for the no-hardcoded-price test.

From EVIDENCE C31 — the four usage fields round-trip verbatim on the canonical schema (four-distinct-multiplication cost is possible, no 70%-collapse). From EVIDENCE C41/Q12/Q15 — subscription + per-token prices are DATA in model_pricing (Plan 04 seed), volatile, re-verified at build; the worker NEVER hardcodes a number.

From EVIDENCE C43 (STATIC-VALIDATED) — the `{ fetch, queue, scheduled }` module-worker object with a `scheduled(controller: ScheduledController, env, ctx)` handler typechecks against the pinned `@cloudflare/workers-types`; appending `scheduled` to Plan 06's `{ fetch, queue }` export is type-safe (H12). Plan 06's `module-worker.typecheck.test.ts` extends to assert the `scheduled` member once this plan lands.

Postgres advisory-lock idiom (H14) — a per-day idempotent single-flight: `SELECT pg_try_advisory_xact_lock($1)` where `$1` is a stable 64-bit key derived from the target day (e.g. `hashtext('fennec-rollup-' || day)::bigint` or a `('x' || md5(...))::bit(64)::bigint` cast) inside a transaction. If it returns false, another run holds the lock → the aggregator exits early (no concurrent full-day upsert race). `pg_try_advisory_xact_lock` auto-releases at transaction end, so no explicit unlock/leak. All parameterized (T-05-03).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cost model — four-distinct-token-line-item estimate + effective-date price lookup + no-hardcoded-price guard (ANL-06/07/08/09)</name>
  <files>backend/src/workers/cost.ts, backend/src/workers/cost.test.ts, backend/src/workers/no-hardcoded-price.test.ts, backend/src/db/queries/model-pricing.ts</files>
  <read_first>
    - backend/src/db/queries/ai-events.ts (parameterized query idiom S7)
    - supabase/migrations/20260531000011_model_pricing.sql (model_pricing columns + effective-date semantics from Plan 04 — note the corrected 011 numbering; seed 016 is Anthropic-only)
    - backend/src/api/events-batch.hot-path.test.ts (EXACT grep-guard structure to copy — S9, EVIDENCE C30)
    - .planning/phases/02-parallel-adapters-backend-analysis-layer/02-CONTEXT.md (D2-24 four line items, D2-25 estimated/billed, D2-26 price-from-table, D2-27 subscription-separate)
    - .planning/phases/02-parallel-adapters-backend-analysis-layer/02-EVIDENCE.md (C16 effective-date selection, C31 four usage fields, C38 GPT prices UNVERIFIABLE, C41 corrected subscription prices)
    - .planning/phases/02-parallel-adapters-backend-analysis-layer/02-OPEN-QUESTIONS.md (Q15 pricing volatile, Q23 subscription per-user maturation, Q24 GPT deferral)
  </read_first>
  <behavior>
    - computeEstimatedCost multiplies EACH of the four token kinds by its OWN price: input×input_price + output×output_price + cache_creation×cache_creation_price + cache_read×cache_read_price — four distinct terms (D2-24/ANL-06).
    - A test asserts cache_creation and cache_read are DISTINCT multiplications (e.g. different prices produce a result that could not arise from collapsing them into one input figure — no 70%-collapse).
    - getEffectivePrice(model, token_kind, occurred_at) returns the price from the model_pricing row whose [effective_from, effective_to) window contains occurred_at; an event before the Sonnet-5 cutover gets the intro price, after gets the new price (EVIDENCE C16).
    - Subscription cost is computed SEPARATELY (pricing_kind='subscription', monthly_price) at the ORG level and returned as its own value — never added into the per-token estimate (D2-27/ANL-09). This phase there is NO per-seat/assignment table, so subscription is NOT split per-user by guessing — it is an org-level line (Q23).
    - The cost/aggregator source contains NO numeric price literal — all prices come from model_pricing (grep guard, ANL-08/D2-26).
    - A Gemini/Copilot/GPT event with absent token usage yields a 0 (or null-token-safe) estimate without throwing (graceful degradation, Q9/Q24 / EVIDENCE C10/C38). Because the seed (016) is Anthropic-only, a GPT/Gemini/Copilot event has no per-token price row and costs 0 this phase — documented, not a bug.
  </behavior>
  <action>
Create `backend/src/db/queries/model-pricing.ts` exporting `getEffectivePrice(client, { model, token_kind, occurred_at })` — a parameterized SELECT of `price_per_mtok` from `model_pricing WHERE model=$1 AND token_kind=$2 AND pricing_kind='per_token' AND effective_from <= $3 AND (effective_to IS NULL OR effective_to > $3)` (the half-open window, EVIDENCE C16); it returns undefined/null-safe when no row matches (e.g. GPT this phase — Q24), which computeEstimatedCost treats as 0. Add `getSubscriptionCost(client, { org_id, day })` selecting the sum of `monthly_price` for `pricing_kind='subscription'` rows active on the day — an ORG-LEVEL total this phase (no per-seat table exists — Q23; do NOT attempt per-user split). Add a batch/caching helper (Claude's Discretion) so the aggregator does not issue one query per event — e.g. load all effective per-token prices for the day's models once into a map. All parameterized (T-05-03). The price VALUES live only in the DB rows (Plan 04 seed 016, Anthropic-only) — this file contains NO numeric price constant.

Create `backend/src/workers/cost.ts` exporting `computeEstimatedCost(usage, priceLookup)` — a PURE function taking the four token counts (`input_tokens`, `output_tokens`, `cache_creation_input_tokens`, `cache_read_input_tokens`, any of which may be undefined→treat as 0) and a price lookup (the per-token prices resolved via getEffectivePrice), returning `input_tokens/1e6 × input_price + output_tokens/1e6 × output_price + cache_creation_input_tokens/1e6 × cache_creation_price + cache_read_input_tokens/1e6 × cache_read_price` — FOUR named distinct terms (D2-24). Document the per-MTok scaling. NO hardcoded price — the prices are the function's inputs. Keep subscription cost OUT of this function (it is computed separately and surfaced as cost_subscription — D2-27).

Create `backend/src/workers/cost.test.ts` — assert: (a) four-distinct-multiplication (give distinct prices per token kind and assert the sum equals the four-term formula, and that cache_creation and cache_read contribute independently — a test where cache_read_price=0 but cache_creation_price>0 yields a different result than the reverse); (b) absent token fields → 0 contribution, no throw; (c) subscription is NOT part of computeEstimatedCost's output; (d) a model with NO matching per-token price row (the GPT-deferred case — Q24) yields 0, no throw (graceful degradation).

Create `backend/src/workers/no-hardcoded-price.test.ts` — copy the events-batch.hot-path.test.ts `readFileSync` + `expect(source).not.toMatch(...)` structure (S9). Read `cost.ts` AND `aggregator.ts` (Task 2) source and assert no numeric price literal appears in the cost math (ANL-08). Grep hygiene (per planner rule): strip comment lines before counting so a comment like "prices come from model_pricing, e.g. $5/MTok" does not self-invalidate the guard — assert the pattern only against non-comment lines (e.g. filter out lines beginning with `//` / ` *`). Define the forbidden pattern narrowly (a bare decimal used as a per-token/per-month rate in a multiplication), and document the pattern in a comment.
  </action>
  <verify>
    <automated>cd /Users/Tanmai.N/Documents/fennec && npx vitest run backend/src/workers/cost.test.ts backend/src/workers/no-hardcoded-price.test.ts && npm run typecheck && grep -c "cache_creation" backend/src/workers/cost.ts && grep -c "getEffectivePrice" backend/src/db/queries/model-pricing.ts</automated>
  </verify>
  <acceptance_criteria>
    - `computeEstimatedCost` has four distinct token×price terms — `grep -c "cache_creation" backend/src/workers/cost.ts` ≥1 AND `grep -c "cache_read" backend/src/workers/cost.ts` ≥1, and the test proves the two cache terms are independent (no 70%-collapse — ANL-06/D2-24).
    - `getEffectivePrice` selects by the half-open `[effective_from, effective_to)` window; a test proves before/after the Sonnet-5 cutover returns different prices (EVIDENCE C16).
    - `no-hardcoded-price.test.ts` asserts no numeric price literal in cost.ts + aggregator.ts (ANL-08/D2-26) using comment-stripped grep — bound to `npx vitest run backend/src/workers/no-hardcoded-price.test.ts`.
    - Subscription cost is computed separately, ORG-LEVEL (no per-user split — Q23), and NOT inside computeEstimatedCost (D2-27) — asserted by cost.test.ts; `getSubscriptionCost` takes `{ org_id, day }`.
    - A model with no matching per-token price row (GPT this phase — Q24) costs 0 without throwing — asserted by cost.test.ts.
    - `npx vitest run backend/src/workers/cost.test.ts backend/src/workers/no-hardcoded-price.test.ts` exits 0; `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>Pure four-line-item cost estimate (no cache-collapse), effective-date price lookup from model_pricing (Anthropic-only seed; GPT deferred → 0), org-level subscription computed separately (Q23), no-hardcoded-price grep guard green; tests + typecheck green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Aggregator cron — advisory-lock single-flight, H4 user/project grouping, rollups + reconcile (ANL-05/07/09, H4, H14)</name>
  <files>backend/src/workers/aggregator.ts, backend/src/workers/aggregator.test.ts, backend/src/workers/aggregator.reconcile.test.ts, backend/src/db/queries/daily-rollups.ts, backend/wrangler.jsonc, backend/src/index.ts</files>
  <read_first>
    - backend/src/db/client.ts (pgClient lifecycle S8)
    - backend/src/db/queries/ai-events.ts (parameterized upsert idiom S7; the four usage fields live in payload — EVIDENCE C31; user_id/user_id_unknown/repo_remote columns for H4 grouping; attachUser re-key idiom)
    - backend/src/workers/cost.ts + backend/src/db/queries/model-pricing.ts (Task 1 — computeEstimatedCost + getEffectivePrice + getSubscriptionCost)
    - supabase/migrations/20260531000012_daily_rollups_by_user.sql + ...013_daily_rollups_by_project.sql (upsert targets + columns from Plan 04 — corrected 012/013 numbering; user_id/project_id both NULLABLE)
    - backend/src/index.ts (READ Plan 06's landed `export default { fetch, queue }` — append scheduled without clobbering queue; the queue handler + queueDlq are inlined/imported there)
    - backend/wrangler.jsonc (READ Plan 06's landed queues block incl. the DLQ consumer — add triggers.crons sibling; EVIDENCE C15)
    - .planning/phases/02-parallel-adapters-backend-analysis-layer/02-CONTEXT.md (D2-23 cron-not-consumer + idempotent day-replace; D2-24/25/27 cost fields)
    - .planning/phases/02-parallel-adapters-backend-analysis-layer/02-OPEN-QUESTIONS.md (Q20 repo→project mapping maturation; Q23 subscription per-user maturation)
  </read_first>
  <behavior>
    - The scheduled() run FIRST acquires a per-day advisory lock (H14): `SELECT pg_try_advisory_xact_lock($1)` inside the aggregation transaction, `$1` = a stable 64-bit key derived from the target day. If the lock is NOT acquired (an overlapping tick or manual re-trigger is already running), the run exits early WITHOUT writing — no concurrent full-day upsert race on the same (org_id,*,day) rows. The lock auto-releases at transaction end.
    - The scheduled() handler reads the day's ai_events (with payload token fields), git_events, prompt_outcomes, model_fit_scores, groups by user and by project, and writes one daily_rollups_by_user + one daily_rollups_by_project row per entity/day.
    - Per-user grouping (H4): group source rows by `COALESCE(user_id::text, user_id_unknown)` so per-machine NULL-user events form DISTINCT rows; for NULL-user machines write a DETERMINISTIC synthetic UUID derived from user_id_unknown into the rollup user_id (namespaced so Phase-3 attach can re-key — Q23). Real user_id is used directly when non-NULL.
    - Per-project grouping (H4): derive project from `ai_events.repo_remote` where available (canonicalise minimally — Claude's Discretion), else the per-org default project; write the resolved (nullable) project_id. Full repo→project resolution matures Phase 3/4 (Q20) — document the mapping.
    - Re-running the cron for the same day REPLACES that day's rollup rows (idempotent upsert ON CONFLICT DO UPDATE — D2-23), never appends a duplicate.
    - cost_estimated is the sum of computeEstimatedCost over the entity's events (four-line-item, effective-date priced); cost_billed is written NULL (no reconciliation this phase — D2-25); cost_subscription is written as a distinct ORG-LEVEL value (getSubscriptionCost({org_id,day}) — no per-seat split, Q23), NEVER folded into cost_estimated (D2-27/ANL-09).
    - Reconciliation is proven TWO ways: (a) a mocked-arithmetic unit test (proves the four-line-item math), AND (b) a DISTINCT staging-data reconciliation check that runs against real seeded rows and asserts the rollup totals equal a direct raw-event aggregate for the day (H14 — the mock only proves arithmetic; the staging check proves it end-to-end).
    - org_id comes from the source rows; all queries parameterized (T-05-02/T-05-03).
  </behavior>
  <action>
Create `backend/src/db/queries/daily-rollups.ts` exporting: (1) `tryAcquireDayLock(client, day)` — `SELECT pg_try_advisory_xact_lock($1) AS acquired` with a parameterized stable 64-bit key derived from `day` (e.g. `hashtext('fennec-rollup-' || $1)::bigint` computed in SQL, or compute the bigint in TS and pass it as `$1`); returns the boolean (H14). Must run inside the same transaction as the upserts so the xact-scoped lock covers the whole day-write. (2) `upsertUserRollup(client, row)` and `upsertProjectRollup(client, row)` — parameterized INSERTs into the two rollup tables with `ON CONFLICT (org_id, user_id, day) DO UPDATE SET event_count=$_, cost_estimated=$_, cost_billed=$_, cost_subscription=$_` (and the project variant `(org_id, project_id, day)`) — the day-replace idempotency (D2-23/S7). `cost_billed` is passed NULL (D2-25). Parameterized only.

Create `backend/src/workers/aggregator.ts` exporting `export async function scheduled(controller: ScheduledController, env: Env, ctx: ExecutionContext): Promise<void>` (D2-23 — a cron handler, explicitly NOT a queue consumer; a comment records this). Logic (S8 lifecycle):
  (1) determine the target day (yesterday/today from controller.scheduledTime — Claude's Discretion; document it).
  (2) BEGIN a transaction and `tryAcquireDayLock(client, day)` (H14) — if not acquired, ROLLBACK/return early (single-flight; a concurrent run owns the day). Do all reads + upserts inside this transaction so the lock holds until commit.
  (3) Load the day's per-token effective prices once via the model-pricing helper (Task 1).
  (4) Query the day's ai_events grouped per-user by `COALESCE(user_id::text, user_id_unknown)` (H4) and per-project by the repo_remote→project/default-project derivation (H4/Q20 — document the mapping).
  (5) For each entity: sum event_count, sum `computeEstimatedCost` over its events (four line items, effective-date priced — Task 1); compute cost_subscription from `getSubscriptionCost({ org_id, day })` (org-level — Q23); leave cost_billed NULL.
  (6) upsert one user rollup + one project rollup per entity (day-replace); for NULL-user machines the rollup user_id is the deterministic synthetic UUID (H4).
  (7) COMMIT (releasing the advisory lock).
  Never sum cost_subscription into cost_estimated (D2-27). NO hardcoded price (all from model_pricing — Task 1). org_id from the source rows (T-05-02).

Create `backend/src/workers/aggregator.test.ts` — use the mock pg client to feed a day of seeded ai_events (with the four token fields; some with user_id NULL + distinct user_id_unknown values, some with distinct repo_remote) + a couple of git_events/prompt_outcomes, run `scheduled`, and assert: (a) the advisory lock is attempted and, when `pg_try_advisory_xact_lock` returns false, the run writes NO rollups (H14 single-flight); (b) two machines with distinct user_id_unknown produce TWO distinct user rollup rows (not one collapsed NULL row — H4), each with a deterministic synthetic user_id; (c) events with distinct repo_remote map to distinct project rollups, and events with no resolvable repo map to the default project (H4); (d) cost_estimated equals the four-line-item sum for the seeded tokens at the seeded prices; (e) cost_billed is NULL (D2-25); (f) cost_subscription is a distinct org-level value and is NOT included in cost_estimated (subscription>0 → cost_estimated excludes it — ANL-09/Q23); (g) re-running produces the same rows via DO UPDATE (idempotent day-replace — D2-23).

Create `backend/src/workers/aggregator.reconcile.test.ts` (H14 — DISTINCT from the mocked-arithmetic test) — a staging-data reconciliation check: gated on Plan 04 pushed + a reachable Postgres (guard/skip when the DB env is absent so the suite stays runnable offline), seed a known set of ai_events for a day into the real tables, run `scheduled`, then assert the written rollup `SUM(cost_estimated)` and `SUM(event_count)` EQUAL a direct raw-event aggregate query over the same day's source rows (ANL-05 end-to-end). A comment documents that this proves reconciliation against real rows, not just the mock's arithmetic.

In `backend/wrangler.jsonc`, add a `"triggers": { "crons": ["0 2 * * *"] }` sibling of the queues block (daily 02:00 UTC — EVIDENCE C15; the exact schedule is Claude's Discretion, document it). Do NOT touch the queues block (incl. the DLQ consumer) Plan 06 added.

In `backend/src/index.ts`, import `scheduled` from `./workers/aggregator.js` and append it to the module-worker default export: `export default { fetch: app.fetch, queue, scheduled };` (READ Plan 06's landed `{ fetch, queue }` first — extend, do not replace). The Plan 06 `module-worker.typecheck.test.ts` now also covers the `scheduled` member (EVIDENCE C43).
  </action>
  <verify>
    <automated>cd /Users/Tanmai.N/Documents/fennec && npx vitest run backend/src/workers/aggregator.test.ts && npm run typecheck && grep -c "scheduled" backend/src/index.ts && grep -c "crons" backend/wrangler.jsonc && grep -c "ON CONFLICT" backend/src/db/queries/daily-rollups.ts && grep -c "advisory" backend/src/db/queries/daily-rollups.ts</automated>
  </verify>
  <acceptance_criteria>
    - `backend/src/workers/aggregator.ts` exports `scheduled` (a cron handler, NOT a queue consumer), uses the pgClient lifecycle (S8), and acquires a per-day advisory lock as an idempotent single-flight before writing (H14) — a lock-not-acquired run writes no rollups (asserted).
    - `backend/src/db/queries/daily-rollups.ts` exports `tryAcquireDayLock` (H14), `upsertUserRollup`, `upsertProjectRollup` with `ON CONFLICT ... DO UPDATE` (day-replace idempotency — D2-23).
    - Per-user rollups are DISTINCT per machine via COALESCE(user_id, user_id_unknown) + a deterministic synthetic user_id (not one NULL row — H4); per-project rollups derive project from repo_remote with a default-project fallback (H4) — both asserted by aggregator.test.ts.
    - Reconciliation is proven by BOTH aggregator.test.ts (mocked arithmetic) AND aggregator.reconcile.test.ts (staging-data, DISTINCT — H14) — the latter asserts rollup totals equal a direct raw aggregate against real seeded rows (ANL-05).
    - `cost_billed` written NULL (D2-25); `cost_subscription` distinct, org-level (Q23), and NOT summed into cost_estimated (ANL-09) — asserted by aggregator.test.ts.
    - `backend/wrangler.jsonc` has `triggers.crons`; `backend/src/index.ts` default export is `{ fetch, queue, scheduled }` (Plan 06's queue + DLQ wiring preserved).
    - `npx vitest run backend/src/workers/aggregator.test.ts` exits 0; `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>scheduled() cron takes a per-day advisory-lock single-flight (H14), groups per-user by COALESCE(user_id,user_id_unknown) with a deterministic synthetic key + per-project by repo_remote/default (H4), writes one user + one project rollup per entity with four-line-item cost_estimated, null cost_billed, distinct org-level cost_subscription; idempotent day-replace; totals reconcile via both a mocked unit test AND a distinct staging-data check (H14); cron trigger + scheduled export wired; tests + typecheck green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw event rows → aggregator | the cron reads ai_events/git_events/prompt_outcomes/model_fit_scores and must scope by org_id from those rows |
| model_pricing rows → cost math | prices are data; a hardcoded price in code would break the effective-date single-source-of-truth (ANL-08) |
| cron trigger → DB | a cron run writes rollups; a re-run must replace, not duplicate, the day; overlapping runs must not race concurrent full-day upserts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | Hardcoded price constant bypasses model_pricing (ANL-08 anti-pattern) | HIGH | mitigate | no-hardcoded-price grep guard on cost.ts + aggregator.ts (Task 1, S9); prices only from getEffectivePrice reading model_pricing |
| T-07-02 | Tampering | Cache tokens collapsed into one figure (LiteLLM 70%-miscount, ANL-06) | HIGH | mitigate | four distinct token×price terms in computeEstimatedCost; test proves cache_creation and cache_read are independent multiplications (Task 1/D2-24) |
| T-07-03 | Repudiation | Subscription cost silently folded into cost_estimated, or per-user-guessed with no seat table (ANL-09 anti-pattern) | MEDIUM | mitigate | cost_subscription is a distinct rollup column computed separately at the ORG level (no per-seat guess this phase — Q23); test asserts it is NOT in cost_estimated (Task 2/D2-27) |
| T-07-04 | Tampering / Concurrency | Re-running or overlapping cron runs double-count / race concurrent full-day upserts on the same (org_id,*,day) rows | MEDIUM | mitigate | (a) idempotent upsert `ON CONFLICT ... DO UPDATE` replaces the day's rows (D2-23); (b) a per-day `pg_try_advisory_xact_lock` single-flight makes an overlapping/re-triggered run exit early instead of racing (H14); tests assert re-run yields the same rows AND a lock-not-acquired run writes nothing (Task 2) |
| T-07-05 | Information disclosure | Cross-tenant rollup rows | HIGH | mitigate | aggregator groups + writes by org_id from the source rows, never a client-supplied value (T-05-02); parameterized queries (T-05-03) |
| T-07-06 | Tampering | SQL injection in aggregator/rollup/pricing/advisory-lock queries | HIGH | mitigate | parameterized `$1..$N` only in model-pricing.ts + daily-rollups.ts + aggregator queries (T-05-03); the advisory-lock key is parameterized |
| T-07-07 | DoS | Cron aggregates the whole table every run (unbounded scan) | LOW | mitigate | queries scoped to the target day's partition (occurred_at range); document the day-bounded query in the SUMMARY |
| T-07-08 | Repudiation / Correctness | Per-machine (NULL-user) events collapse into one NULL rollup row, mis-attributing cost/usage across machines | MEDIUM | mitigate | per-user grouping on COALESCE(user_id, user_id_unknown) + a deterministic synthetic user_id per machine keeps rows distinct (H4); test asserts two machines → two rows |
| T-07-SC | Tampering | New npm install | LOW | accept | No new runtime dependency — uses existing `pg`, `@fennec/shared`, Cloudflare cron trigger (no binding install); nothing installed |
</threat_model>

<verification>
- `npx vitest run backend/src/workers/cost.test.ts backend/src/workers/no-hardcoded-price.test.ts backend/src/workers/aggregator.test.ts` all green.
- Four-line-item cost with distinct cache terms; effective-date price selection across the Sonnet-5 cutover; no hardcoded price; GPT-model events cost 0 (deferred, Q24) without throwing.
- Per-user rollups DISTINCT per machine via COALESCE(user_id, user_id_unknown) + deterministic synthetic key (not one NULL row — H4); per-project via repo_remote/default-project (H4).
- Reconciliation proven TWO ways: the mocked-arithmetic aggregator.test.ts AND the DISTINCT staging-data aggregator.reconcile.test.ts (rollup totals == direct raw aggregate — ANL-05, H14).
- Advisory-lock single-flight: a lock-not-acquired run writes nothing; re-run yields the same rows (idempotent day-replace — H14/D2-23).
- cost_billed null; cost_subscription distinct, org-level (Q23), excluded from cost_estimated.
- `backend/src/index.ts` exports `{ fetch, queue, scheduled }` (Plan 06 + Plan 07 combined; queue + DLQ wiring preserved); `wrangler.jsonc` has queues (incl. DLQ consumer) + crons; module-worker typecheck test green (C43).
- `npm run typecheck` clean.
- Manual (build-time, gated on Plan 04 pushed + live Cloudflare deploy): trigger the cron on staging after a day of seeded events; confirm rollup rows + reconcile with a raw query; trigger two overlapping runs and confirm no double-count (advisory lock holds).
</verification>

<success_criteria>
- Daily aggregator cron writes daily_rollups_by_user + daily_rollups_by_project under a per-day advisory-lock single-flight; totals reconcile via both a mocked unit test and a distinct staging check (ANL-05, H14).
- Per-user rollups are distinct per machine (not one NULL row) and per-project rollups derive from repo_remote with a default-project fallback (H4).
- cost_estimated uses four distinct cache-token line items (ANL-06); cost_billed distinct + null until reconciliation (ANL-07).
- Pricing read from model_pricing effective-date rows (Anthropic-only seed; GPT deferred), no hardcoded price (ANL-08).
- Subscription cost is a distinct ORG-LEVEL field, never summed into cost_estimated (ANL-09, Q23).
</success_criteria>

<output>
Create `.planning/phases/02-parallel-adapters-backend-analysis-layer/02-07-SUMMARY.md` — record the cron schedule + target-day selection, the per-day advisory-lock key derivation (H14), the per-user COALESCE + deterministic-synthetic-UUID grouping (H4/Q23), the repo_remote→project mapping + default-project fallback used (and its Phase 3/4 maturation note — Q20), the org-level subscription scoping (Q23) + GPT deferral (Q24), the effective-price batch/caching approach, the staging reconciliation check, and confirmation the module-worker export is `{ fetch, queue, scheduled }` (Plan 06's queue + DLQ preserved).
</output>
